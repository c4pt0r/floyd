--- a/ds4_metal.m
+++ b/ds4_metal.m
@@ -13234,10 +13234,92 @@
         uint64_t                out1_dim,
         const ds4_gpu_tensor *x,
         uint64_t                n_tok) {
-    (void)out0; (void)out1; (void)model_map; (void)model_size;
-    (void)weight0_offset; (void)weight1_offset;
-    (void)in_dim; (void)out0_dim; (void)out1_dim; (void)x; (void)n_tok;
-    return 0;
+    if (!g_initialized && !ds4_gpu_init()) return 0;
+    if (!out0 || !out1 || !x || !model_map || n_tok == 0 || n_tok > 4 ||
+        (in_dim & 31u) != 0 || in_dim > UINT32_MAX ||
+        out0_dim == 0 || out0_dim > UINT32_MAX ||
+        out1_dim == 0 || out1_dim > UINT32_MAX) {
+        return 0;
+    }
+
+    @autoreleasepool {
+        id<MTLBuffer> xbuf = ds4_gpu_tensor_buffer(x);
+        id<MTLBuffer> out0buf = ds4_gpu_tensor_buffer(out0);
+        id<MTLBuffer> out1buf = ds4_gpu_tensor_buffer(out1);
+        const uint64_t x_bytes = n_tok * in_dim * sizeof(float);
+        const uint64_t out0_bytes = n_tok * out0_dim * sizeof(float);
+        const uint64_t out1_bytes = n_tok * out1_dim * sizeof(float);
+        if (!xbuf || !out0buf || !out1buf ||
+            ds4_gpu_tensor_bytes(x) < x_bytes ||
+            ds4_gpu_tensor_bytes(out0) < out0_bytes ||
+            ds4_gpu_tensor_bytes(out1) < out1_bytes) {
+            return 0;
+        }
+
+        const uint64_t row_bytes = (in_dim / 32u) * 34u;
+        const uint64_t weight0_bytes = out0_dim * row_bytes;
+        const uint64_t weight1_bytes = out1_dim * row_bytes;
+        if (weight0_offset > model_size ||
+            weight0_bytes > model_size - weight0_offset ||
+            weight1_offset > model_size ||
+            weight1_bytes > model_size - weight1_offset) {
+            return 0;
+        }
+
+        uint64_t weight0_inner = 0;
+        uint64_t weight1_inner = 0;
+        id<MTLBuffer> weight0 = ds4_gpu_wrap_model_range(
+            model_map, model_size, weight0_offset, weight0_bytes,
+            &weight0_inner);
+        id<MTLBuffer> weight1 = ds4_gpu_wrap_model_range(
+            model_map, model_size, weight1_offset, weight1_bytes,
+            &weight1_inner);
+        if (!weight0 || !weight1) return 0;
+
+        ds4_gpu_q8_0_matvec_args args0 =
+            ds4_gpu_make_q8_0_mv_args(in_dim, out0_dim);
+        ds4_gpu_q8_0_matvec_args args1 =
+            ds4_gpu_make_q8_0_mv_args(in_dim, out1_dim);
+        args0.ne11 = args0.ne1 = (int32_t)n_tok;
+        args1.ne11 = args1.ne1 = (int32_t)n_tok;
+        ds4_gpu_mv_dispatch dispatch = ds4_gpu_make_q8_0_mv_dispatch();
+        args0.nr0 = dispatch.nr0;
+        args1.nr0 = dispatch.nr0;
+        const char *pipeline_name = n_tok == 1
+            ? "kernel_dsv4_pair_q8_0_f32_rows1"
+            : (n_tok == 2 ? "kernel_dsv4_pair_q8_0_f32_rows2"
+                          : (n_tok == 3
+                              ? "kernel_dsv4_pair_q8_0_f32_rows3"
+                              : "kernel_dsv4_pair_q8_0_f32_rows4"));
+        id<MTLComputePipelineState> pipeline =
+            ds4_gpu_get_mul_mv_pipeline(pipeline_name, dispatch.nsg);
+        if (!pipeline) return 0;
+
+        int owned = 0;
+        id<MTLCommandBuffer> cb = ds4_gpu_command_buffer(&owned);
+        if (!cb) return 0;
+        id<MTLComputeCommandEncoder> enc = ds4_gpu_compute_encoder(cb);
+        [enc setComputePipelineState:pipeline];
+        [enc setBytes:&args0 length:sizeof(args0) atIndex:0];
+        [enc setBytes:&args1 length:sizeof(args1) atIndex:1];
+        [enc setBuffer:weight0 offset:(NSUInteger)weight0_inner atIndex:2];
+        [enc setBuffer:weight1 offset:(NSUInteger)weight1_inner atIndex:3];
+        [enc setBuffer:xbuf offset:ds4_gpu_tensor_offset(x) atIndex:4];
+        [enc setBuffer:out0buf offset:ds4_gpu_tensor_offset(out0) atIndex:5];
+        [enc setBuffer:out1buf offset:ds4_gpu_tensor_offset(out1) atIndex:6];
+        [enc setThreadgroupMemoryLength:dispatch.smem atIndex:0];
+        const NSUInteger max_out = (NSUInteger)(out0_dim > out1_dim
+            ? out0_dim : out1_dim);
+        [enc dispatchThreadgroups:MTLSizeMake(
+                (max_out + (NSUInteger)dispatch.nr0 - 1u) /
+                    (NSUInteger)dispatch.nr0,
+                1,
+                2)
+             threadsPerThreadgroup:MTLSizeMake(
+                32, (NSUInteger)dispatch.nsg, 1)];
+        ds4_gpu_end_compute_encoder(cb, enc);
+        return ds4_gpu_finish_command_buffer(cb, owned, "Q8_0 paired matvec");
+    }
 }
 
 int ds4_gpu_matmul_q8_0_f16_out_tensor(
--- a/metal/dense.metal
+++ b/metal/dense.metal
@@ -286,8 +286,78 @@
         ushort sgitg [[simdgroup_index_in_threadgroup]]) {
     kernel_mul_mv_q8_0_f32_exact_rows_impl<4>(args, src0, src1, dst, shmem,
                                               tgpig, tiisg, sgitg);
+}
+
+[[host_name("kernel_dsv4_pair_q8_0_f32_rows1")]]
+kernel void kernel_dsv4_pair_q8_0_f32_rows1(
+        constant ds4_metal_args_mul_mv &args0,
+        constant ds4_metal_args_mul_mv &args1,
+        device const char *src00, device const char *src01,
+        device const char *src1,
+        device char *dst0, device char *dst1,
+        threadgroup char *shmem [[threadgroup(0)]],
+        uint3 tgpig [[threadgroup_position_in_grid]],
+        ushort tiisg [[thread_index_in_simdgroup]],
+        ushort sgitg [[simdgroup_index_in_threadgroup]]) {
+    const uint3 local_tgpig = uint3(tgpig.x, 0, 0);
+    if (tgpig.z == 0) {
+        if (tgpig.x * N_R0_Q8_0 >= args0.ne01) return;
+        kernel_mul_mv_q8_0_f32_impl<N_R0_Q8_0,
+            constant ds4_metal_args_mul_mv &>(
+                args0, src00, src1, dst0, shmem, local_tgpig, tiisg, sgitg);
+    } else {
+        if (tgpig.x * N_R0_Q8_0 >= args1.ne01) return;
+        kernel_mul_mv_q8_0_f32_impl<N_R0_Q8_0,
+            constant ds4_metal_args_mul_mv &>(
+                args1, src01, src1, dst1, shmem, local_tgpig, tiisg, sgitg);
+    }
+}
+
+template<short NR1>
+void kernel_dsv4_pair_q8_0_f32_exact_rows_impl(
+        constant ds4_metal_args_mul_mv &args0,
+        constant ds4_metal_args_mul_mv &args1,
+        device const char *src00, device const char *src01,
+        device const char *src1,
+        device char *dst0, device char *dst1,
+        threadgroup char *shmem,
+        uint3 tgpig,
+        ushort tiisg,
+        ushort sgitg) {
+    const uint3 local_tgpig = uint3(tgpig.x, 0, 0);
+    if (tgpig.z == 0) {
+        if (tgpig.x * N_R0_Q8_0 >= args0.ne01) return;
+        kernel_mul_mv_q8_0_f32_exact_rows_impl<NR1>(
+            args0, src00, src1, dst0, shmem, local_tgpig, tiisg, sgitg);
+    } else {
+        if (tgpig.x * N_R0_Q8_0 >= args1.ne01) return;
+        kernel_mul_mv_q8_0_f32_exact_rows_impl<NR1>(
+            args1, src01, src1, dst1, shmem, local_tgpig, tiisg, sgitg);
+    }
+}
+
+#define DS4_PAIR_Q8_EXACT_ROWS_KERNEL(N) \
+[[host_name("kernel_dsv4_pair_q8_0_f32_rows" #N)]] \
+kernel void kernel_dsv4_pair_q8_0_f32_rows##N( \
+        constant ds4_metal_args_mul_mv &args0, \
+        constant ds4_metal_args_mul_mv &args1, \
+        device const char *src00, device const char *src01, \
+        device const char *src1, \
+        device char *dst0, device char *dst1, \
+        threadgroup char *shmem [[threadgroup(0)]], \
+        uint3 tgpig [[threadgroup_position_in_grid]], \
+        ushort tiisg [[thread_index_in_simdgroup]], \
+        ushort sgitg [[simdgroup_index_in_threadgroup]]) { \
+    kernel_dsv4_pair_q8_0_f32_exact_rows_impl<N>( \
+        args0, args1, src00, src01, src1, dst0, dst1, shmem, \
+        tgpig, tiisg, sgitg); \
 }
 
+DS4_PAIR_Q8_EXACT_ROWS_KERNEL(2)
+DS4_PAIR_Q8_EXACT_ROWS_KERNEL(3)
+DS4_PAIR_Q8_EXACT_ROWS_KERNEL(4)
+#undef DS4_PAIR_Q8_EXACT_ROWS_KERNEL
+
 // Decode shared-expert gate/up projections followed by SwiGLU:
 //
 //     mid = silu(min(gate, limit)) * clamp(up, -limit, limit)
