diff --git a/ds4_metal.m b/ds4_metal.m
index 9db774d..c847f8b 100644
--- a/ds4_metal.m
+++ b/ds4_metal.m
@@ -13295,10 +13295,78 @@ int ds4_gpu_matmul_q8_0_pair_tensor(
         uint64_t                out1_dim,
         const ds4_gpu_tensor *x,
         uint64_t                n_tok) {
-    (void)out0; (void)out1; (void)model_map; (void)model_size;
-    (void)weight0_offset; (void)weight1_offset;
-    (void)in_dim; (void)out0_dim; (void)out1_dim; (void)x; (void)n_tok;
-    return 0;
+    if (!g_initialized && !ds4_gpu_init()) return 0;
+    if (!out0 || !out1 || !model_map || !x || n_tok != 1 ||
+        (in_dim & 31u) != 0 || in_dim > UINT32_MAX ||
+        out0_dim == 0 || out0_dim > UINT32_MAX ||
+        out1_dim == 0 || out1_dim > UINT32_MAX) {
+        return 0;
+    }
+
+    @autoreleasepool {
+        id<MTLBuffer> xbuf = ds4_gpu_tensor_buffer(x);
+        id<MTLBuffer> out0buf = ds4_gpu_tensor_buffer(out0);
+        id<MTLBuffer> out1buf = ds4_gpu_tensor_buffer(out1);
+        if (!xbuf || !out0buf || !out1buf ||
+            ds4_gpu_tensor_bytes(x) < in_dim * sizeof(float) ||
+            ds4_gpu_tensor_bytes(out0) < out0_dim * sizeof(float) ||
+            ds4_gpu_tensor_bytes(out1) < out1_dim * sizeof(float)) {
+            return 0;
+        }
+
+        const uint64_t row_bytes = (in_dim / 32u) * 34u;
+        const uint64_t weight0_bytes = out0_dim * row_bytes;
+        const uint64_t weight1_bytes = out1_dim * row_bytes;
+        if (weight0_offset > model_size ||
+            weight0_bytes > model_size - weight0_offset ||
+            weight1_offset > model_size ||
+            weight1_bytes > model_size - weight1_offset) {
+            return 0;
+        }
+
+        uint64_t weight0_inner = 0;
+        uint64_t weight1_inner = 0;
+        id<MTLBuffer> weight0 = ds4_gpu_wrap_model_range(
+                model_map, model_size, weight0_offset, weight0_bytes,
+                &weight0_inner);
+        id<MTLBuffer> weight1 = ds4_gpu_wrap_model_range(
+                model_map, model_size, weight1_offset, weight1_bytes,
+                &weight1_inner);
+        if (!weight0 || !weight1) return 0;
+
+        ds4_gpu_q8_0_matvec_args args0 =
+            ds4_gpu_make_q8_0_mv_args(in_dim, out0_dim);
+        ds4_gpu_q8_0_matvec_args args1 =
+            ds4_gpu_make_q8_0_mv_args(in_dim, out1_dim);
+        ds4_gpu_mv_dispatch dispatch = ds4_gpu_make_q8_0_mv_dispatch();
+        args0.nr0 = dispatch.nr0;
+        args1.nr0 = dispatch.nr0;
+        id<MTLComputePipelineState> pipeline = ds4_gpu_get_mul_mv_pipeline(
+                "kernel_mul_mv_q8_0_f32_pair", dispatch.nsg);
+        if (!pipeline) return 0;
+
+        int owned = 0;
+        id<MTLCommandBuffer> cb = ds4_gpu_command_buffer(&owned);
+        if (!cb) return 0;
+        id<MTLComputeCommandEncoder> enc = ds4_gpu_compute_encoder(cb);
+        [enc setComputePipelineState:pipeline];
+        [enc setBytes:&args0 length:sizeof(args0) atIndex:0];
+        [enc setBytes:&args1 length:sizeof(args1) atIndex:1];
+        [enc setBuffer:weight0 offset:(NSUInteger)weight0_inner atIndex:2];
+        [enc setBuffer:weight1 offset:(NSUInteger)weight1_inner atIndex:3];
+        [enc setBuffer:xbuf offset:ds4_gpu_tensor_offset(x) atIndex:4];
+        [enc setBuffer:out0buf offset:ds4_gpu_tensor_offset(out0) atIndex:5];
+        [enc setBuffer:out1buf offset:ds4_gpu_tensor_offset(out1) atIndex:6];
+        [enc setThreadgroupMemoryLength:dispatch.smem atIndex:0];
+        const uint64_t max_out = out0_dim > out1_dim ? out0_dim : out1_dim;
+        [enc dispatchThreadgroups:MTLSizeMake(
+                 ((NSUInteger)max_out + (NSUInteger)dispatch.nr0 - 1u) /
+                     (NSUInteger)dispatch.nr0,
+                 1, 1)
+             threadsPerThreadgroup:MTLSizeMake(32, (NSUInteger)dispatch.nsg, 1)];
+        ds4_gpu_end_compute_encoder(cb, enc);
+        return ds4_gpu_finish_command_buffer(cb, owned, "Q8_0 tensor pair matvec");
+    }
 }
 
 int ds4_gpu_matmul_q8_0_f16_out_tensor(
diff --git a/metal/dense.metal b/metal/dense.metal
index 4dd9d4f..a990f27 100644
--- a/metal/dense.metal
+++ b/metal/dense.metal
@@ -190,6 +190,33 @@ kernel void kernel_mul_mv_q8_0_f32(
     kernel_mul_mv_q8_0_f32_impl<N_R0_Q8_0, constant ds4_metal_args_mul_mv &>(args, src0, src1, dst, shmem, tgpig, tiisg, sgitg);
 }
 
+// Decode q_a and kv projections share one activation row. Keep the existing
+// Q8 reduction order while avoiding a second Metal dispatch and encoder.
+[[host_name("kernel_mul_mv_q8_0_f32_pair")]]
+kernel void kernel_mul_mv_q8_0_f32_pair(
+        constant ds4_metal_args_mul_mv & args0,
+        constant ds4_metal_args_mul_mv & args1,
+        device const char * src0_0,
+        device const char * src0_1,
+        device const char * src1,
+        device       char * dst0,
+        device       char * dst1,
+        threadgroup  char * shmem [[threadgroup(0)]],
+        uint3  tgpig[[threadgroup_position_in_grid]],
+        ushort tiisg[[thread_index_in_simdgroup]],
+        ushort sgitg[[simdgroup_index_in_threadgroup]]) {
+    constexpr short NR0 = N_R0_Q8_0;
+    const int r0 = tgpig.x * NR0;
+    if (r0 < args0.ne01) {
+        kernel_mul_mv_q8_0_f32_impl<NR0, constant ds4_metal_args_mul_mv &>(
+                args0, src0_0, src1, dst0, shmem, tgpig, tiisg, sgitg);
+    }
+    if (r0 < args1.ne01) {
+        kernel_mul_mv_q8_0_f32_impl<NR0, constant ds4_metal_args_mul_mv &>(
+                args1, src0_1, src1, dst1, shmem, tgpig, tiisg, sgitg);
+    }
+}
+
 template<short NR1, typename args_t>
 void kernel_mul_mv_q8_0_f32_exact_rows_impl(
         args_t args,
