--- a/ds4.c
+++ b/ds4.c
@@ -3602,7 +3602,17 @@
         tensor_expect_layout(w->output_hc_fn,    DS4_TENSOR_F16,  2, hc_dim, DS4_N_HC, 0);
         tensor_expect_layout(w->output_hc_scale, DS4_TENSOR_F32,  1, 1, 0, 0);
         tensor_expect_layout(w->output_norm,     DS4_TENSOR_F32,  1, DS4_N_EMBD, 0, 0);
-        tensor_expect_layout(w->output,          DS4_TENSOR_Q8_0, 2, DS4_N_EMBD, DS4_N_VOCAB, 0);
+        if (w->output->type != DS4_TENSOR_Q8_0 &&
+            w->output->type != DS4_TENSOR_Q4_K) {
+            fprintf(stderr,
+                    "ds4: tensor %.*s has type %s, expected q8_0 or q4_k\n",
+                    (int)w->output->name.len,
+                    w->output->name.ptr,
+                    tensor_type_name(w->output->type));
+            exit(1);
+        }
+        tensor_expect_layout(w->output, w->output->type,
+                             2, DS4_N_EMBD, DS4_N_VOCAB, 0);
     }
 
     for (uint32_t il = layer_start; il <= layer_end; il++) {
@@ -16357,14 +16367,13 @@
     if (ok) {
         metal_graph_debug_dump_tensor("result_norm", g->output_norm, DS4_N_EMBD, DS4_N_LAYER, 0);
     }
-    if (ok) ok = ds4_gpu_matmul_q8_0_tensor(g->logits,
-                                              model->map,
-                                              model->size,
-                                              weights->output->abs_offset,
-                                              DS4_N_EMBD,
-                                              vocab_dim,
-                                              g->output_norm,
-                                              1) != 0;
+    if (ok) ok = metal_graph_matmul_plain_tensor(g->logits,
+                                                  model,
+                                                  weights->output,
+                                                  DS4_N_EMBD,
+                                                  vocab_dim,
+                                                  g->output_norm,
+                                                  1);
     if (ok) {
         metal_graph_debug_dump_tensor("result_output", g->logits, vocab_dim, DS4_N_LAYER, 0);
     }
@@ -16446,14 +16455,13 @@
                                                        DS4_N_EMBD,
                                                        n_tokens,
                                                        DS4_RMS_EPS) != 0;
-    if (ok) ok = ds4_gpu_matmul_q8_0_tensor(logits,
-                                              model->map,
-                                              model->size,
-                                              weights->output->abs_offset,
-                                              DS4_N_EMBD,
-                                              vocab_dim,
-                                              output_norm,
-                                              n_tokens) != 0;
+    if (ok) ok = metal_graph_matmul_plain_tensor(logits,
+                                                  model,
+                                                  weights->output,
+                                                  DS4_N_EMBD,
+                                                  vocab_dim,
+                                                  output_norm,
+                                                  n_tokens);
 
     ds4_gpu_tensor_free(logits);
     ds4_gpu_tensor_free(output_norm);
@@ -16483,6 +16491,10 @@
         return ds4_gpu_matmul_q8_0_tensor(out, model->map, model->size,
                                             w->abs_offset, in_dim, out_dim, x, n_tok) != 0;
     }
+    if (w->type == DS4_TENSOR_Q4_K) {
+        return ds4_gpu_matmul_q4_k_tensor(out, model->map, model->size,
+                                           w->abs_offset, in_dim, out_dim, x, n_tok) != 0;
+    }
     fprintf(stderr, "ds4: Metal plain matmul does not support %s\n", tensor_type_name(w->type));
     return false;
 }
@@ -16536,14 +16548,13 @@
                                                   mtp->norm->abs_offset,
                                                   DS4_N_EMBD,
                                                   DS4_RMS_EPS) != 0;
-    if (ok) ok = ds4_gpu_matmul_q8_0_tensor(g->logits,
-                                              base_model->map,
-                                              base_model->size,
-                                              base_weights->output->abs_offset,
-                                              DS4_N_EMBD,
-                                              vocab_dim,
-                                              g->output_norm,
-                                              1) != 0;
+    if (ok) ok = metal_graph_matmul_plain_tensor(g->logits,
+                                                  base_model,
+                                                  base_weights->output,
+                                                  DS4_N_EMBD,
+                                                  vocab_dim,
+                                                  g->output_norm,
+                                                  1);
     return ok;
 }
 
@@ -20689,10 +20700,9 @@
     if (ok) ok = ds4_gpu_rms_norm_weight_rows_tensor(
             output_norm, output_embd, support_model->map, support_model->size,
             dspark->norm->abs_offset, DS4_N_EMBD, n_tokens, DS4_RMS_EPS) != 0;
-    if (ok) ok = ds4_gpu_matmul_q8_0_tensor(
-            logits, base_model->map, base_model->size,
-            base_weights->output->abs_offset, DS4_N_EMBD, DS4_N_VOCAB,
-            output_norm, n_tokens) != 0;
+    if (ok) ok = metal_graph_matmul_plain_tensor(
+            logits, base_model, base_weights->output,
+            DS4_N_EMBD, DS4_N_VOCAB, output_norm, n_tokens);
     ds4_gpu_tensor_free(logits);
     ds4_gpu_tensor_free(output_norm);
     ds4_gpu_tensor_free(output_embd);
