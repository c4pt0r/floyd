diff --git a/ds4.c b/ds4.c
index d11be5d..1b9cf05 100644
--- a/ds4.c
+++ b/ds4.c
@@ -20666,10 +20666,8 @@ static bool metal_graph_eval_mtp_draft(
                                               top_id);
 }
 
-static bool metal_graph_encode_dspark_head_batch(
+static bool metal_graph_encode_dspark_head_hidden_batch(
         ds4_gpu_graph *g,
-        const ds4_model *base_model,
-        const ds4_weights *base_weights,
         const ds4_model *support_model,
         const ds4_dspark_weights *dspark,
         uint32_t n_tokens) {
@@ -20682,9 +20680,7 @@ static bool metal_graph_encode_dspark_head_batch(
             g->batch_ffn_cur, 0, (uint64_t)n_tokens * DS4_N_EMBD * sizeof(float));
     ds4_gpu_tensor *output_norm = ds4_gpu_tensor_view(
             g->batch_ffn_norm, 0, (uint64_t)n_tokens * DS4_N_EMBD * sizeof(float));
-    ds4_gpu_tensor *logits = ds4_gpu_tensor_view(
-            g->spec_logits, 0, (uint64_t)n_tokens * DS4_N_VOCAB * sizeof(float));
-    bool ok = output_pre && output_weights && output_embd && output_norm && logits;
+    bool ok = output_pre && output_weights && output_embd && output_norm;
     if (ok) ok = ds4_gpu_rms_norm_plain_rows_tensor(
             g->batch_flat_hc, g->batch_cur_hc, (uint32_t)hc_dim,
             n_tokens, DS4_RMS_EPS) != 0;
@@ -20702,14 +20698,30 @@ static bool metal_graph_encode_dspark_head_batch(
     if (ok) ok = ds4_gpu_rms_norm_weight_rows_tensor(
             output_norm, output_embd, support_model->map, support_model->size,
             dspark->norm->abs_offset, DS4_N_EMBD, n_tokens, DS4_RMS_EPS) != 0;
+    ds4_gpu_tensor_free(output_norm);
+    ds4_gpu_tensor_free(output_embd);
+    ds4_gpu_tensor_free(output_weights);
+    ds4_gpu_tensor_free(output_pre);
+    return ok;
+}
+
+static bool metal_graph_encode_dspark_logits_batch(
+        ds4_gpu_graph *g,
+        const ds4_model *base_model,
+        const ds4_weights *base_weights,
+        uint32_t n_tokens) {
+    ds4_gpu_tensor *output_norm = ds4_gpu_tensor_view(
+            g->batch_ffn_norm, 0,
+            (uint64_t)n_tokens * DS4_N_EMBD * sizeof(float));
+    ds4_gpu_tensor *logits = ds4_gpu_tensor_view(
+            g->spec_logits, 0,
+            (uint64_t)n_tokens * DS4_N_VOCAB * sizeof(float));
+    bool ok = output_norm && logits;
     if (ok) ok = metal_graph_matmul_plain_tensor(
             logits, base_model, base_weights->output,
             DS4_N_EMBD, DS4_N_VOCAB, output_norm, n_tokens);
     ds4_gpu_tensor_free(logits);
     ds4_gpu_tensor_free(output_norm);
-    ds4_gpu_tensor_free(output_embd);
-    ds4_gpu_tensor_free(output_weights);
-    ds4_gpu_tensor_free(output_pre);
     return ok;
 }
 
@@ -20721,14 +20733,17 @@ static bool metal_graph_eval_dspark_proposals(
         const ds4_dspark_weights *dspark,
         int input_token,
         int proposal_count,
+        float confidence_threshold,
         int output_ids[6],
-        float output_confidence[5]) {
+        float output_confidence[5],
+        bool *early_skip) {
     enum { DSPARK_MAX_STAGE_TOKENS = 5, DSPARK_NOISE = 128799 };
     if (!g || !g->dspark_enabled || !support_model || !dspark ||
-        !output_ids || !output_confidence || proposal_count < 2 ||
+        !output_ids || !output_confidence || !early_skip || proposal_count < 2 ||
         proposal_count > 6 ||
         g->dspark_position == 0) return false;
     const int stage_tokens = proposal_count - 1;
+    *early_skip = false;
 
     int draft_ids[DSPARK_MAX_STAGE_TOKENS] = {
         input_token, DSPARK_NOISE, DSPARK_NOISE, DSPARK_NOISE, DSPARK_NOISE,
@@ -20767,7 +20782,12 @@ static bool metal_graph_eval_dspark_proposals(
     ds4_gpu_tensor *confidence_hidden = ds4_gpu_tensor_view(
             g->batch_router_probs, 0,
             (uint64_t)stage_tokens * sizeof(float));
-    if (!draft_hidden || !confidence_hidden) ok = false;
+    ds4_gpu_tensor *markov_score0 = ds4_gpu_tensor_view(
+            g->batch_router_weights, 0, sizeof(float));
+    ds4_gpu_tensor *hidden_score0 = ds4_gpu_tensor_view(
+            confidence_hidden, 0, sizeof(float));
+    if (!draft_hidden || !confidence_hidden || !markov_score0 || !hidden_score0)
+        ok = false;
 
     if (ok) ok = ds4_gpu_begin_commands() != 0;
     for (uint32_t stage = 0; ok && stage < 3; stage++) {
@@ -20779,12 +20799,11 @@ static bool metal_graph_eval_dspark_proposals(
                                              g->dspark_position,
                                              (uint32_t)stage_tokens);
     }
-    if (ok) ok = metal_graph_encode_dspark_head_batch(g,
-                                                       base_model,
-                                                       base_weights,
-                                                       support_model,
-                                                       dspark,
-                                                       (uint32_t)stage_tokens);
+    if (ok) ok = metal_graph_encode_dspark_head_hidden_batch(
+            g,
+            support_model,
+            dspark,
+            (uint32_t)stage_tokens);
     if (ok) ok = ds4_gpu_matmul_f32_tensor(
             confidence_hidden,
             support_model->map,
@@ -20794,16 +20813,62 @@ static bool metal_graph_eval_dspark_proposals(
             1,
             draft_hidden,
             (uint32_t)stage_tokens) != 0;
+    if (ok) ok = ds4_gpu_embed_token_hc_tensor(
+            g->mtp_embed,
+            support_model->map,
+            support_model->size,
+            dspark->markov_w1->abs_offset,
+            (uint32_t)dspark->markov_w1->dim[1],
+            (uint32_t)input_token,
+            256,
+            1) != 0;
+    if (ok) ok = ds4_gpu_matmul_f32_tensor(
+            markov_score0,
+            support_model->map,
+            support_model->size,
+            dspark->confidence_proj->abs_offset +
+                (uint64_t)DS4_N_EMBD * sizeof(float),
+            256,
+            1,
+            g->mtp_embed,
+            1) != 0;
     if (ok) ok = ds4_gpu_end_commands() != 0;
     else (void)ds4_gpu_synchronize();
 
+    float first_hidden_confidence = 0.0f;
+    float first_markov_confidence = 0.0f;
+    if (ok) ok = ds4_gpu_tensor_read(
+            hidden_score0, 0,
+            &first_hidden_confidence, sizeof(first_hidden_confidence)) != 0;
+    if (ok) ok = ds4_gpu_tensor_read(
+            markov_score0, 0,
+            &first_markov_confidence, sizeof(first_markov_confidence)) != 0;
+    const float first_confidence =
+        first_hidden_confidence + first_markov_confidence;
+    const float first_probability =
+        1.0f / (1.0f + expf(-first_confidence));
+    const bool skip_head = ok && confidence_threshold > 0.0f &&
+        first_probability < confidence_threshold;
+
     output_ids[0] = input_token;
+    if (skip_head) {
+        output_confidence[0] = first_confidence;
+        for (int position = 1; position < stage_tokens; position++)
+            output_confidence[position] = -FLT_MAX;
+        *early_skip = true;
+    }
+
     ds4_gpu_tensor *selected_ids = ds4_gpu_tensor_view(
             g->batch_router_selected, 0,
             (uint64_t)stage_tokens * sizeof(int32_t));
     if (!selected_ids) ok = false;
-    if (ok) ok = ds4_gpu_begin_commands() != 0;
-    for (int position = 0; ok && position < stage_tokens; position++) {
+    if (ok && !skip_head) ok = ds4_gpu_begin_commands() != 0;
+    if (ok && !skip_head) ok = metal_graph_encode_dspark_logits_batch(
+            g,
+            base_model,
+            base_weights,
+            (uint32_t)stage_tokens);
+    for (int position = 0; ok && !skip_head && position < stage_tokens; position++) {
         ds4_gpu_tensor *row_logits = ds4_gpu_tensor_view(
                 g->spec_logits,
                 (uint64_t)position * DS4_N_VOCAB * sizeof(float),
@@ -20828,17 +20893,7 @@ static bool metal_graph_eval_dspark_proposals(
             : NULL;
         ok = row_logits && hidden_score && markov_score && selected_id &&
              (position == 0 || previous_id);
-        if (ok && position == 0) {
-            ok = ds4_gpu_embed_token_hc_tensor(
-                    g->mtp_embed,
-                    support_model->map,
-                    support_model->size,
-                    dspark->markov_w1->abs_offset,
-                    (uint32_t)dspark->markov_w1->dim[1],
-                    (uint32_t)input_token,
-                    256,
-                    1) != 0;
-        } else if (ok) {
+        if (ok && position > 0) {
             ok = ds4_gpu_embed_tokens_hc_tensor(
                     g->mtp_embed,
                     previous_id,
@@ -20866,7 +20921,7 @@ static bool metal_graph_eval_dspark_proposals(
         if (ok) ok = ds4_gpu_argmax_tensor(selected_id,
                                             row_logits,
                                             DS4_N_VOCAB) != 0;
-        if (ok) ok = ds4_gpu_matmul_f32_tensor(
+        if (ok && position > 0) ok = ds4_gpu_matmul_f32_tensor(
                 markov_score,
                 support_model->map,
                 support_model->size,
@@ -20886,16 +20941,18 @@ static bool metal_graph_eval_dspark_proposals(
         ds4_gpu_tensor_free(hidden_score);
         ds4_gpu_tensor_free(row_logits);
     }
-    if (ok) ok = ds4_gpu_end_commands() != 0;
-    else (void)ds4_gpu_synchronize();
-    if (ok) ok = ds4_gpu_tensor_read(
+    if (ok && !skip_head) ok = ds4_gpu_end_commands() != 0;
+    else if (!ok) (void)ds4_gpu_synchronize();
+    if (ok && !skip_head) ok = ds4_gpu_tensor_read(
             selected_ids, 0, output_ids + 1,
             (uint64_t)stage_tokens * sizeof(output_ids[0])) != 0;
-    if (ok) ok = ds4_gpu_tensor_read(
+    if (ok && !skip_head) ok = ds4_gpu_tensor_read(
             g->batch_router_weights, 0, output_confidence,
             (uint64_t)stage_tokens * sizeof(output_confidence[0])) != 0;
 
     ds4_gpu_tensor_free(selected_ids);
+    ds4_gpu_tensor_free(hidden_score0);
+    ds4_gpu_tensor_free(markov_score0);
     ds4_gpu_tensor_free(confidence_hidden);
     ds4_gpu_tensor_free(draft_hidden);
 
@@ -21023,9 +21080,10 @@ int ds4_dspark_probe_stages(const char *base_path, const char *support_path,
         }
     }
     if (ok) ok = ds4_gpu_begin_commands() != 0;
-    if (ok) ok = metal_graph_encode_dspark_head_batch(
-                        &graph, &base_model, &base_weights,
-                        &support_model, &dspark, DSPARK_DRAFT);
+    if (ok) ok = metal_graph_encode_dspark_head_hidden_batch(
+                        &graph, &support_model, &dspark, DSPARK_DRAFT);
+    if (ok) ok = metal_graph_encode_dspark_logits_batch(
+                        &graph, &base_model, &base_weights, DSPARK_DRAFT);
     if (ok) ok = ds4_gpu_end_commands() != 0;
 
     output_ids[0] = input_token;
@@ -28362,6 +28420,7 @@ static int ds4_session_eval_internal(ds4_session *s, int token, bool probe_mtp,
         int proposal_count = e->mtp_draft_tokens;
         if (proposal_count > 6) proposal_count = 6;
         const double proposal_started = now_sec();
+        bool proposal_early_skip = false;
         if (input_token >= 0 && metal_graph_eval_dspark_proposals(
                 &s->graph,
                 &e->model,
@@ -28370,8 +28429,11 @@ static int ds4_session_eval_internal(ds4_session *s, int token, bool probe_mtp,
                 &e->dspark_weights,
                 input_token,
                 proposal_count,
+                e->dspark_confidence_threshold,
                 s->dspark_proposals,
-                s->dspark_confidence)) {
+                s->dspark_confidence,
+                &proposal_early_skip)) {
+            if (proposal_early_skip) s->spec_stats.proposal_early_skips++;
             const int sampled_count = proposal_count - 1;
             const int confident_count = ds4_dspark_confident_prefix_length_staged(
                     s->dspark_confidence,
diff --git a/ds4.h b/ds4.h
index 2cc035d..074bec6 100644
--- a/ds4.h
+++ b/ds4.h
@@ -67,6 +67,7 @@ typedef struct {
     double verify_head_ms;
     double verify_read_ms;
     double replay_ms;
+    uint64_t proposal_early_skips;
     uint64_t verify_outcome_calls[7][7];
     double verify_outcome_ms[7][7];
 } ds4_session_spec_stats;
