diff --git a/ds4.c b/ds4.c
index 968d8cb..0e1d11d 100644
--- a/ds4.c
+++ b/ds4.c
@@ -28443,6 +28443,8 @@ int ds4_session_get_kernel_stats(const ds4_session *s,
             gpu_stats.tiny_batch_exact_attn_calls;
         out->tiny_batch_exact_q8_calls =
             gpu_stats.tiny_batch_exact_q8_calls;
+        out->tiny_batch_exact_f16_calls =
+            gpu_stats.tiny_batch_exact_f16_calls;
     }
 #endif
     return 1;
diff --git a/ds4.h b/ds4.h
index 0381754..786cea7 100644
--- a/ds4.h
+++ b/ds4.h
@@ -80,6 +80,7 @@ typedef struct {
     uint64_t tiny_batch_raw_store_calls;
     uint64_t tiny_batch_exact_attn_calls;
     uint64_t tiny_batch_exact_q8_calls;
+    uint64_t tiny_batch_exact_f16_calls;
 } ds4_session_kernel_stats;
 
 #define DS4_DEFAULT_TEMPERATURE 1.0f
diff --git a/ds4_gpu.h b/ds4_gpu.h
index 529d2ab..1e60e7b 100644
--- a/ds4_gpu.h
+++ b/ds4_gpu.h
@@ -28,6 +28,7 @@ typedef struct {
     uint64_t tiny_batch_raw_store_calls;
     uint64_t tiny_batch_exact_attn_calls;
     uint64_t tiny_batch_exact_q8_calls;
+    uint64_t tiny_batch_exact_f16_calls;
     uint64_t exact_q8_pipeline_warmups;
     uint64_t fast_argmax_calls;
 } ds4_gpu_kernel_stats;
diff --git a/ds4_metal.m b/ds4_metal.m
index 0f0a148..90dd0db 100644
--- a/ds4_metal.m
+++ b/ds4_metal.m
@@ -349,6 +349,7 @@ static int ds4_gpu_stream_expert_cache_entry_protected(
 static uint64_t g_tiny_batch_raw_store_calls;
 static uint64_t g_tiny_batch_exact_attn_calls;
 static uint64_t g_tiny_batch_exact_q8_calls;
+static uint64_t g_tiny_batch_exact_f16_calls;
 static uint64_t g_exact_q8_pipeline_warmups;
 static uint64_t g_fast_argmax_calls;
 
@@ -364,6 +365,7 @@ void ds4_gpu_get_kernel_stats(ds4_gpu_kernel_stats *out) {
     out->tiny_batch_raw_store_calls = g_tiny_batch_raw_store_calls;
     out->tiny_batch_exact_attn_calls = g_tiny_batch_exact_attn_calls;
     out->tiny_batch_exact_q8_calls = g_tiny_batch_exact_q8_calls;
+    out->tiny_batch_exact_f16_calls = g_tiny_batch_exact_f16_calls;
     out->exact_q8_pipeline_warmups = g_exact_q8_pipeline_warmups;
     out->fast_argmax_calls = g_fast_argmax_calls;
 }
@@ -6358,6 +6360,20 @@ int ds4_gpu_init(void) {
             }
             g_exact_q8_pipeline_warmups++;
         }
+        static const char *exact_f16_pipeline_names[] = {
+            "kernel_mul_mv_f16_f32_exact_rows2",
+            "kernel_mul_mv_f16_f32_exact_rows3",
+            "kernel_mul_mv_f16_f32_exact_rows4",
+        };
+        for (size_t i = 0;
+             i < sizeof(exact_f16_pipeline_names) / sizeof(exact_f16_pipeline_names[0]);
+             i++) {
+            if (!ds4_gpu_get_mul_mv_pipeline(exact_f16_pipeline_names[i], 8)) {
+                g_queue = nil;
+                g_device = nil;
+                return 0;
+            }
+        }
 
         g_initialized = 1;
     }
@@ -13530,8 +13546,17 @@ int ds4_gpu_matmul_f16_tensor(
                 mv_dispatch.smem = 32u * 4u * sizeof(float);
             }
             mv_args.nr0 = mv_dispatch.nr0;
+            const bool exact_rows = tiny_decode_order && n_tok >= 2 && n_tok <= 4 &&
+                in_dim >= 32u && (in_dim % 4u) == 0 &&
+                getenv("DS4_METAL_DISABLE_TINY_EXACT_F16_ROWS") == NULL;
+            const char *pipeline_name = mv_dispatch.function_name;
+            if (exact_rows) {
+                pipeline_name = n_tok == 2 ? "kernel_mul_mv_f16_f32_exact_rows2" :
+                    (n_tok == 3 ? "kernel_mul_mv_f16_f32_exact_rows3" :
+                                  "kernel_mul_mv_f16_f32_exact_rows4");
+            }
             id<MTLComputePipelineState> pipeline =
-                ds4_gpu_get_mul_mv_pipeline(mv_dispatch.function_name, mv_dispatch.nsg);
+                ds4_gpu_get_mul_mv_pipeline(pipeline_name, mv_dispatch.nsg);
             if (!pipeline) return 0;
 
             id<MTLComputeCommandEncoder> enc = ds4_gpu_compute_encoder(cb);
@@ -13544,10 +13569,11 @@ int ds4_gpu_matmul_f16_tensor(
                 [enc setThreadgroupMemoryLength:mv_dispatch.smem atIndex:0];
             }
             [enc dispatchThreadgroups:MTLSizeMake(((NSUInteger)out_dim + (NSUInteger)mv_dispatch.nr0 - 1u) / (NSUInteger)mv_dispatch.nr0,
-                                                  (NSUInteger)n_tok,
+                                                  exact_rows ? 1u : (NSUInteger)n_tok,
                                                   1)
                  threadsPerThreadgroup:MTLSizeMake(32, (NSUInteger)mv_dispatch.nsg, 1)];
             ds4_gpu_end_compute_encoder(cb, enc);
+            if (exact_rows) g_tiny_batch_exact_f16_calls++;
 
             if (!ds4_gpu_finish_command_buffer(cb, owned, "F16 tensor matvec")) return 0;
             return 1;
diff --git a/metal/dense.metal b/metal/dense.metal
index 1ae6ea7..f3ae9b0 100644
--- a/metal/dense.metal
+++ b/metal/dense.metal
@@ -652,6 +652,128 @@ typedef decltype(kernel_mul_mv_t_t_4<half, half4, half, half4>) mul_mv_t_t_4;
 template [[host_name("kernel_mul_mv_f32_f32_4")]] kernel mul_mv_t_t_4 kernel_mul_mv_t_t_4<float, float4, float, float4>;
 template [[host_name("kernel_mul_mv_f16_f32_4")]] kernel mul_mv_t_t_4 kernel_mul_mv_t_t_4<half,  half4,  float, float4>;
 
+template<short NR1, short NR0, typename args_t>
+void kernel_mul_mv_f16_f32_exact_rows_4_impl(
+        args_t args,
+        device const char * src0,
+        device const char * src1,
+        device       char * dst,
+        threadgroup  char * shmem,
+        uint3  tgpig,
+        ushort tiisg,
+        ushort sgitg) {
+    const short NSG = FC_mul_mv_nsg;
+    constexpr short NW = N_SIMDWIDTH;
+    constexpr short NB = 32;
+    constexpr short NF = 16;
+    constexpr short NF4 = NF/4;
+
+    const int nb = args.ne00/NB;
+    const int r0 = tgpig.x*NR0;
+    const int im = tgpig.z;
+    const uint i12 = im%args.ne12;
+    const uint i13 = im/args.ne12;
+
+    device const half * ax[NR0];
+    device const half4 * ax4[NR0];
+    FOR_UNROLL (short row = 0; row < NR0; ++row) {
+        const uint64_t offset0 = (r0 + row)*args.nb01 +
+            (i12/args.r2)*args.nb02 + (i13/args.r3)*args.nb03;
+        ax[row] = (device const half *)(src0 + offset0);
+        ax4[row] = (device const half4 *)(src0 + offset0);
+    }
+
+    float sumf[NR1][NR0] = {{0.f}};
+    const short ix = tiisg/(NW/NF);
+    const short il = tiisg%(NW/NF);
+    const int ib0 = sgitg*NF + ix;
+
+    device const float4 * yb4[NR1];
+    FOR_UNROLL (short token = 0; token < NR1; ++token) {
+        const uint64_t offset1 = token*args.nb11 +
+            i12*args.nb12 + i13*args.nb13;
+        yb4[token] = (device const float4 *)(src1 + offset1) +
+            (ib0*NB + il*NF)/4;
+    }
+
+    for (int ib = ib0; ib < nb; ib += NSG*NF) {
+        float sumq[NR1][NR0] = {{0.f}};
+        FOR_UNROLL (short i = 0; i < NF4; ++i) {
+            float4 weight[NR0];
+            FOR_UNROLL (short row = 0; row < NR0; ++row) {
+                weight[row] = float4(ax4[row][(ib*NB + il*NF)/4 + i]);
+            }
+            FOR_UNROLL (short token = 0; token < NR1; ++token) {
+                const float4 activation = yb4[token][i];
+                FOR_UNROLL (short row = 0; row < NR0; ++row) {
+                    sumq[token][row] += dot(weight[row], activation);
+                }
+            }
+        }
+        FOR_UNROLL (short token = 0; token < NR1; ++token) {
+            FOR_UNROLL (short row = 0; row < NR0; ++row) {
+                sumf[token][row] += sumq[token][row];
+            }
+            yb4[token] += NSG*NF*NW/4;
+        }
+    }
+
+    for (int i = nb*NB + sgitg*NW + tiisg; i < args.ne00; i += NW*NSG) {
+        FOR_UNROLL (short token = 0; token < NR1; ++token) {
+            const uint64_t offset1 = token*args.nb11 +
+                i12*args.nb12 + i13*args.nb13;
+            device const float * y = (device const float *)(src1 + offset1);
+            FOR_UNROLL (short row = 0; row < NR0; ++row) {
+                sumf[token][row] += ax[row][i] * y[i];
+            }
+        }
+    }
+
+    FOR_UNROLL (short token = 0; token < NR1; ++token) {
+        device float * dst_f32 = (device float *)dst +
+            (uint64_t)im*args.ne0*args.ne1 + (uint64_t)token*args.ne0;
+        helper_mv_reduce_and_write<NR0>(dst_f32, sumf[token], r0,
+                                        args.ne01, tiisg, sgitg, shmem);
+    }
+}
+
+template<short NR1, typename args_t>
+void kernel_mul_mv_f16_f32_exact_rows_4_disp(
+        args_t args,
+        device const char * src0,
+        device const char * src1,
+        device       char * dst,
+        threadgroup  char * shmem,
+        uint3  tgpig,
+        ushort tiisg,
+        ushort sgitg) {
+    switch (args.nr0) {
+        case 2: kernel_mul_mv_f16_f32_exact_rows_4_impl<NR1, 2>(
+            args, src0, src1, dst, shmem, tgpig, tiisg, sgitg); break;
+        case 4: kernel_mul_mv_f16_f32_exact_rows_4_impl<NR1, 4>(
+            args, src0, src1, dst, shmem, tgpig, tiisg, sgitg); break;
+    }
+}
+
+#define DS4_F16_EXACT_ROWS_KERNEL(NR1) \
+[[host_name("kernel_mul_mv_f16_f32_exact_rows" #NR1)]] \
+kernel void kernel_mul_mv_f16_f32_exact_rows##NR1( \
+        constant ds4_metal_args_mul_mv & args, \
+        device const char * src0, device const char * src1, \
+        device char * dst, threadgroup char * shmem [[threadgroup(0)]], \
+        uint3 tgpig [[threadgroup_position_in_grid]], \
+        ushort tiisg [[thread_index_in_simdgroup]], \
+        ushort sgitg [[simdgroup_index_in_threadgroup]]) { \
+    kernel_mul_mv_f16_f32_exact_rows_4_disp<NR1>( \
+        args, src0, src1, dst, shmem, tgpig, tiisg, sgitg); \
+}
+
+DS4_F16_EXACT_ROWS_KERNEL(2)
+DS4_F16_EXACT_ROWS_KERNEL(3)
+DS4_F16_EXACT_ROWS_KERNEL(4)
+
+#undef DS4_F16_EXACT_ROWS_KERNEL
+
 // DS4 compressor projections always compute two same-shaped F16 matvecs from
 // the same normalized activation: one for projected KV and one for pooling
 // scores.  This paired variant keeps the exact dense F16 row-reduction shape
