diff --git a/ds4_gpu.h b/ds4_gpu.h
--- a/ds4_gpu.h
+++ b/ds4_gpu.h
@@ -32,6 +32,7 @@ typedef struct {
     uint64_t tiny_batch_raw_store_calls;
     uint64_t tiny_batch_exact_attn_calls;
     uint64_t tiny_batch_exact_q8_calls;
+    uint64_t tiny_batch_exact_q4_calls;
     uint64_t q8_projection_pair_calls;
     uint64_t exact_q8_pipeline_warmups;
     uint64_t fast_argmax_calls;
diff --git a/ds4_metal.m b/ds4_metal.m
--- a/ds4_metal.m
+++ b/ds4_metal.m
@@ -353,5 +353,6 @@ static uint64_t g_tiny_batch_raw_store_calls;
 static uint64_t g_tiny_batch_exact_attn_calls;
 static uint64_t g_tiny_batch_exact_q8_calls;
+static uint64_t g_tiny_batch_exact_q4_calls;
 static uint64_t g_q8_projection_pair_calls;
 static uint64_t g_exact_q8_pipeline_warmups;
 static uint64_t g_fast_argmax_calls;
@@ -377,6 +378,7 @@ void ds4_gpu_get_kernel_stats(ds4_gpu_kernel_stats *out) {
     out->tiny_batch_raw_store_calls = g_tiny_batch_raw_store_calls;
     out->tiny_batch_exact_attn_calls = g_tiny_batch_exact_attn_calls;
     out->tiny_batch_exact_q8_calls = g_tiny_batch_exact_q8_calls;
+    out->tiny_batch_exact_q4_calls = g_tiny_batch_exact_q4_calls;
     out->q8_projection_pair_calls = g_q8_projection_pair_calls;
     out->exact_q8_pipeline_warmups = g_exact_q8_pipeline_warmups;
     out->fast_argmax_calls = g_fast_argmax_calls;
@@ -6373,6 +6375,20 @@ int ds4_gpu_init(void) {
             }
             g_exact_q8_pipeline_warmups++;
         }
+        static const char *exact_q4_pipeline_names[] = {
+            "kernel_mul_mv_q4_K_f32_exact_rows2",
+            "kernel_mul_mv_q4_K_f32_exact_rows3",
+            "kernel_mul_mv_q4_K_f32_exact_rows4",
+        };
+        for (size_t i = 0;
+             i < sizeof(exact_q4_pipeline_names) / sizeof(exact_q4_pipeline_names[0]);
+             i++) {
+            if (!ds4_gpu_get_mul_mv_pipeline(exact_q4_pipeline_names[i], 2)) {
+                g_queue = nil;
+                g_device = nil;
+                return 0;
+            }
+        }
         static const char *attn_hc_exact_pipeline_names[] = {
             "kernel_dsv4_q8_hc_expand4_q8_0_rows2",
             "kernel_dsv4_q8_hc_expand4_q8_0_rows3",
@@ -21235,6 +21251,17 @@ int ds4_gpu_matmul_q4_k_tensor(
             &inner_offset);
         if (!wbuf || !g_moe_mul_mv_id_q4_k_pipeline) return 0;
 
+        const bool exact_rows = g_tiny_decode_order && n_tok >= 2 && n_tok <= 4 &&
+            getenv("DS4_METAL_DISABLE_TINY_EXACT_Q4_ROWS") == NULL;
+        id<MTLComputePipelineState> pipeline = g_moe_mul_mv_id_q4_k_pipeline;
+        if (exact_rows) {
+            const char *name = n_tok == 2 ? "kernel_mul_mv_q4_K_f32_exact_rows2" :
+                (n_tok == 3 ? "kernel_mul_mv_q4_K_f32_exact_rows3" :
+                              "kernel_mul_mv_q4_K_f32_exact_rows4");
+            pipeline = ds4_gpu_get_mul_mv_pipeline(name, 2);
+            if (!pipeline) return 0;
+        }
+
         int32_t ids_stack[256] = {0};
         int32_t *ids = ids_stack;
         if (n_tok > (uint64_t)(sizeof(ids_stack) / sizeof(ids_stack[0]))) {
@@ -21262,20 +21289,24 @@ int ds4_gpu_matmul_q4_k_tensor(
             return 0;
         }
         id<MTLComputeCommandEncoder> enc = ds4_gpu_compute_encoder(cb);
-        [enc setComputePipelineState:g_moe_mul_mv_id_q4_k_pipeline];
+        [enc setComputePipelineState:pipeline];
         [enc setBytes:&args length:sizeof(args) atIndex:0];
         [enc setBuffer:wbuf offset:(NSUInteger)inner_offset atIndex:1];
         [enc setBuffer:xbuf offset:ds4_gpu_tensor_offset(x) atIndex:2];
         [enc setBuffer:outbuf offset:ds4_gpu_tensor_offset(out) atIndex:3];
-        if (idsbuf) {
-            [enc setBuffer:idsbuf offset:0 atIndex:4];
-        } else {
-            [enc setBytes:ids length:ids_bytes atIndex:4];
+        if (!exact_rows) {
+            if (idsbuf) {
+                [enc setBuffer:idsbuf offset:0 atIndex:4];
+            } else {
+                [enc setBytes:ids length:ids_bytes atIndex:4];
+            }
         }
         [enc dispatchThreadgroups:MTLSizeMake(
-                 ((NSUInteger)out_dim + 3u) / 4u, 1, (NSUInteger)n_tok)
+                 ((NSUInteger)out_dim + 3u) / 4u, 1,
+                 exact_rows ? 1u : (NSUInteger)n_tok)
              threadsPerThreadgroup:MTLSizeMake(32, 2, 1)];
         ds4_gpu_end_compute_encoder(cb, enc);
+        if (exact_rows) g_tiny_batch_exact_q4_calls++;
         if (ids != ids_stack) free(ids);
         if (!ds4_gpu_finish_command_buffer(cb, owned, "dense Q4_K matvec")) {
             return 0;
diff --git a/metal/moe.metal b/metal/moe.metal
--- a/metal/moe.metal
+++ b/metal/moe.metal
@@ -594,6 +594,118 @@ void kernel_mul_mv_q4_K_f32_impl(
     (void)shmem;
 }
 
+template<short NR1>
+void kernel_mul_mv_q4_K_f32_exact_rows_impl(
+        constant ds4_metal_args_mul_mv_id & args,
+        device const char * src0,
+        device const char * src1,
+        device       char * dst,
+        threadgroup  char * shmem,
+        uint3  tgpig,
+        ushort tiisg,
+        ushort sgitg) {
+    const short NSG = FC_mul_mv_nsg;
+    constexpr short NR0 = N_R0_Q4_K;
+    constexpr uint16_t kmask1 = 0x3f3f;
+    constexpr uint16_t kmask2 = 0x0f0f;
+    constexpr uint16_t kmask3 = 0xc0c0;
+
+    const short ix = tiisg / 8;
+    const short it = tiisg % 8;
+    const short iq = it / 4;
+    const short ir = it % 4;
+    const int nb = args.ne00 / QK_K;
+    const int first_row = (tgpig.x * NSG + sgitg) * NR0;
+    device const block_q4_K * x =
+        (device const block_q4_K *)(src0 + (uint64_t)first_row * args.nb01);
+    float sumf[NR1][NR0] = {{0.f}};
+
+    for (int ib = ix; ib < nb; ib += 4) {
+        FOR_UNROLL (short token = 0; token < NR1; ++token) {
+            device const float * y =
+                (device const float *)(src1 + (uint64_t)token * args.nb12);
+            device const float * y4 = y + ib * QK_K + 64 * iq + 8 * ir;
+            float yl[16];
+            float yh[16];
+            float4 sumy = {0.f, 0.f, 0.f, 0.f};
+            FOR_UNROLL (short i = 0; i < 8; ++i) {
+                yl[i + 0] = y4[i +   0]; sumy[0] += yl[i + 0];
+                yl[i + 8] = y4[i +  32]; sumy[1] += yl[i + 8];
+                yh[i + 0] = y4[i + 128]; sumy[2] += yh[i + 0];
+                yh[i + 8] = y4[i + 160]; sumy[3] += yh[i + 8];
+            }
+
+            device const uint16_t * sc =
+                (device const uint16_t *)x[ib].scales + iq;
+            device const uint16_t * q1 =
+                (device const uint16_t *)x[ib].qs + 16 * iq + 4 * ir;
+            device const half * dh = &x[ib].d;
+            uint16_t sc16[4];
+            thread const uint8_t * sc8 = (thread const uint8_t *)sc16;
+
+            FOR_UNROLL (short row = 0; row < NR0; ++row) {
+                sc16[0] = sc[0] & kmask1;
+                sc16[1] = sc[2] & kmask1;
+                sc16[2] = ((sc[4] >> 0) & kmask2) | ((sc[0] & kmask3) >> 2);
+                sc16[3] = ((sc[4] >> 4) & kmask2) | ((sc[2] & kmask3) >> 2);
+                device const uint16_t * q2 = q1 + 32;
+                float4 acc1 = {0.f, 0.f, 0.f, 0.f};
+                float4 acc2 = {0.f, 0.f, 0.f, 0.f};
+
+                FOR_UNROLL (short i = 0; i < 4; ++i) {
+                    acc1[0] += yl[2 * i + 0] * (q1[i] & 0x000F);
+                    acc1[1] += yl[2 * i + 1] * (q1[i] & 0x0F00);
+                    acc1[2] += yl[2 * i + 8] * (q1[i] & 0x00F0);
+                    acc1[3] += yl[2 * i + 9] * (q1[i] & 0xF000);
+                    acc2[0] += yh[2 * i + 0] * (q2[i] & 0x000F);
+                    acc2[1] += yh[2 * i + 1] * (q2[i] & 0x0F00);
+                    acc2[2] += yh[2 * i + 8] * (q2[i] & 0x00F0);
+                    acc2[3] += yh[2 * i + 9] * (q2[i] & 0xF000);
+                }
+
+                sumf[token][row] +=
+                    dh[0] * ((acc1[0] + 1.f / 256.f * acc1[1]) * sc8[0] +
+                             (acc1[2] + 1.f / 256.f * acc1[3]) * sc8[1] * 1.f / 16.f +
+                             (acc2[0] + 1.f / 256.f * acc2[1]) * sc8[4] +
+                             (acc2[2] + 1.f / 256.f * acc2[3]) * sc8[5] * 1.f / 16.f) -
+                    dh[1] * (sumy[0] * sc8[2] + sumy[1] * sc8[3] +
+                             sumy[2] * sc8[6] + sumy[3] * sc8[7]);
+
+                q1 += args.nb01 / 2;
+                sc += args.nb01 / 2;
+                dh += args.nb01 / 2;
+            }
+        }
+    }
+
+    FOR_UNROLL (short token = 0; token < NR1; ++token) {
+        device float * dst_f32 =
+            (device float *)dst + (uint64_t)token * args.ne0;
+        FOR_UNROLL (short row = 0; row < NR0 && first_row + row < args.ne0; ++row) {
+            const float sum_all = simd_sum(sumf[token][row]);
+            if (tiisg == 0) dst_f32[first_row + row] = sum_all;
+        }
+    }
+    (void)shmem;
+}
+
+#define template_q4_exact_rows(NR1) \
+    [[host_name("kernel_mul_mv_q4_K_f32_exact_rows" #NR1)]] \
+    kernel void kernel_mul_mv_q4_K_f32_exact_rows##NR1( \
+            constant ds4_metal_args_mul_mv_id & args, \
+            device const char * src0, device const char * src1, \
+            device char * dst, threadgroup char * shmem [[threadgroup(0)]], \
+            uint3 tgpig [[threadgroup_position_in_grid]], \
+            ushort tiisg [[thread_index_in_simdgroup]], \
+            ushort sgitg [[simdgroup_index_in_threadgroup]]) { \
+        kernel_mul_mv_q4_K_f32_exact_rows_impl<NR1>( \
+            args, src0, src1, dst, shmem, tgpig, tiisg, sgitg); \
+    }
+
+template_q4_exact_rows(2)
+template_q4_exact_rows(3)
+template_q4_exact_rows(4)
+
 template<int nr0, typename args_t>
 void kernel_mul_mv_iq2_xxs_f32_impl(
         args_t args,
