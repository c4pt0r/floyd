diff --git a/ds4.c b/ds4.c
index a55d4c9..2ea142e 100644
--- a/ds4.c
+++ b/ds4.c
@@ -3723,15 +3723,18 @@ static void dspark_block_validate_layout(const ds4_layer_weights *l) {
     tensor_expect_layout(l->hc_attn_scale,  DS4_TENSOR_F32, 1, 3, 0, 0);
     tensor_expect_layout(l->hc_attn_base,   DS4_TENSOR_F32, 1, hc_mix_dim, 0, 0);
     tensor_expect_layout(l->attn_norm,      DS4_TENSOR_F32, 1, DS4_N_EMBD, 0, 0);
-    tensor_expect_layout(l->attn_q_a,       DS4_TENSOR_Q8_0, 2, DS4_N_EMBD, DS4_N_LORA_Q, 0);
+    tensor_expect_f16_or_q8_0_layout(l->attn_q_a, 2, DS4_N_EMBD, DS4_N_LORA_Q, 0);
     tensor_expect_layout(l->attn_q_a_norm,  DS4_TENSOR_F32, 1, DS4_N_LORA_Q, 0, 0);
-    tensor_expect_layout(l->attn_q_b,       DS4_TENSOR_Q8_0, 2, DS4_N_LORA_Q, q_dim, 0);
-    tensor_expect_layout(l->attn_kv,        DS4_TENSOR_Q8_0, 2, DS4_N_EMBD, DS4_N_HEAD_DIM, 0);
+    tensor_expect_f16_or_q8_0_layout(l->attn_q_b, 2, DS4_N_LORA_Q, q_dim, 0);
+    tensor_expect_f16_or_q8_0_layout(l->attn_kv, 2, DS4_N_EMBD, DS4_N_HEAD_DIM, 0);
     tensor_expect_layout(l->attn_kv_a_norm, DS4_TENSOR_F32, 1, DS4_N_HEAD_DIM, 0, 0);
     tensor_expect_layout(l->attn_sinks,     DS4_TENSOR_F32, 1, DS4_N_HEAD, 0, 0);
-    tensor_expect_layout(l->attn_output_a,  DS4_TENSOR_Q8_0, 2,
-                         DS4_N_HEAD_DIM * (DS4_N_HEAD / DS4_N_OUT_GROUP), out_low_dim, 0);
-    tensor_expect_layout(l->attn_output_b,  DS4_TENSOR_Q8_0, 2, out_low_dim, DS4_N_EMBD, 0);
+    tensor_expect_f16_or_q8_0_layout(l->attn_output_a, 2,
+                                    DS4_N_HEAD_DIM * (DS4_N_HEAD / DS4_N_OUT_GROUP),
+                                    out_low_dim, 0);
+    tensor_expect_f16_or_q8_0_layout(l->attn_output_b, 2, out_low_dim, DS4_N_EMBD, 0);
+    if (l->attn_output_a->type != l->attn_output_b->type)
+        ds4_die("DSpark attention output projections use different tensor types");
     tensor_expect_plain_layout(l->hc_ffn_fn, 2, hc_dim, hc_mix_dim, 0);
     tensor_expect_layout(l->hc_ffn_scale,   DS4_TENSOR_F32, 1, 3, 0, 0);
     tensor_expect_layout(l->hc_ffn_base,    DS4_TENSOR_F32, 1, hc_mix_dim, 0, 0);
@@ -3759,7 +3762,7 @@ static void dspark_weights_validate_layout(const ds4_dspark_weights *w) {
     tensor_expect_plain_layout(w->hc_head_fn, 2, hc_dim, DS4_N_HC, 0);
     tensor_expect_layout(w->hc_head_scale, DS4_TENSOR_F32, 1, 1, 0, 0);
     tensor_expect_layout(w->norm, DS4_TENSOR_F32, 1, DS4_N_EMBD, 0, 0);
-    tensor_expect_layout(w->markov_w1, DS4_TENSOR_Q8_0, 2, 256, DS4_N_VOCAB, 0);
+    tensor_expect_f16_or_q8_0_layout(w->markov_w1, 2, 256, DS4_N_VOCAB, 0);
     tensor_expect_layout(w->markov_w2, DS4_TENSOR_Q8_0, 2, 256, DS4_N_VOCAB, 0);
     tensor_expect_layout(w->confidence_proj, DS4_TENSOR_F32, 2,
                          DS4_N_EMBD + 256u, 1, 0);
@@ -10509,6 +10512,7 @@ typedef struct {
     uint32_t spec_prefix1_n_comp[DS4_MAX_LAYER];
     uint32_t spec_prefix1_n_index_comp[DS4_MAX_LAYER];
     bool spec_capture_prefix1;
+    bool spec_verify_decode_order;
     uint32_t raw_cap;
     /* Maximum compressed-row capacity across layers.  Shared work buffers use
      * this worst-case size because ratio-4 indexer layers can still reach it. */
@@ -10571,6 +10575,13 @@ typedef struct {
     ds4_gpu_tensor *mtp_next_hc;
     ds4_gpu_tensor *mtp_raw_cache;
     uint32_t mtp_n_raw;
+    ds4_gpu_tensor *dspark_main_hidden;
+    ds4_gpu_tensor *dspark_main_x;
+    ds4_gpu_tensor *dspark_raw_cache[3];
+    const ds4_model *dspark_model;
+    const ds4_dspark_weights *dspark_weights;
+    uint32_t dspark_position;
+    bool dspark_enabled;
     uint32_t prefill_cap;
     uint32_t raw_window;

@@ -10629,6 +10640,8 @@ typedef struct {
     bool batch_routed_mid_is_f16;
     ds4_gpu_tensor *batch_ffn_out;
     bool materialize_ffn_out;
+    bool skip_raw_kv_quant;
+    bool dspark_full_block_attention;
     ds4_gpu_tensor *directional_steering_dirs;
     float directional_steering_attn_scale;
     float directional_steering_ffn_scale;
@@ -10726,6 +10739,10 @@ static void metal_graph_free(ds4_gpu_graph *g) {
     ds4_gpu_tensor_free(g->batch_cur_hc);
     ds4_gpu_tensor_free(g->prefill_tokens);
     ds4_gpu_tensor_free(g->logits);
+    for (uint32_t stage = 0; stage < 3; stage++)
+        ds4_gpu_tensor_free(g->dspark_raw_cache[stage]);
+    ds4_gpu_tensor_free(g->dspark_main_x);
+    ds4_gpu_tensor_free(g->dspark_main_hidden);
     ds4_gpu_tensor_free(g->mtp_raw_cache);
     ds4_gpu_tensor_free(g->mtp_next_hc);
     ds4_gpu_tensor_free(g->mtp_state_hc);
@@ -10967,6 +10984,8 @@ static ds4_gpu_tensor *metal_graph_alloc_kv_cache_tensor(bool managed, uint64_t
  * the command batch, so it is intentionally isolated here.
  */

+static bool g_metal_graph_dump_verifier = false;
+
 static bool metal_graph_debug_wants(const char *name, uint32_t il, uint32_t pos) {
     const char *prefix = getenv("DS4_METAL_GRAPH_DUMP_PREFIX");
     if (!prefix || !prefix[0]) return false;
@@ -11001,7 +11020,8 @@ static void metal_graph_debug_dump_tensor(
     float *buf = xmalloc((size_t)n_f32 * sizeof(buf[0]));
     if (ds4_gpu_tensor_read(t, 0, buf, n_f32 * sizeof(buf[0])) != 0) {
         char path[1024];
-        snprintf(path, sizeof(path), "%s_%s-%u_pos%u.bin", prefix, name, il, pos);
+        snprintf(path, sizeof(path), "%s_%s%s-%u_pos%u.bin", prefix,
+                 g_metal_graph_dump_verifier ? "verify_" : "", name, il, pos);
         if (write_f32_binary_file(path, buf, n_f32)) {
             fprintf(stderr, "ds4: dumped %s layer %u pos %u to %s\n", name, il, pos, path);
         }
@@ -11032,7 +11052,8 @@ static void metal_graph_debug_dump_f16_tensor(
     if (ds4_gpu_tensor_read(t, 0, hbuf, n_f16 * sizeof(hbuf[0])) != 0) {
         for (uint64_t i = 0; i < n_f16; i++) fbuf[i] = f16_to_f32(hbuf[i]);
         char path[1024];
-        snprintf(path, sizeof(path), "%s_%s-%u_pos%u.bin", prefix, name, il, pos);
+        snprintf(path, sizeof(path), "%s_%s%s-%u_pos%u.bin", prefix,
+                 g_metal_graph_dump_verifier ? "verify_" : "", name, il, pos);
         if (write_f32_binary_file(path, fbuf, n_f16)) {
             fprintf(stderr, "ds4: dumped %s layer %u pos %u to %s\n", name, il, pos, path);
         }
@@ -11062,7 +11083,8 @@ static void metal_graph_debug_dump_i32_tensor(
     int32_t *buf = xmalloc((size_t)n_i32 * sizeof(buf[0]));
     if (ds4_gpu_tensor_read(t, 0, buf, n_i32 * sizeof(buf[0])) != 0) {
         char path[1024];
-        snprintf(path, sizeof(path), "%s_%s-%u_pos%u.i32", prefix, name, il, pos);
+        snprintf(path, sizeof(path), "%s_%s%s-%u_pos%u.i32", prefix,
+                 g_metal_graph_dump_verifier ? "verify_" : "", name, il, pos);
         FILE *fp = fopen(path, "wb");
         if (fp) {
             if (fwrite(buf, sizeof(buf[0]), (size_t)n_i32, fp) == (size_t)n_i32) {
@@ -11445,6 +11467,29 @@ static bool metal_graph_alloc(
     return metal_graph_alloc_raw_cap(g, weights, layer, DS4_N_SWA, DS4_N_SWA, 1, false);
 }

+static bool metal_graph_alloc_dspark_state(
+        ds4_gpu_graph *g,
+        const ds4_model *model,
+        const ds4_dspark_weights *weights) {
+    if (!g || !model || !weights || g->prefill_cap == 0) return false;
+    g->dspark_main_hidden = ds4_gpu_tensor_alloc(
+            (uint64_t)g->prefill_cap * 3u * DS4_N_EMBD * sizeof(float));
+    g->dspark_main_x = ds4_gpu_tensor_alloc(
+            (uint64_t)g->prefill_cap * DS4_N_EMBD * sizeof(float));
+    bool ok = g->dspark_main_hidden && g->dspark_main_x;
+    for (uint32_t stage = 0; ok && stage < 3; stage++) {
+        g->dspark_raw_cache[stage] = ds4_gpu_tensor_alloc(
+                (uint64_t)DS4_N_SWA * DS4_N_HEAD_DIM * sizeof(float));
+        ok = g->dspark_raw_cache[stage] != NULL;
+    }
+    if (!ok) return false;
+    g->dspark_model = model;
+    g->dspark_weights = weights;
+    g->dspark_position = 0;
+    g->dspark_enabled = true;
+    return true;
+}
+
 static bool metal_graph_install_model_spans(
         const ds4_model              *model,
         const ds4_model_map_span_vec *spans,
@@ -16409,15 +16454,8 @@ static bool metal_graph_matmul_q8_0_named_tensor(
     (void)module;
     (void)il;
     (void)pos0;
-    const bool ok = ds4_gpu_matmul_q8_0_tensor(out,
-                                                 model->map,
-                                                 model->size,
-                                                 w->abs_offset,
-                                                 in_dim,
-                                                 out_dim,
-                                                 x,
-                                                 n_tok) != 0;
-    return ok;
+    return metal_graph_matmul_plain_tensor(
+            out, model, w, in_dim, out_dim, x, n_tok);
 }

 static bool metal_graph_encode_output_head_mtp(
@@ -17091,6 +17129,146 @@ static uint32_t metal_graph_token_split_after_layers(void) {
     return split_after_layers;
 }

+static bool metal_graph_capture_dspark_main_hidden(
+        ds4_gpu_graph *g,
+        uint32_t il,
+        const ds4_gpu_tensor *hc,
+        uint32_t n_tokens) {
+    if (!g->dspark_enabled || il < DS4_N_LAYER - 3u || il >= DS4_N_LAYER)
+        return true;
+    return ds4_gpu_hc_mean_stage_tensor(g->dspark_main_hidden,
+                                         hc,
+                                         DS4_N_EMBD,
+                                         DS4_N_HC,
+                                         3,
+                                         il - (DS4_N_LAYER - 3u),
+                                         n_tokens) != 0;
+}
+
+static bool metal_graph_dspark_copy_ring(
+        ds4_gpu_tensor *cache,
+        const ds4_gpu_tensor *rows,
+        uint32_t start,
+        uint32_t n_tokens) {
+    if (!cache || !rows || n_tokens == 0 || n_tokens > DS4_N_SWA) return false;
+    const uint64_t row_bytes = (uint64_t)DS4_N_HEAD_DIM * sizeof(float);
+    const uint32_t row = start % DS4_N_SWA;
+    uint32_t first = DS4_N_SWA - row;
+    if (first > n_tokens) first = n_tokens;
+    bool ok = ds4_gpu_tensor_copy(cache,
+                                  (uint64_t)row * row_bytes,
+                                  rows,
+                                  0,
+                                  (uint64_t)first * row_bytes) != 0;
+    if (ok && first < n_tokens) {
+        ok = ds4_gpu_tensor_copy(cache,
+                                 0,
+                                 rows,
+                                 (uint64_t)first * row_bytes,
+                                 (uint64_t)(n_tokens - first) * row_bytes) != 0;
+    }
+    return ok;
+}
+
+static bool metal_graph_encode_dspark_append_main(
+        ds4_gpu_graph *g,
+        uint32_t start,
+        uint32_t n_tokens) {
+    if (!g->dspark_enabled) return true;
+    if (!g->dspark_model || !g->dspark_weights || n_tokens == 0 ||
+        n_tokens > g->prefill_cap) return false;
+
+    uint32_t skip = 0;
+    if (n_tokens > DS4_N_SWA) {
+        skip = n_tokens - DS4_N_SWA;
+        start += skip;
+        n_tokens = DS4_N_SWA;
+    }
+    const uint64_t main_width = 3ull * DS4_N_EMBD;
+    ds4_gpu_tensor *main_hidden = ds4_gpu_tensor_view(
+            g->dspark_main_hidden,
+            (uint64_t)skip * main_width * sizeof(float),
+            (uint64_t)n_tokens * main_width * sizeof(float));
+    ds4_gpu_tensor *main_x = ds4_gpu_tensor_view(
+            g->dspark_main_x,
+            0,
+            (uint64_t)n_tokens * DS4_N_EMBD * sizeof(float));
+    bool ok = main_hidden && main_x;
+    if (ok) ok = metal_graph_matmul_plain_tensor(
+            g->batch_ffn_cur,
+            g->dspark_model,
+            g->dspark_weights->main_proj,
+            main_width,
+            DS4_N_EMBD,
+            main_hidden,
+            n_tokens);
+    if (ok) ok = ds4_gpu_rms_norm_weight_rows_tensor(
+            main_x,
+            g->batch_ffn_cur,
+            g->dspark_model->map,
+            g->dspark_model->size,
+            g->dspark_weights->main_norm->abs_offset,
+            DS4_N_EMBD,
+            n_tokens,
+            DS4_RMS_EPS) != 0;
+    if (ok) {
+        metal_graph_debug_dump_tensor("dspark_main_x",
+                                      main_x,
+                                      (uint64_t)n_tokens * DS4_N_EMBD,
+                                      0,
+                                      start);
+    }
+
+    const float freq_base = layer_rope_freq_base(0);
+    const float freq_scale = layer_rope_freq_scale(0);
+    for (uint32_t stage = 0; ok && stage < 3; stage++) {
+        const ds4_layer_weights *layer = &g->dspark_weights->stage[stage];
+        ok = metal_graph_matmul_plain_tensor(g->batch_kv_raw,
+                                             g->dspark_model,
+                                             layer->attn_kv,
+                                             DS4_N_EMBD,
+                                             DS4_N_HEAD_DIM,
+                                             main_x,
+                                             n_tokens);
+        if (ok) ok = ds4_gpu_rms_norm_weight_rows_tensor(
+                g->batch_kv,
+                g->batch_kv_raw,
+                g->dspark_model->map,
+                g->dspark_model->size,
+                layer->attn_kv_a_norm->abs_offset,
+                DS4_N_HEAD_DIM,
+                n_tokens,
+                DS4_RMS_EPS) != 0;
+        if (ok) ok = ds4_gpu_rope_tail_tensor(g->batch_kv,
+                                               n_tokens,
+                                               DS4_N_HEAD_KV,
+                                               DS4_N_HEAD_DIM,
+                                               DS4_N_ROT,
+                                               start,
+                                               0,
+                                               false,
+                                               freq_base,
+                                               freq_scale,
+                                               0.0f,
+                                               1.0f,
+                                               DS4_ROPE_YARN_BETA_FAST,
+                                               DS4_ROPE_YARN_BETA_SLOW) != 0;
+        if (ok) {
+            metal_graph_debug_dump_tensor("dspark_main_kv",
+                                          g->batch_kv,
+                                          (uint64_t)n_tokens * DS4_N_HEAD_DIM,
+                                          stage,
+                                          start);
+        }
+        if (ok) ok = metal_graph_dspark_copy_ring(
+                g->dspark_raw_cache[stage], g->batch_kv, start, n_tokens);
+    }
+    if (ok) g->dspark_position = start + n_tokens;
+    ds4_gpu_tensor_free(main_x);
+    ds4_gpu_tensor_free(main_hidden);
+    return ok;
+}
+
 /* Encode a full single-token decode step on Metal.  This is the generation
  * hot path: update caches, run all layers, then produce logits. */
 static bool metal_graph_encode_token_raw_swa(
@@ -17140,11 +17318,15 @@ static bool metal_graph_encode_token_raw_swa(
         ds4_gpu_tensor *tmp = g->cur_hc;
         g->cur_hc = g->after_ffn_hc;
         g->after_ffn_hc = tmp;
+        if (ok) ok = metal_graph_capture_dspark_main_hidden(
+                g, il, g->cur_hc, 1);
         if (ok && allow_split_flush && split_after_layers != 0 && il + 1u == split_after_layers) {
             ok = ds4_gpu_flush_commands() != 0;
         }
     }

+    if (ok) ok = metal_graph_encode_dspark_append_main(g, pos, 1);
+
     if (ok && need_logits) {
         ok = metal_graph_encode_output_head(g, model, weights, weights->output->dim[1]);
     }
@@ -17505,6 +17687,8 @@ static bool metal_graph_encode_layer_attention_batch(
     uint32_t *comp_counts = compressed ? xcalloc(n_tokens, sizeof(comp_counts[0])) : NULL;
     uint32_t *index_counts = ratio == 4 ? xcalloc(n_tokens, sizeof(index_counts[0])) : NULL;
     const bool qkv_rms_fused = !metal_graph_use_reference_qkv_norm();
+    const bool tiny_decode_order = n_tokens <= 8 &&
+        g->spec_verify_decode_order;
     ds4_gpu_tensor *hc_mix_view = ds4_gpu_tensor_view(
             g->batch_hc_mix, 0, (uint64_t)n_tokens * mix_hc * sizeof(float));
     ds4_gpu_tensor *hc_split_view = ds4_gpu_tensor_view(
@@ -17516,7 +17700,8 @@ static bool metal_graph_encode_layer_attention_batch(
     bool ok = hc_mix_view && hc_split_view && attn_cur_view && after_attn_hc_view;
     const bool fuse_hc_norm = DS4_N_HC == 4 &&
                               !metal_graph_use_reference_hc_decode() &&
-                              metal_graph_enable_batch_hc_norm_fusion();
+                              (tiny_decode_order ||
+                               metal_graph_enable_batch_hc_norm_fusion());
     if (ok) ok = ds4_gpu_rms_norm_plain_rows_tensor(g->batch_flat_hc,
                                                       g->batch_cur_hc,
                                                       (uint32_t)hc_dim,
@@ -17661,7 +17846,7 @@ static bool metal_graph_encode_layer_attention_batch(
         metal_graph_debug_wants("Qraw", il, pos0) ||
         metal_graph_debug_wants("Qnorm", il, pos0);
     bool q_b_f16_out = false;
-    if (ok && !q_path_debug) {
+    if (ok && !q_path_debug && layer->attn_q_b->type == DS4_TENSOR_Q8_0) {
         q_b_f16_out = ds4_gpu_attn_q_b_f16_head_rms_rope_tail_tensor(g->batch_q,
                                                                      g->batch_q_half,
                                                                      model->map,
@@ -17786,10 +17971,11 @@ static bool metal_graph_encode_layer_attention_batch(
         metal_graph_debug_dump_tensor("KVrope", g->batch_kv,
                                       (uint64_t)n_tokens * DS4_N_HEAD_DIM, il, pos0);
     }
-    if (ok) ok = ds4_gpu_dsv4_fp8_kv_quantize_tensor(g->batch_kv,
-                                                       n_tokens,
-                                                       DS4_N_HEAD_DIM,
-                                                       DS4_N_ROT) != 0;
+    if (ok && !g->skip_raw_kv_quant)
+        ok = ds4_gpu_dsv4_fp8_kv_quantize_tensor(g->batch_kv,
+                                                  n_tokens,
+                                                  DS4_N_HEAD_DIM,
+                                                  DS4_N_ROT) != 0;
     if (ok) {
         metal_graph_debug_dump_tensor("KVcur", g->batch_kv,
                                       (uint64_t)n_tokens * DS4_N_HEAD_DIM, il, pos0);
@@ -17824,7 +18010,8 @@ static bool metal_graph_encode_layer_attention_batch(
                                                           DS4_N_HEAD,
                                                           DS4_N_HEAD_DIM) != 0;
         if (ok) batch_attention_done = true;
-    } else if (ok && !zero_prefix && ratio == 0 && n_tokens <= g->raw_cap) {
+    } else if (ok && !zero_prefix && ratio == 0 && n_tokens <= g->raw_cap &&
+               !tiny_decode_order) {
         /*
          * The ubatch path stores the whole batch in the SWA cache, then runs
          * one batched attention kernel with an absolute-position causal/window
@@ -17864,6 +18051,7 @@ static bool metal_graph_encode_layer_attention_batch(
                                                                    g->raw_cap,
                                                                    raw_start,
                                                                    g->raw_window,
+                                                                   g->dspark_full_block_attention ? 1u : 0u,
                                                                    DS4_N_HEAD,
                                                                    DS4_N_HEAD_DIM) != 0;
         }
@@ -18450,7 +18638,8 @@ static bool metal_graph_encode_layer_attention_batch(
         }
         if (ratio == 4) DS4_METAL_PROFILE_ATTN_STAGE("indexer_setup");

-        if (ok && !zero_prefix && n_tokens <= g->raw_cap) {
+        if (ok && !zero_prefix && n_tokens <= g->raw_cap &&
+            !tiny_decode_order) {
             const uint32_t n_raw = metal_graph_raw_span_for_batch(g, pos0, n_tokens);
             /* See the raw-only branch above: batched mixed attention also
              * consumes a logical raw window, linearized out of the ring. */
@@ -18845,8 +19034,13 @@ static bool metal_graph_encode_layer_attention_batch(
     const bool attn_out_debug =
         metal_graph_debug_wants("attn_low", il, pos0) ||
         metal_graph_debug_wants("attn_out", il, pos0);
+    const bool attn_weights_f16 =
+        layer->attn_output_a->type == DS4_TENSOR_F16 &&
+        layer->attn_output_b->type == DS4_TENSOR_F16;
+    bool attn_out_hc_done = false;
     bool attn_out_f16 = false;
-    if (ok &&
+    if (!attn_out_hc_done && ok &&
+        !attn_weights_f16 &&
         !attn_out_debug &&
         !metal_graph_directional_steering_attn_enabled(g)) {
         attn_out_f16 = ds4_gpu_attention_output_q8_batch_f16_tensor(g->batch_q_half,
@@ -18862,22 +19056,26 @@ static bool metal_graph_encode_layer_attention_batch(
                                                                     g->batch_heads,
                                                                     n_tokens) != 0;
     }
-    if (!attn_out_f16) {
+    if (!attn_out_hc_done && !attn_out_f16) {
         if (ok) {
-            ok = ds4_gpu_attention_output_q8_batch_tensor(g->batch_attn_out,
-                                                          g->batch_attn_low,
-                                                          g->batch_group_tmp,
-                                                          g->batch_low_tmp,
-                                                          model->map,
-                                                          model->size,
-                                                          layer->attn_output_a->abs_offset,
-                                                          layer->attn_output_b->abs_offset,
-                                                          group_dim,
-                                                          rank,
-                                                          n_groups,
-                                                          DS4_N_EMBD,
-                                                          g->batch_heads,
-                                                          n_tokens) != 0;
+            if (attn_weights_f16) {
+                ok = ds4_gpu_attention_output_f16_batch_tensor(
+                        g->batch_attn_out, g->batch_attn_low,
+                        model->map, model->size,
+                        layer->attn_output_a->abs_offset,
+                        layer->attn_output_b->abs_offset,
+                        group_dim, rank, n_groups, DS4_N_EMBD,
+                        g->batch_heads, n_tokens) != 0;
+            } else {
+                ok = ds4_gpu_attention_output_q8_batch_tensor(
+                        g->batch_attn_out, g->batch_attn_low,
+                        g->batch_group_tmp, g->batch_low_tmp,
+                        model->map, model->size,
+                        layer->attn_output_a->abs_offset,
+                        layer->attn_output_b->abs_offset,
+                        group_dim, rank, n_groups, DS4_N_EMBD,
+                        g->batch_heads, n_tokens) != 0;
+            }
         }
         if (ok) {
             metal_graph_debug_dump_tensor("attn_low", g->batch_attn_low,
@@ -18891,17 +19089,18 @@ static bool metal_graph_encode_layer_attention_batch(
         }
     }
     DS4_METAL_PROFILE_ATTN_STAGE("output_proj");
-    if (ok && !attn_out_f16 && metal_graph_directional_steering_attn_enabled(g)) {
+    if (ok && !attn_out_hc_done && !attn_out_f16 &&
+        metal_graph_directional_steering_attn_enabled(g)) {
         ok = metal_graph_apply_directional_steering_attn(g, g->batch_attn_out, il, n_tokens);
     }
-    if (ok && attn_out_f16) {
+    if (ok && !attn_out_hc_done && attn_out_f16) {
         ok = ds4_gpu_hc_expand_split_half_tensor(after_attn_hc_view,
                                                  g->batch_q_half,
                                                  g->batch_cur_hc,
                                                  hc_split_view,
                                                  DS4_N_EMBD,
                                                  DS4_N_HC) != 0;
-    } else if (ok) {
+    } else if (ok && !attn_out_hc_done) {
         ok = ds4_gpu_hc_expand_split_tensor(after_attn_hc_view,
                                             g->batch_attn_out,
                                             g->batch_cur_hc,
@@ -18947,6 +19146,8 @@ static bool metal_graph_encode_layer_ffn_batch(
     const uint64_t gate_expert_bytes = expert_mid_dim * gate_row_bytes;
     const uint64_t down_row_bytes = routed_expert_row_bytes(layer->ffn_down_exps);
     const uint64_t down_expert_bytes = routed_out_dim * down_row_bytes;
+    const bool tiny_decode_order = n_tokens <= 8 &&
+        g->spec_verify_decode_order;
     const bool layer_stage_profile = metal_graph_layer_stage_profile_enabled(il);
     double layer_stage_t0 = layer_stage_profile ? now_sec() : 0.0;
 #define DS4_METAL_PROFILE_FFN_STAGE(name) do { \
@@ -18966,7 +19167,8 @@ static bool metal_graph_encode_layer_ffn_batch(
     bool ok = hc_mix_view && hc_split_view && ffn_cur_view && next_hc_view;
     const bool fuse_hc_norm = DS4_N_HC == 4 &&
                               !metal_graph_use_reference_hc_decode() &&
-                              metal_graph_enable_batch_hc_norm_fusion();
+                              (tiny_decode_order ||
+                               metal_graph_enable_batch_hc_norm_fusion());
     if (ok) ok = ds4_gpu_rms_norm_plain_rows_tensor(g->batch_flat_hc,
                                                       g->batch_after_attn_hc,
                                                       (uint32_t)hc_dim,
@@ -19045,14 +19247,13 @@ static bool metal_graph_encode_layer_ffn_batch(
                                       (uint64_t)n_tokens * DS4_N_EMBD, il, pos0);
     }
     DS4_METAL_PROFILE_FFN_STAGE("norm");
-    if (ok) ok = ds4_gpu_matmul_f16_tensor(g->batch_router_logits,
-                                             model->map,
-                                             model->size,
-                                             layer->ffn_gate_inp->abs_offset,
-                                             DS4_N_EMBD,
-                                             DS4_N_EXPERT,
-                                             g->batch_ffn_norm,
-                                             n_tokens) != 0;
+    if (ok) ok = metal_graph_matmul_plain_tensor(g->batch_router_logits,
+                                                  model,
+                                                  layer->ffn_gate_inp,
+                                                  DS4_N_EMBD,
+                                                  DS4_N_EXPERT,
+                                                  g->batch_ffn_norm,
+                                                  n_tokens);

     if (ok) ok = ds4_gpu_router_select_batch_tensor(g->batch_router_selected,
                                                       g->batch_router_weights,
@@ -19155,9 +19356,11 @@ static bool metal_graph_encode_layer_ffn_batch(

     const bool keep_ffn_out = metal_graph_needs_ffn_out(g, il, pos0);
     bool shared_down_f16 = false;
+    bool shared_gate_up_fused = false;

 #define DS4_METAL_TRY_SHARED_DOWN_F16() do { \
-        if (ok && !keep_ffn_out && !metal_graph_debug_wants("ffn_shexp", il, pos0)) { \
+        if (ok && layer->ffn_down_shexp->type == DS4_TENSOR_Q8_0 && \
+            !keep_ffn_out && !metal_graph_debug_wants("ffn_shexp", il, pos0)) { \
             shared_down_f16 = ds4_gpu_matmul_q8_0_f16_out_tensor(g->batch_q_half, \
                                                                  model->map, \
                                                                  model->size, \
@@ -19170,7 +19373,28 @@ static bool metal_graph_encode_layer_ffn_batch(
     } while (0)

 #define DS4_METAL_ENCODE_PREFILL_SHARED_EXPERT() do { \
-        if (ok) ok = metal_graph_matmul_q8_0_named_tensor("shared_gate", \
+        shared_gate_up_fused = false; \
+        if (ok && n_tokens >= 2 && n_tokens <= 4 && \
+            layer->ffn_gate_shexp->type == DS4_TENSOR_Q8_0 && \
+            layer->ffn_up_shexp->type == DS4_TENSOR_Q8_0 && \
+            getenv("DS4_METAL_DISABLE_TINY_BATCH_SHARED_SWIGLU") == NULL && \
+            !metal_graph_debug_wants("ffn_shexp_gate", il, pos0) && \
+            !metal_graph_debug_wants("ffn_shexp_up", il, pos0)) { \
+            shared_gate_up_fused = ds4_gpu_shared_gate_up_swiglu_q8_0_batch_tensor( \
+                    g->batch_shared_gate, \
+                    g->batch_shared_up, \
+                    g->batch_shared_mid, \
+                    model->map, \
+                    model->size, \
+                    layer->ffn_gate_shexp->abs_offset, \
+                    layer->ffn_up_shexp->abs_offset, \
+                    DS4_N_EMBD, \
+                    shared_dim, \
+                    g->batch_ffn_norm, \
+                    n_tokens, \
+                    DS4_SWIGLU_CLAMP_EXP) != 0; \
+        } \
+        if (ok && !shared_gate_up_fused) ok = metal_graph_matmul_q8_0_named_tensor("shared_gate", \
                                                           il, \
                                                           pos0, \
                                                           g->batch_shared_gate, \
@@ -19180,7 +19404,7 @@ static bool metal_graph_encode_layer_ffn_batch(
                                                           shared_dim, \
                                                           g->batch_ffn_norm, \
                                                           n_tokens); \
-        if (ok) ok = metal_graph_matmul_q8_0_named_tensor("shared_up", \
+        if (ok && !shared_gate_up_fused) ok = metal_graph_matmul_q8_0_named_tensor("shared_up", \
                                                           il, \
                                                           pos0, \
                                                           g->batch_shared_up, \
@@ -19191,7 +19415,7 @@ static bool metal_graph_encode_layer_ffn_batch(
                                                           g->batch_ffn_norm, \
                                                           n_tokens); \
         DS4_METAL_PROFILE_FFN_STAGE("shared_gate_up"); \
-        if (ok) ok = ds4_gpu_swiglu_tensor(g->batch_shared_mid, \
+        if (ok && !shared_gate_up_fused) ok = ds4_gpu_swiglu_tensor(g->batch_shared_mid, \
                                              g->batch_shared_gate, \
                                              g->batch_shared_up, \
                                              (uint32_t)((uint64_t)n_tokens * shared_dim), \
@@ -19216,6 +19440,7 @@ static bool metal_graph_encode_layer_ffn_batch(
     } while (0)

     bool shared_done = false;
+    bool tiny_ffn_hc_done = false;
     if (ok && selected_readahead_shared) {
         if (ds4_gpu_end_commands() == 0) {
             ok = false;
@@ -19348,6 +19573,108 @@ static bool metal_graph_encode_layer_ffn_batch(
                                       (uint64_t)n_tokens * DS4_N_EMBD, il, pos0);
     }
     DS4_METAL_PROFILE_FFN_STAGE("routed_moe");
+    if (ok && tiny_decode_order &&
+        !metal_graph_directional_steering_ffn_enabled(g) &&
+        layer->ffn_gate_shexp->type == DS4_TENSOR_Q8_0 &&
+        layer->ffn_up_shexp->type == DS4_TENSOR_Q8_0 &&
+        layer->ffn_down_shexp->type == DS4_TENSOR_Q8_0) {
+#if defined(__APPLE__)
+        ok = ds4_gpu_shared_gate_up_swiglu_q8_0_decode_order_batch_tensor(
+                g->batch_shared_gate,
+                g->batch_shared_up,
+                g->batch_shared_mid,
+                model->map,
+                model->size,
+                layer->ffn_gate_shexp->abs_offset,
+                layer->ffn_up_shexp->abs_offset,
+                DS4_N_EMBD,
+                shared_dim,
+                g->batch_ffn_norm,
+                n_tokens,
+                DS4_SWIGLU_CLAMP_EXP) != 0;
+        if (ok) {
+            ok = ds4_gpu_shared_down_hc_expand_q8_0_decode_order_batch_tensor(
+                    g->batch_next_hc,
+                    g->batch_shared_out,
+                    model->map,
+                    model->size,
+                    layer->ffn_down_shexp->abs_offset,
+                    shared_dim,
+                    DS4_N_EMBD,
+                    g->batch_shared_mid,
+                    g->batch_routed_out,
+                    g->batch_after_attn_hc,
+                    g->batch_hc_split,
+                    DS4_N_EMBD,
+                    DS4_N_HC,
+                    n_tokens) != 0;
+        }
+#else
+        for (uint32_t t = 0; ok && t < n_tokens; t++) {
+            ds4_gpu_tensor *gate_view = metal_graph_tensor_row_view(
+                    g->batch_shared_gate, t, shared_dim);
+            ds4_gpu_tensor *up_view = metal_graph_tensor_row_view(
+                    g->batch_shared_up, t, shared_dim);
+            ds4_gpu_tensor *mid_view = metal_graph_tensor_row_view(
+                    g->batch_shared_mid, t, shared_dim);
+            ds4_gpu_tensor *shared_view = metal_graph_tensor_row_view(
+                    g->batch_shared_out, t, DS4_N_EMBD);
+            ds4_gpu_tensor *norm_view = metal_graph_tensor_row_view(
+                    g->batch_ffn_norm, t, DS4_N_EMBD);
+            ds4_gpu_tensor *routed_view = metal_graph_tensor_row_view(
+                    g->batch_routed_out, t, DS4_N_EMBD);
+            ds4_gpu_tensor *residual_view = metal_graph_tensor_row_view(
+                    g->batch_after_attn_hc, t, hc_dim);
+            ds4_gpu_tensor *split_view = metal_graph_tensor_row_view(
+                    g->batch_hc_split, t, mix_hc);
+            ds4_gpu_tensor *next_view = metal_graph_tensor_row_view(
+                    g->batch_next_hc, t, hc_dim);
+            ok = gate_view && up_view && mid_view && shared_view && norm_view &&
+                 routed_view && residual_view && split_view && next_view;
+            if (ok) {
+                ok = ds4_gpu_shared_gate_up_swiglu_q8_0_tensor(
+                        gate_view,
+                        up_view,
+                        mid_view,
+                        model->map,
+                        model->size,
+                        layer->ffn_gate_shexp->abs_offset,
+                        layer->ffn_up_shexp->abs_offset,
+                        DS4_N_EMBD,
+                        shared_dim,
+                        norm_view,
+                        DS4_SWIGLU_CLAMP_EXP) != 0;
+            }
+            if (ok) {
+                ok = ds4_gpu_shared_down_hc_expand_q8_0_tensor(
+                        next_view,
+                        shared_view,
+                        model->map,
+                        model->size,
+                        layer->ffn_down_shexp->abs_offset,
+                        shared_dim,
+                        DS4_N_EMBD,
+                        mid_view,
+                        routed_view,
+                        residual_view,
+                        split_view,
+                        DS4_N_EMBD,
+                        DS4_N_HC) != 0;
+            }
+            ds4_gpu_tensor_free(next_view);
+            ds4_gpu_tensor_free(split_view);
+            ds4_gpu_tensor_free(residual_view);
+            ds4_gpu_tensor_free(routed_view);
+            ds4_gpu_tensor_free(norm_view);
+            ds4_gpu_tensor_free(shared_view);
+            ds4_gpu_tensor_free(mid_view);
+            ds4_gpu_tensor_free(up_view);
+            ds4_gpu_tensor_free(gate_view);
+        }
+#endif
+        tiny_ffn_hc_done = ok;
+        shared_done = ok;
+    }
     if (!shared_done) {
         DS4_METAL_ENCODE_PREFILL_SHARED_EXPERT();
     }
@@ -19365,10 +19692,12 @@ static bool metal_graph_encode_layer_ffn_batch(
         metal_graph_debug_dump_tensor("ffn_out", g->batch_ffn_out,
                                       (uint64_t)n_tokens * DS4_N_EMBD, il, pos0);
     }
-    if (ok && metal_graph_directional_steering_ffn_enabled(g)) {
+    if (ok && !tiny_ffn_hc_done &&
+        metal_graph_directional_steering_ffn_enabled(g)) {
         ok = metal_graph_apply_directional_steering_ffn(g, g->batch_ffn_out, il, n_tokens);
     }
-    if (ok && metal_graph_directional_steering_ffn_enabled(g)) {
+    if (ok && !tiny_ffn_hc_done &&
+        metal_graph_directional_steering_ffn_enabled(g)) {
         ok = ds4_gpu_hc_expand_split_tensor(next_hc_view,
                                               g->batch_ffn_out,
                                               g->batch_after_attn_hc,
@@ -19376,7 +19705,7 @@ static bool metal_graph_encode_layer_ffn_batch(
                                               DS4_N_EMBD,
                                               DS4_N_HC) != 0;
     }
-    else if (ok && shared_down_f16) {
+    else if (ok && !tiny_ffn_hc_done && shared_down_f16) {
         ok = ds4_gpu_hc_expand_add_split_half_add_tensor(next_hc_view,
                                                          g->batch_routed_out,
                                                          g->batch_q_half,
@@ -19385,7 +19714,7 @@ static bool metal_graph_encode_layer_ffn_batch(
                                                          DS4_N_EMBD,
                                                          DS4_N_HC) != 0;
     }
-    else if (ok) {
+    else if (ok && !tiny_ffn_hc_done) {
         ok = ds4_gpu_hc_expand_add_split_tensor(next_hc_view,
                                                   g->batch_routed_out,
                                                   g->batch_shared_out,
@@ -19430,6 +19759,8 @@ static bool metal_graph_encode_layer_batch(
         g->batch_cur_hc = g->batch_next_hc;
         g->batch_next_hc = tmp;
     }
+    if (ok) ok = metal_graph_capture_dspark_main_hidden(
+            g, il, g->batch_cur_hc, n_tokens);
     return ok;
 }

@@ -20209,6 +20540,408 @@ static bool metal_graph_eval_mtp_draft(
                                               top_id);
 }

+static bool metal_graph_encode_dspark_head_batch(
+        ds4_gpu_graph *g,
+        const ds4_model *base_model,
+        const ds4_weights *base_weights,
+        const ds4_model *support_model,
+        const ds4_dspark_weights *dspark,
+        uint32_t n_tokens) {
+    const uint64_t hc_dim = (uint64_t)DS4_N_HC * DS4_N_EMBD;
+    ds4_gpu_tensor *output_pre = ds4_gpu_tensor_view(
+            g->batch_hc_mix, 0, (uint64_t)n_tokens * DS4_N_HC * sizeof(float));
+    ds4_gpu_tensor *output_weights = ds4_gpu_tensor_view(
+            g->batch_hc_split, 0, (uint64_t)n_tokens * DS4_N_HC * sizeof(float));
+    ds4_gpu_tensor *output_embd = ds4_gpu_tensor_view(
+            g->batch_ffn_cur, 0, (uint64_t)n_tokens * DS4_N_EMBD * sizeof(float));
+    ds4_gpu_tensor *output_norm = ds4_gpu_tensor_view(
+            g->batch_ffn_norm, 0, (uint64_t)n_tokens * DS4_N_EMBD * sizeof(float));
+    ds4_gpu_tensor *logits = ds4_gpu_tensor_view(
+            g->spec_logits, 0, (uint64_t)n_tokens * DS4_N_VOCAB * sizeof(float));
+    bool ok = output_pre && output_weights && output_embd && output_norm && logits;
+    if (ok) ok = ds4_gpu_rms_norm_plain_rows_tensor(
+            g->batch_flat_hc, g->batch_cur_hc, (uint32_t)hc_dim,
+            n_tokens, DS4_RMS_EPS) != 0;
+    if (ok) ok = ds4_gpu_matmul_f16_tensor(
+            output_pre, support_model->map, support_model->size,
+            dspark->hc_head_fn->abs_offset, hc_dim, DS4_N_HC,
+            g->batch_flat_hc, n_tokens) != 0;
+    if (ok) ok = ds4_gpu_output_hc_weights_tensor(
+            output_weights, output_pre, support_model->map, support_model->size,
+            dspark->hc_head_scale->abs_offset,
+            dspark->hc_head_base->abs_offset, DS4_N_HC, DS4_HC_EPS) != 0;
+    if (ok) ok = ds4_gpu_hc_weighted_sum_tensor(
+            output_embd, g->batch_cur_hc, output_weights,
+            DS4_N_EMBD, DS4_N_HC) != 0;
+    if (ok) ok = ds4_gpu_rms_norm_weight_rows_tensor(
+            output_norm, output_embd, support_model->map, support_model->size,
+            dspark->norm->abs_offset, DS4_N_EMBD, n_tokens, DS4_RMS_EPS) != 0;
+    if (ok) ok = ds4_gpu_matmul_q8_0_tensor(
+            logits, base_model->map, base_model->size,
+            base_weights->output->abs_offset, DS4_N_EMBD, DS4_N_VOCAB,
+            output_norm, n_tokens) != 0;
+    ds4_gpu_tensor_free(logits);
+    ds4_gpu_tensor_free(output_norm);
+    ds4_gpu_tensor_free(output_embd);
+    ds4_gpu_tensor_free(output_weights);
+    ds4_gpu_tensor_free(output_pre);
+    return ok;
+}
+
+static bool metal_graph_eval_dspark_proposals(
+        ds4_gpu_graph *g,
+        const ds4_model *base_model,
+        const ds4_weights *base_weights,
+        const ds4_model *support_model,
+        const ds4_dspark_weights *dspark,
+        int input_token,
+        int proposal_count,
+        int output_ids[6],
+        float output_confidence[5]) {
+    enum { DSPARK_MAX_STAGE_TOKENS = 5, DSPARK_NOISE = 128799 };
+    if (!g || !g->dspark_enabled || !support_model || !dspark ||
+        !output_ids || !output_confidence || proposal_count < 2 ||
+        proposal_count > 6 ||
+        g->dspark_position == 0) return false;
+    const int stage_tokens = proposal_count - 1;
+
+    int draft_ids[DSPARK_MAX_STAGE_TOKENS] = {
+        input_token, DSPARK_NOISE, DSPARK_NOISE, DSPARK_NOISE, DSPARK_NOISE,
+    };
+    token_vec draft = {
+        .v = draft_ids,
+        .len = stage_tokens,
+        .cap = DSPARK_MAX_STAGE_TOKENS,
+    };
+    bool ok = metal_graph_upload_prompt_tokens(g->prefill_tokens,
+                                                &draft,
+                                                0,
+                                                (uint32_t)stage_tokens);
+    if (ok) ok = metal_graph_upload_prompt_embeddings_hc(
+            g->batch_cur_hc,
+            g->prefill_tokens,
+            base_model,
+            base_weights,
+            &draft,
+            0,
+            (uint32_t)stage_tokens);
+
+    ds4_gpu_tensor *saved_cache = g->layer_raw_cache[0];
+    const uint32_t saved_raw_cap = g->raw_cap;
+    const uint32_t saved_raw_window = g->raw_window;
+    const bool saved_skip_quant = g->skip_raw_kv_quant;
+    const bool saved_full_block = g->dspark_full_block_attention;
+    g->raw_cap = DS4_N_SWA;
+    g->raw_window = DS4_N_SWA;
+    g->skip_raw_kv_quant = true;
+    g->dspark_full_block_attention = true;
+
+    ds4_gpu_tensor *draft_hidden = ds4_gpu_tensor_view(
+            g->batch_ffn_cur, 0,
+            (uint64_t)stage_tokens * DS4_N_EMBD * sizeof(float));
+    ds4_gpu_tensor *confidence_hidden = ds4_gpu_tensor_view(
+            g->batch_router_probs, 0,
+            (uint64_t)stage_tokens * sizeof(float));
+    if (!draft_hidden || !confidence_hidden) ok = false;
+
+    if (ok) ok = ds4_gpu_begin_commands() != 0;
+    for (uint32_t stage = 0; ok && stage < 3; stage++) {
+        g->layer_raw_cache[0] = g->dspark_raw_cache[stage];
+        ok = metal_graph_encode_layer_batch(g,
+                                             support_model,
+                                             &dspark->stage[stage],
+                                             0,
+                                             g->dspark_position,
+                                             (uint32_t)stage_tokens);
+    }
+    if (ok) ok = metal_graph_encode_dspark_head_batch(g,
+                                                       base_model,
+                                                       base_weights,
+                                                       support_model,
+                                                       dspark,
+                                                       (uint32_t)stage_tokens);
+    if (ok) ok = ds4_gpu_matmul_f32_tensor(
+            confidence_hidden,
+            support_model->map,
+            support_model->size,
+            dspark->confidence_proj->abs_offset,
+            DS4_N_EMBD,
+            1,
+            draft_hidden,
+            (uint32_t)stage_tokens) != 0;
+    if (ok) ok = ds4_gpu_end_commands() != 0;
+    else (void)ds4_gpu_synchronize();
+
+    output_ids[0] = input_token;
+    ds4_gpu_tensor *selected_ids = ds4_gpu_tensor_view(
+            g->batch_router_selected, 0,
+            (uint64_t)stage_tokens * sizeof(int32_t));
+    if (!selected_ids) ok = false;
+    if (ok) ok = ds4_gpu_begin_commands() != 0;
+    for (int position = 0; ok && position < stage_tokens; position++) {
+        ds4_gpu_tensor *row_logits = ds4_gpu_tensor_view(
+                g->spec_logits,
+                (uint64_t)position * DS4_N_VOCAB * sizeof(float),
+                (uint64_t)DS4_N_VOCAB * sizeof(float));
+        ds4_gpu_tensor *hidden_score = ds4_gpu_tensor_view(
+                confidence_hidden,
+                (uint64_t)position * sizeof(float),
+                sizeof(float));
+        ds4_gpu_tensor *markov_score = ds4_gpu_tensor_view(
+                g->batch_router_weights,
+                (uint64_t)position * sizeof(float),
+                sizeof(float));
+        ds4_gpu_tensor *selected_id = ds4_gpu_tensor_view(
+                selected_ids,
+                (uint64_t)position * sizeof(int32_t),
+                sizeof(int32_t));
+        ds4_gpu_tensor *previous_id = position > 0
+            ? ds4_gpu_tensor_view(
+                selected_ids,
+                (uint64_t)(position - 1) * sizeof(int32_t),
+                sizeof(int32_t))
+            : NULL;
+        ok = row_logits && hidden_score && markov_score && selected_id &&
+             (position == 0 || previous_id);
+        if (ok && position == 0) {
+            ok = ds4_gpu_embed_token_hc_tensor(
+                    g->mtp_embed,
+                    support_model->map,
+                    support_model->size,
+                    dspark->markov_w1->abs_offset,
+                    (uint32_t)dspark->markov_w1->dim[1],
+                    (uint32_t)input_token,
+                    256,
+                    1) != 0;
+        } else if (ok) {
+            ok = ds4_gpu_embed_tokens_hc_tensor(
+                    g->mtp_embed,
+                    previous_id,
+                    support_model->map,
+                    support_model->size,
+                    dspark->markov_w1->abs_offset,
+                    (uint32_t)dspark->markov_w1->dim[1],
+                    1,
+                    256,
+                    1) != 0;
+        }
+        if (ok) ok = ds4_gpu_matmul_q8_0_tensor(
+                g->logits,
+                support_model->map,
+                support_model->size,
+                dspark->markov_w2->abs_offset,
+                256,
+                DS4_N_VOCAB,
+                g->mtp_embed,
+                1) != 0;
+        if (ok) ok = ds4_gpu_add_tensor(row_logits,
+                                         row_logits,
+                                         g->logits,
+                                         DS4_N_VOCAB) != 0;
+        if (ok) ok = ds4_gpu_argmax_tensor(selected_id,
+                                            row_logits,
+                                            DS4_N_VOCAB) != 0;
+        if (ok) ok = ds4_gpu_matmul_f32_tensor(
+                markov_score,
+                support_model->map,
+                support_model->size,
+                dspark->confidence_proj->abs_offset +
+                    (uint64_t)DS4_N_EMBD * sizeof(float),
+                256,
+                1,
+                g->mtp_embed,
+                1) != 0;
+        if (ok) ok = ds4_gpu_add_tensor(markov_score,
+                                         hidden_score,
+                                         markov_score,
+                                         1) != 0;
+        ds4_gpu_tensor_free(previous_id);
+        ds4_gpu_tensor_free(selected_id);
+        ds4_gpu_tensor_free(markov_score);
+        ds4_gpu_tensor_free(hidden_score);
+        ds4_gpu_tensor_free(row_logits);
+    }
+    if (ok) ok = ds4_gpu_end_commands() != 0;
+    else (void)ds4_gpu_synchronize();
+    if (ok) ok = ds4_gpu_tensor_read(
+            selected_ids, 0, output_ids + 1,
+            (uint64_t)stage_tokens * sizeof(output_ids[0])) != 0;
+    if (ok) ok = ds4_gpu_tensor_read(
+            g->batch_router_weights, 0, output_confidence,
+            (uint64_t)stage_tokens * sizeof(output_confidence[0])) != 0;
+
+    ds4_gpu_tensor_free(selected_ids);
+    ds4_gpu_tensor_free(confidence_hidden);
+    ds4_gpu_tensor_free(draft_hidden);
+
+    g->layer_raw_cache[0] = saved_cache;
+    g->raw_cap = saved_raw_cap;
+    g->raw_window = saved_raw_window;
+    g->skip_raw_kv_quant = saved_skip_quant;
+    g->dspark_full_block_attention = saved_full_block;
+    return ok;
+}
+
+int ds4_dspark_probe_stages(const char *base_path, const char *support_path,
+                            const float *prefill_kv, unsigned prefill_count,
+                            const float *current_main_x, int input_token,
+                            float *stage_outputs, int *output_ids) {
+    enum { DSPARK_STAGES = 3, DSPARK_DRAFT = 5, DSPARK_NOISE = 128799 };
+    if (!base_path || !support_path || !prefill_kv || !current_main_x ||
+        !stage_outputs || !output_ids || prefill_count == 0 ||
+        prefill_count + DSPARK_DRAFT > DS4_N_SWA)
+        return 1;
+
+    ds4_model base_model, support_model;
+    memset(&base_model, 0, sizeof(base_model));
+    memset(&support_model, 0, sizeof(support_model));
+    base_model.fd = support_model.fd = -1;
+    ds4_weights base_weights;
+    ds4_dspark_weights dspark;
+    ds4_gpu_graph graph;
+    memset(&graph, 0, sizeof(graph));
+    ds4_gpu_tensor *stage_cache[DSPARK_STAGES] = {0};
+    bool graph_ready = false;
+    bool ok = true;
+
+    model_open(&base_model, base_path, true, true);
+    config_validate_model(&base_model);
+    weights_bind(&base_weights, &base_model, false, 0, 0, true, false);
+    model_open(&support_model, support_path, true, true);
+    dspark_weights_bind(&dspark, &support_model);
+
+    ok = ds4_gpu_init() != 0;
+    if (ok) ok = ds4_gpu_set_model_fd_for_map(base_model.fd, base_model.map) != 0;
+    if (ok) ok = ds4_gpu_set_model_map_range(base_model.map, base_model.size,
+                                               base_model.tensor_data_pos,
+                                               base_model.size - base_model.tensor_data_pos,
+                                               base_model.max_tensor_bytes) != 0;
+    if (ok) ok = ds4_gpu_set_model_map_range(support_model.map, support_model.size,
+                                               support_model.tensor_data_pos,
+                                               support_model.size - support_model.tensor_data_pos,
+                                               support_model.max_tensor_bytes) != 0;
+    if (ok) {
+        ok = metal_graph_alloc_raw_cap(&graph, &base_weights, &base_weights.layer[0],
+                                       DS4_N_SWA, DS4_N_SWA, 16, true);
+        graph_ready = ok;
+        graph.skip_raw_kv_quant = true;
+        graph.dspark_full_block_attention = true;
+    }
+    for (int stage = 0; ok && stage < DSPARK_STAGES; stage++) {
+        stage_cache[stage] = ds4_gpu_tensor_alloc(
+                (uint64_t)DS4_N_SWA * DS4_N_HEAD_DIM * sizeof(float));
+        ok = stage_cache[stage] != NULL;
+        if (ok) {
+            ok = ds4_gpu_tensor_write(stage_cache[stage], 0,
+                                      prefill_kv + (uint64_t)stage * prefill_count * DS4_N_HEAD_DIM,
+                                      (uint64_t)prefill_count * DS4_N_HEAD_DIM * sizeof(float)) != 0;
+        }
+    }
+
+    int draft_ids[DSPARK_DRAFT] = {
+        input_token, DSPARK_NOISE, DSPARK_NOISE, DSPARK_NOISE, DSPARK_NOISE,
+    };
+    token_vec draft = {.v = draft_ids, .len = DSPARK_DRAFT, .cap = DSPARK_DRAFT};
+    if (ok) {
+        ok = metal_graph_upload_prompt_embeddings_hc_cpu(
+                graph.batch_cur_hc, &base_model, &base_weights,
+                &draft, 0, DSPARK_DRAFT);
+    }
+    if (ok) {
+        ok = ds4_gpu_tensor_write(graph.attn_norm, 0, current_main_x,
+                                  (uint64_t)DS4_N_EMBD * sizeof(float)) != 0;
+    }
+
+    ds4_gpu_tensor *saved_cache = graph.layer_raw_cache[0];
+    const float freq_base = layer_rope_freq_base(0);
+    const float freq_scale = layer_rope_freq_scale(0);
+    for (int stage = 0; ok && stage < DSPARK_STAGES; stage++) {
+        graph.layer_raw_cache[0] = stage_cache[stage];
+        ok = ds4_gpu_begin_commands() != 0;
+        if (ok) ok = metal_graph_matmul_plain_tensor(
+                graph.kv_raw, &support_model, dspark.stage[stage].attn_kv,
+                DS4_N_EMBD, DS4_N_HEAD_DIM, graph.attn_norm, 1);
+        if (ok) ok = ds4_gpu_rms_norm_weight_tensor(graph.kv, graph.kv_raw,
+                                                      support_model.map,
+                                                      support_model.size,
+                                                      dspark.stage[stage].attn_kv_a_norm->abs_offset,
+                                                      DS4_N_HEAD_DIM, DS4_RMS_EPS) != 0;
+        if (ok) ok = ds4_gpu_rope_tail_tensor(graph.kv, 1, DS4_N_HEAD_KV,
+                                               DS4_N_HEAD_DIM, DS4_N_ROT,
+                                               prefill_count, 0, false,
+                                               freq_base, freq_scale, 0.0f, 1.0f,
+                                               DS4_ROPE_YARN_BETA_FAST,
+                                               DS4_ROPE_YARN_BETA_SLOW) != 0;
+        if (ok && !graph.skip_raw_kv_quant)
+            ok = ds4_gpu_dsv4_fp8_kv_quantize_tensor(
+                    graph.kv, 1, DS4_N_HEAD_DIM, DS4_N_ROT) != 0;
+        if (ok) ok = ds4_gpu_store_raw_kv_tensor(stage_cache[stage], graph.kv,
+                                                  DS4_N_SWA, prefill_count,
+                                                  DS4_N_HEAD_DIM) != 0;
+        if (ok) ok = metal_graph_encode_layer_batch(&graph, &support_model,
+                                                     &dspark.stage[stage], 0,
+                                                     prefill_count + 1u,
+                                                     DSPARK_DRAFT);
+        if (ok) ok = ds4_gpu_end_commands() != 0;
+        if (ok) {
+            ok = ds4_gpu_tensor_read(
+                    graph.batch_cur_hc, 0,
+                    stage_outputs + (uint64_t)stage * DSPARK_DRAFT * DS4_N_HC * DS4_N_EMBD,
+                    (uint64_t)DSPARK_DRAFT * DS4_N_HC * DS4_N_EMBD * sizeof(float)) != 0;
+            if (ok && getenv("DS4_DSPARK_PROBE_DEBUG")) {
+                char label[32];
+                snprintf(label, sizeof(label), "dspark_stage_%d", stage);
+                print_vec_stats(label,
+                                stage_outputs + (uint64_t)stage * DSPARK_DRAFT * DS4_N_HC * DS4_N_EMBD,
+                                (uint64_t)DSPARK_DRAFT * DS4_N_HC * DS4_N_EMBD);
+            }
+        }
+    }
+    if (ok) ok = ds4_gpu_begin_commands() != 0;
+    if (ok) ok = metal_graph_encode_dspark_head_batch(
+                        &graph, &base_model, &base_weights,
+                        &support_model, &dspark, DSPARK_DRAFT);
+    if (ok) ok = ds4_gpu_end_commands() != 0;
+
+    output_ids[0] = input_token;
+    const uint16_t *markov_w1 = tensor_data(&support_model, dspark.markov_w1);
+    float markov_embed[256];
+    for (int position = 0; ok && position < DSPARK_DRAFT; position++) {
+        const uint16_t *row = markov_w1 + (uint64_t)output_ids[position] * 256u;
+        for (uint32_t i = 0; i < 256; i++) markov_embed[i] = f16_to_f32(row[i]);
+        ok = ds4_gpu_tensor_write(graph.mtp_embed, 0, markov_embed,
+                                  sizeof(markov_embed)) != 0;
+        ds4_gpu_tensor *row_logits = ds4_gpu_tensor_view(
+                graph.spec_logits,
+                (uint64_t)position * DS4_N_VOCAB * sizeof(float),
+                (uint64_t)DS4_N_VOCAB * sizeof(float));
+        if (!row_logits) ok = false;
+        if (ok) ok = ds4_gpu_begin_commands() != 0;
+        if (ok) ok = ds4_gpu_matmul_q8_0_tensor(
+                graph.logits, support_model.map, support_model.size,
+                dspark.markov_w2->abs_offset, 256, DS4_N_VOCAB,
+                graph.mtp_embed, 1) != 0;
+        if (ok) ok = ds4_gpu_add_tensor(
+                row_logits, row_logits, graph.logits, DS4_N_VOCAB) != 0;
+        if (ok) ok = ds4_gpu_argmax_tensor(
+                graph.comp_selected, row_logits, DS4_N_VOCAB) != 0;
+        if (ok) ok = ds4_gpu_end_commands() != 0;
+        if (ok) ok = ds4_gpu_tensor_read(
+                graph.comp_selected, 0, &output_ids[position + 1],
+                sizeof(output_ids[position + 1])) != 0;
+        ds4_gpu_tensor_free(row_logits);
+    }
+    graph.layer_raw_cache[0] = saved_cache;
+    if (!ok) (void)ds4_gpu_synchronize();
+    for (int stage = 0; stage < DSPARK_STAGES; stage++)
+        ds4_gpu_tensor_free(stage_cache[stage]);
+    if (graph_ready) metal_graph_free(&graph);
+    weights_free(&base_weights);
+    model_close(&support_model);
+    model_close(&base_model);
+    return ok ? 0 : 1;
+}
+
 /* =========================================================================
  * Imatrix Collection.
  * =========================================================================
@@ -20422,6 +21155,7 @@ static bool metal_graph_reset_prefill_state(ds4_gpu_graph *g) {
     memset(g->layer_n_comp, 0, sizeof(g->layer_n_comp));
     memset(g->layer_n_index_comp, 0, sizeof(g->layer_n_index_comp));
     g->mtp_n_raw = 0;
+    g->dspark_position = 0;
     for (uint32_t il = 0; il < DS4_N_LAYER; il++) {
         const uint32_t ratio = ds4_layer_compress_ratio(il);
         if (ratio == 0) continue;
@@ -20536,6 +21270,8 @@ static bool metal_graph_prefill_layer_major(
             display_progress(display_progress_ud, "prefill_display",
                              (int)(start + n_tokens), prompt->len);

+        if (ok) ok = metal_graph_encode_dspark_append_main(g, start, n_tokens);
+
         const uint64_t hc_dim = (uint64_t)DS4_N_HC * DS4_N_EMBD;
         uint32_t output_row = (uint32_t)n_tokens - 1u;
         const char *output_row_env = getenv("DS4_METAL_GRAPH_OUTPUT_ROW");
@@ -20968,6 +21704,14 @@ static bool metal_graph_prefill_layer_major(
         return false;
     }

+    if (g->dspark_enabled) {
+        ok = ds4_gpu_begin_commands() != 0;
+        if (ok) ok = metal_graph_encode_dspark_append_main(g, start, n_tokens);
+        if (ok) ok = ds4_gpu_end_commands() != 0;
+        else (void)ds4_gpu_synchronize();
+        if (!ok) return false;
+    }
+
     const uint64_t hc_dim = (uint64_t)DS4_N_HC * DS4_N_EMBD;
     uint32_t output_row = (uint32_t)n_tokens - 1u;
     const char *output_row_env = getenv("DS4_METAL_GRAPH_OUTPUT_ROW");
@@ -21290,8 +22034,13 @@ static bool metal_graph_verify_suffix_tops(
     if (!ok) return false;

     const bool saved_capture = g->spec_capture_prefix1;
+    const bool saved_decode_order = g->spec_verify_decode_order;
     g->spec_capture_prefix1 = capture_prefix1 && n_tokens == 2;
+    g->spec_verify_decode_order = true;

+    const bool saved_dump_verifier = g_metal_graph_dump_verifier;
+    g_metal_graph_dump_verifier = true;
+    ds4_gpu_set_tiny_decode_order(true);
     ok = ds4_gpu_begin_commands() != 0;
     for (uint32_t il = 0; ok && il < DS4_N_LAYER; il++) {
         ok = metal_graph_encode_layer_batch(g,
@@ -21303,6 +22052,9 @@ static bool metal_graph_verify_suffix_tops(
     }
     if (ok) ok = ds4_gpu_end_commands() != 0;
     else (void)ds4_gpu_synchronize();
+    ds4_gpu_set_tiny_decode_order(saved_decode_order);
+    g_metal_graph_dump_verifier = saved_dump_verifier;
+    g->spec_verify_decode_order = saved_decode_order;
     g->spec_capture_prefix1 = saved_capture;
     if (!ok) return false;

@@ -21961,9 +22713,11 @@ struct ds4_engine {
     ds4_vocab vocab;
     ds4_weights weights;
     ds4_mtp_weights mtp_weights;
+    ds4_dspark_weights dspark_weights;
     ds4_backend backend;
     int mtp_draft_tokens;
     float mtp_margin;
+    float dspark_confidence_threshold;
     char *directional_steering_file;
     float *directional_steering_dirs;
     float directional_steering_attn_scale;
@@ -21980,6 +22734,7 @@ struct ds4_engine {
     ds4_distributed_options distributed;
     bool metal_ready;
     bool mtp_ready;
+    bool dspark_ready;
 };

 static bool cpu_directional_steering_enabled(
@@ -23418,6 +24173,10 @@ struct ds4_session {
     float *logits;
     float *mtp_logits;
     int mtp_draft_token;
+    int dspark_proposals[6];
+    float dspark_confidence[5];
+    int dspark_proposal_count;
+    ds4_session_spec_stats spec_stats;
     uint64_t mtp_probe_total;
     uint64_t mtp_probe_hit;
     ds4_session_progress_fn progress;
@@ -23430,6 +24189,7 @@ struct ds4_session {
     int ctx_size;
     bool checkpoint_valid;
     bool mtp_draft_valid;
+    bool dspark_proposals_valid;
 };

 /* =========================================================================
@@ -24193,13 +24953,30 @@ bool ds4_engine_has_output_head(ds4_engine *e) {
 bool ds4_engine_has_mtp(ds4_engine *e) {
     return e && e->backend != DS4_BACKEND_CPU &&
            e->distributed.role == DS4_DISTRIBUTED_NONE &&
-           e->mtp_ready;
+           (e->mtp_ready || e->dspark_ready);
+}
+
+bool ds4_engine_has_dspark(ds4_engine *e) {
+    return e && e->backend != DS4_BACKEND_CPU &&
+           e->distributed.role == DS4_DISTRIBUTED_NONE &&
+           e->dspark_ready;
 }

 int ds4_engine_mtp_draft_tokens(ds4_engine *e) {
     return ds4_engine_has_mtp(e) ? e->mtp_draft_tokens : 0;
 }

+int ds4_dspark_confident_prefix_length(const float *confidence_logits,
+                                       int block_size, float threshold) {
+    if (!confidence_logits || block_size <= 0) return 0;
+    if (threshold <= 0.0f) return block_size;
+    for (int i = 0; i < block_size; i++) {
+        const float probability = 1.0f / (1.0f + expf(-confidence_logits[i]));
+        if (probability < threshold) return i;
+    }
+    return block_size;
+}
+
 const ds4_tokens *ds4_session_tokens(ds4_session *s) {
     return s ? &s->checkpoint : NULL;
 }
@@ -24310,6 +25087,20 @@ static bool spec_frontier_commit_prefix1(ds4_session *s) {
     else (void)ds4_gpu_synchronize();
     return ok;
 }
+
+/* Commit a verifier prefix without copying compressor state when the rejected
+ * suffix did not perform a ratio-4 state shift. Future raw/compressor slots are
+ * append-only garbage and will be overwritten before they become visible. */
+static void spec_frontier_commit_append_only_prefix(ds4_session *s,
+                                                      uint32_t new_length) {
+    ds4_gpu_graph *g = &s->graph;
+    for (uint32_t il = 0; il < DS4_N_LAYER; il++) {
+        const uint32_t ratio = ds4_layer_compress_ratio(il);
+        if (ratio == 0) continue;
+        g->layer_n_comp[il] = new_length / ratio;
+        if (ratio == 4) g->layer_n_index_comp[il] = new_length / ratio;
+    }
+}
 #endif

 uint64_t ds4_session_payload_bytes(ds4_session *s) {
@@ -25711,6 +26502,10 @@ int ds4_engine_open(ds4_engine **out, const ds4_engine_options *opt) {
     e->mtp_draft_tokens = opt->mtp_draft_tokens > 0 ? opt->mtp_draft_tokens : 1;
     if (e->mtp_draft_tokens > 16) e->mtp_draft_tokens = 16;
     e->mtp_margin = opt->mtp_margin >= 0.0f ? opt->mtp_margin : 3.0f;
+    e->dspark_confidence_threshold =
+        opt->dspark_confidence_threshold >= 0.0f &&
+        opt->dspark_confidence_threshold <= 1.0f
+            ? opt->dspark_confidence_threshold : 0.0f;
     if ((opt->directional_steering_attn != 0.0f || opt->directional_steering_ffn != 0.0f) &&
         (!opt->directional_steering_file || !opt->directional_steering_file[0]))
     {
@@ -25829,19 +26624,34 @@ int ds4_engine_open(ds4_engine **out, const ds4_engine_options *opt) {
         *out = NULL;
         return 1;
     }
-    if (opt->mtp_path && opt->mtp_path[0] &&
+    const bool wants_mtp = opt->mtp_path && opt->mtp_path[0];
+    const bool wants_dspark = opt->dspark_path && opt->dspark_path[0];
+    if (wants_mtp && wants_dspark) {
+        fprintf(stderr, "ds4: MTP and DSpark support models are mutually exclusive\n");
+        ds4_engine_close(e);
+        *out = NULL;
+        return 1;
+    }
+    if ((wants_mtp || wants_dspark) &&
         opt->distributed.role == DS4_DISTRIBUTED_NONE) {
+        const char *support_path = wants_dspark ? opt->dspark_path : opt->mtp_path;
         if (e->ssd_streaming) {
-            fprintf(stderr, "ds4: --ssd-streaming is not compatible with --mtp yet\n");
+            fprintf(stderr, "ds4: --ssd-streaming is not compatible with speculative support yet\n");
             ds4_engine_close(e);
             *out = NULL;
             return 1;
         }
-        model_open(&e->mtp_model, opt->mtp_path, graph_backend, true);
-        mtp_weights_bind(&e->mtp_weights, &e->mtp_model);
-        e->mtp_ready = true;
-        fprintf(stderr, "ds4: MTP support model loaded: %s (draft=%d)\n",
-                opt->mtp_path,
+        model_open(&e->mtp_model, support_path, graph_backend, true);
+        if (wants_dspark) {
+            dspark_weights_bind(&e->dspark_weights, &e->mtp_model);
+            e->dspark_ready = true;
+        } else {
+            mtp_weights_bind(&e->mtp_weights, &e->mtp_model);
+            e->mtp_ready = true;
+        }
+        fprintf(stderr, "ds4: %s support model loaded: %s (draft=%d)\n",
+                wants_dspark ? "DSpark" : "MTP",
+                support_path,
                 e->mtp_draft_tokens);
     }

@@ -26052,7 +26862,7 @@ int ds4_engine_open(ds4_engine **out, const ds4_engine_options *opt) {
             *out = NULL;
             return 1;
         }
-        if (e->mtp_ready &&
+        if ((e->mtp_ready || e->dspark_ready) &&
             !ds4_gpu_set_model_map_range(e->mtp_model.map,
                                            e->mtp_model.size,
                                            e->mtp_model.tensor_data_pos,
@@ -26095,7 +26905,7 @@ int ds4_engine_open(ds4_engine **out, const ds4_engine_options *opt) {
         free(load_sizes);
         /* Also apply explicit optional Q8 preload settings to the MTP support
          * model when loaded. */
-        if (e->mtp_ready) {
+        if (e->mtp_ready || e->dspark_ready) {
             (void)ds4_gpu_set_model_fd_for_map(e->mtp_model.fd, e->mtp_model.map);
             if (!accelerator_cache_model_tensors(e->backend, &e->mtp_model,
                                                  NULL, NULL, 0)) {
@@ -26174,7 +26984,7 @@ void ds4_engine_close(ds4_engine *e) {
     weights_free(&e->weights);
     vocab_free(&e->vocab);
     ds4_threads_shutdown();
-    if (e->mtp_ready) model_close(&e->mtp_model);
+    if (e->mtp_ready || e->dspark_ready) model_close(&e->mtp_model);
     model_close(&e->model);
 #ifndef DS4_NO_GPU
     ds4_gpu_cleanup();
@@ -26222,11 +27032,20 @@ int ds4_session_create(ds4_session **out, ds4_engine *e, int ctx_size) {
         return 1;
     }
     if (!metal_graph_alloc_raw_cap(&s->graph, &e->weights, shape_layer,
-                                   raw_cap, (uint32_t)ctx_size, s->prefill_cap, e->mtp_ready))
+                                   raw_cap, (uint32_t)ctx_size, s->prefill_cap,
+                                   e->mtp_ready || e->dspark_ready))
     {
         free(s);
         return 1;
     }
+    if (e->dspark_ready &&
+        !metal_graph_alloc_dspark_state(&s->graph,
+                                        &e->mtp_model,
+                                        &e->dspark_weights)) {
+        metal_graph_free(&s->graph);
+        free(s);
+        return 1;
+    }
     s->graph.quality = e->quality;
     s->graph.ssd_streaming = e->ssd_streaming;
     s->graph.ssd_streaming_cold = e->ssd_streaming_cold;
@@ -26241,7 +27060,7 @@ int ds4_session_create(ds4_session **out, ds4_engine *e, int ctx_size) {
         return 1;
     }
     s->logits = xmalloc((size_t)DS4_N_VOCAB * sizeof(s->logits[0]));
-    if (e->mtp_ready) {
+    if (e->mtp_ready || e->dspark_ready) {
         s->mtp_logits = xmalloc((size_t)DS4_N_VOCAB * sizeof(s->mtp_logits[0]));
         s->mtp_draft_token = -1;
     }
@@ -27255,6 +28074,7 @@ static int ds4_session_eval_internal(ds4_session *s, int token, bool probe_mtp,
     return 1;
 #else
     ds4_engine *e = s->engine;
+    if (probe_mtp) s->dspark_proposals_valid = false;
     const bool mtp_probe_log = getenv("DS4_MTP_PROBE") != NULL;
     const bool mtp_should_draft =
         probe_mtp && e->mtp_ready && s->mtp_logits &&
@@ -27299,6 +28119,35 @@ static int ds4_session_eval_internal(ds4_session *s, int token, bool probe_mtp,
             fprintf(stderr, "ds4: mtp probe draft failed\n");
         }
     }
+    if (probe_mtp && e->dspark_ready) {
+        const int input_token = sample_argmax(s->logits, DS4_N_VOCAB);
+        int proposal_count = e->mtp_draft_tokens;
+        if (proposal_count > 6) proposal_count = 6;
+        const double proposal_started = now_sec();
+        if (input_token >= 0 && metal_graph_eval_dspark_proposals(
+                &s->graph,
+                &e->model,
+                &e->weights,
+                &e->mtp_model,
+                &e->dspark_weights,
+                input_token,
+                proposal_count,
+                s->dspark_proposals,
+                s->dspark_confidence)) {
+            const int sampled_count = proposal_count - 1;
+            const int confident_count = ds4_dspark_confident_prefix_length(
+                    s->dspark_confidence,
+                    sampled_count,
+                    e->dspark_confidence_threshold);
+            s->dspark_proposal_count = confident_count > 0
+                ? 1 + confident_count : 0;
+            s->dspark_proposals_valid = s->dspark_proposal_count > 0;
+        } else {
+            snprintf(err, errlen, "DSpark proposal failed");
+            return 1;
+        }
+        s->spec_stats.proposal_ms += (now_sec() - proposal_started) * 1000.0;
+    }
     return 0;
 #endif
 }
@@ -27307,6 +28156,209 @@ int ds4_session_eval(ds4_session *s, int token, char *err, size_t errlen) {
     return ds4_session_eval_internal(s, token, true, err, errlen);
 }

+int ds4_session_copy_dspark_proposals(ds4_session *s, int *out, int cap) {
+    if (!s || !out || !s->dspark_proposals_valid ||
+        cap < s->dspark_proposal_count) return 0;
+    memcpy(out, s->dspark_proposals,
+           (size_t)s->dspark_proposal_count * sizeof(s->dspark_proposals[0]));
+    return s->dspark_proposal_count;
+}
+
+int ds4_session_copy_dspark_confidence(ds4_session *s, float *out, int cap) {
+    const int count = s ? s->dspark_proposal_count - 1 : 0;
+    if (!s || !out || !s->dspark_proposals_valid || count <= 0 || cap < count)
+        return 0;
+    memcpy(out, s->dspark_confidence,
+           (size_t)count * sizeof(s->dspark_confidence[0]));
+    return count;
+}
+
+int ds4_session_get_spec_stats(const ds4_session *s, ds4_session_spec_stats *out) {
+    if (!s || !out) return 0;
+    *out = s->spec_stats;
+    return 1;
+}
+
+int ds4_session_get_kernel_stats(const ds4_session *s,
+                                 ds4_session_kernel_stats *out) {
+    if (!s || !out) return 0;
+    memset(out, 0, sizeof(*out));
+#ifndef DS4_NO_GPU
+    if (!ds4_session_is_cpu(s)) {
+        ds4_gpu_kernel_stats gpu_stats;
+        memset(&gpu_stats, 0, sizeof(gpu_stats));
+        ds4_gpu_get_kernel_stats(&gpu_stats);
+        out->tiny_batch_pair_swiglu_calls =
+            gpu_stats.tiny_batch_pair_swiglu_calls;
+        out->tiny_batch_activation_fallback_calls =
+            gpu_stats.tiny_batch_activation_fallback_calls;
+        out->tiny_batch_shared_swiglu_calls =
+            gpu_stats.tiny_batch_shared_swiglu_calls;
+    }
+#endif
+    return 1;
+}
+
+#ifndef DS4_NO_GPU
+static int ds4_session_commit_dspark_speculative(
+        ds4_session *s,
+        int max_tokens,
+        int eos_token,
+        int *accepted,
+        int accepted_cap,
+        int n_accept,
+        char *err,
+        size_t errlen) {
+    if (!s->dspark_proposals_valid || max_tokens <= n_accept ||
+        accepted_cap <= n_accept) return n_accept;
+
+    int draft_n = s->engine->mtp_draft_tokens;
+    if (draft_n > 6) draft_n = 6;
+    if (draft_n > s->dspark_proposal_count)
+        draft_n = s->dspark_proposal_count;
+    if (draft_n > max_tokens - n_accept) draft_n = max_tokens - n_accept;
+    if (draft_n > accepted_cap - n_accept) draft_n = accepted_cap - n_accept;
+    int room = s->ctx_size - s->checkpoint.len;
+    if (draft_n > room) draft_n = room;
+    for (int i = 0; i < draft_n; i++) {
+        if (s->dspark_proposals[i] == eos_token) {
+            draft_n = i + 1;
+            break;
+        }
+    }
+    if (draft_n <= 0 ||
+        sample_argmax(s->logits, DS4_N_VOCAB) != s->dspark_proposals[0]) {
+        s->dspark_proposals_valid = false;
+        return n_accept;
+    }
+    s->spec_stats.rounds++;
+    s->spec_stats.proposed_tokens += (uint64_t)draft_n;
+
+    ds4_spec_frontier frontier;
+    memset(&frontier, 0, sizeof(frontier));
+    const int start = s->checkpoint.len;
+    int row_tops[6] = {-1, -1, -1, -1, -1, -1};
+    float *last_logits = xmalloc((size_t)DS4_N_VOCAB * sizeof(last_logits[0]));
+    bool ok = spec_frontier_snapshot(&frontier, s);
+    if (ok) {
+        for (int i = 0; i < draft_n; i++)
+            token_vec_push(&s->checkpoint, s->dspark_proposals[i]);
+        const double verify_started = now_sec();
+        ok = metal_graph_verify_suffix_tops(&s->graph,
+                                             &s->engine->model,
+                                             &s->engine->weights,
+                                             &s->checkpoint,
+                                             (uint32_t)start,
+                                             (uint32_t)draft_n,
+                                             false,
+                                             row_tops,
+                                             NULL);
+        s->spec_stats.verify_ms += (now_sec() - verify_started) * 1000.0;
+    }
+
+    int commit = ok ? 1 : 0;
+    for (int i = 1; ok && i < draft_n; i++) {
+        if (row_tops[i - 1] != s->dspark_proposals[i]) break;
+        commit++;
+    }
+    if (ok && commit == draft_n) {
+        ok = metal_graph_read_spec_logits_row(&s->graph,
+                                               (uint32_t)(draft_n - 1),
+                                               last_logits);
+        if (ok) ok = ds4_gpu_begin_commands() != 0;
+        if (ok) ok = metal_graph_encode_dspark_append_main(
+                &s->graph, (uint32_t)start, (uint32_t)draft_n);
+        if (ok) ok = ds4_gpu_end_commands() != 0;
+        else (void)ds4_gpu_synchronize();
+        if (ok) {
+            memcpy(s->logits, last_logits,
+                   (size_t)DS4_N_VOCAB * sizeof(s->logits[0]));
+            for (int i = 0; i < draft_n; i++)
+                accepted[n_accept++] = s->dspark_proposals[i];
+            s->spec_stats.accepted_tokens += (uint64_t)draft_n;
+            s->checkpoint_valid = true;
+            s->dspark_proposals_valid = false;
+            spec_frontier_free(&frontier);
+            free(last_logits);
+            return n_accept;
+        }
+    }
+
+    const bool direct_partial = ok && commit > 0 && commit < draft_n &&
+        (start + draft_n) / 4 == (start + commit) / 4;
+    if (direct_partial) {
+        ok = metal_graph_read_spec_logits_row(&s->graph,
+                                               (uint32_t)(commit - 1),
+                                               last_logits);
+        if (ok) {
+            spec_frontier_commit_append_only_prefix(
+                    s, (uint32_t)(start + commit));
+            ok = ds4_gpu_begin_commands() != 0;
+        }
+        if (ok) ok = metal_graph_encode_dspark_append_main(
+                &s->graph, (uint32_t)start, (uint32_t)commit);
+        if (ok) ok = ds4_gpu_end_commands() != 0;
+        else (void)ds4_gpu_synchronize();
+        if (ok) {
+            s->checkpoint.len = start;
+            memcpy(s->logits, last_logits,
+                   (size_t)DS4_N_VOCAB * sizeof(s->logits[0]));
+            for (int i = 0; i < commit; i++) {
+                token_vec_push(&s->checkpoint, s->dspark_proposals[i]);
+                accepted[n_accept++] = s->dspark_proposals[i];
+            }
+            s->spec_stats.accepted_tokens += (uint64_t)commit;
+            s->spec_stats.direct_accepted_tokens += (uint64_t)commit;
+            s->checkpoint_valid = true;
+            s->dspark_proposals_valid = false;
+            spec_frontier_free(&frontier);
+            free(last_logits);
+            return n_accept;
+        }
+        snprintf(err, errlen, "DSpark direct partial commit failed");
+        s->checkpoint_valid = false;
+        spec_frontier_free(&frontier);
+        free(last_logits);
+        return -1;
+    }
+
+    s->checkpoint.len = start;
+    if (!spec_frontier_restore(&frontier, s)) ok = false;
+    const double replay_started = now_sec();
+    if (ok && commit > 0) {
+        for (int i = 0; ok && i < commit; i++) {
+            ok = metal_graph_eval_token_raw_swa(
+                    &s->graph,
+                    &s->engine->model,
+                    &s->engine->weights,
+                    s->dspark_proposals[i],
+                    (uint32_t)(start + i),
+                    last_logits);
+            if (ok) {
+                token_vec_push(&s->checkpoint, s->dspark_proposals[i]);
+                accepted[n_accept++] = s->dspark_proposals[i];
+            }
+        }
+        if (ok) {
+            memcpy(s->logits, last_logits,
+                   (size_t)DS4_N_VOCAB * sizeof(s->logits[0]));
+            s->checkpoint_valid = true;
+        }
+    }
+    s->spec_stats.replay_ms += (now_sec() - replay_started) * 1000.0;
+    if (ok) s->spec_stats.accepted_tokens += (uint64_t)commit;
+    s->dspark_proposals_valid = false;
+    spec_frontier_free(&frontier);
+    free(last_logits);
+    if (!ok) {
+        snprintf(err, errlen, "DSpark verifier failed");
+        s->checkpoint_valid = false;
+        return -1;
+    }
+    return n_accept;
+}
+#endif
+
 /* Speculative decode state machine:
  * 1. commit the normal target token and use its logits to validate draft[0];
  * 2. let MTP recursively draft a tiny suffix from its own raw-cache frontier;
@@ -27349,11 +28401,23 @@ int ds4_session_eval_speculative_argmax(ds4_session *s, int first_token,
      * several proposed positions together; running ordinary decode once per
      * draft token is correctness-safe but cannot be faster than baseline.
      */
+    const double target_started = now_sec();
+    const double proposal_before = s->spec_stats.proposal_ms;
     if (ds4_session_eval(s, first_token, err, errlen) != 0) return -1;
+    const double proposal_delta = s->spec_stats.proposal_ms - proposal_before;
+    double target_ms = (now_sec() - target_started) * 1000.0 - proposal_delta;
+    if (target_ms < 0.0) target_ms = 0.0;
+    s->spec_stats.target_ms += target_ms;
     int n_accept = 0;
     accepted[n_accept++] = first_token;
     if (first_token == eos_token || max_tokens == 1 || n_accept >= accepted_cap) return n_accept;

+    if (e->dspark_ready) {
+        return ds4_session_commit_dspark_speculative(
+                s, max_tokens, eos_token, accepted, accepted_cap,
+                n_accept, err, errlen);
+    }
+
     if (!e->mtp_ready || !s->mtp_draft_valid || e->mtp_draft_tokens <= 1) return n_accept;

     int draft_cap = e->mtp_draft_tokens;
@@ -27919,6 +28983,7 @@ void ds4_session_invalidate(ds4_session *s) {
     s->checkpoint_valid = false;
     s->checkpoint.len = 0;
     s->mtp_draft_valid = false;
+    s->dspark_proposals_valid = false;
 }

 void ds4_session_rewind(ds4_session *s, int pos) {
@@ -27926,6 +28991,7 @@ void ds4_session_rewind(ds4_session *s, int pos) {
     if (pos > s->checkpoint.len) pos = s->checkpoint.len;
     s->checkpoint.len = pos;
     s->mtp_draft_valid = false;
+    s->dspark_proposals_valid = false;
 }

 int ds4_session_pos(ds4_session *s) {
diff --git a/ds4.h b/ds4.h
index 07ba0f0..29a0210 100644
--- a/ds4.h
+++ b/ds4.h
@@ -52,6 +52,23 @@ typedef struct {
     float logprob;
 } ds4_token_score;

+typedef struct {
+    uint64_t rounds;
+    uint64_t proposed_tokens;
+    uint64_t accepted_tokens;
+    uint64_t direct_accepted_tokens;
+    double target_ms;
+    double proposal_ms;
+    double verify_ms;
+    double replay_ms;
+} ds4_session_spec_stats;
+
+typedef struct {
+    uint64_t tiny_batch_pair_swiglu_calls;
+    uint64_t tiny_batch_activation_fallback_calls;
+    uint64_t tiny_batch_shared_swiglu_calls;
+} ds4_session_kernel_stats;
+
 #define DS4_DEFAULT_TEMPERATURE 1.0f
 #define DS4_DEFAULT_TOP_P 1.0f
 #define DS4_DEFAULT_MIN_P 0.05f
@@ -94,11 +111,13 @@ typedef struct {
 typedef struct {
     const char *model_path;
     const char *mtp_path;
+    const char *dspark_path;
     ds4_backend backend;
     int n_threads;
     uint32_t prefill_chunk;
     int mtp_draft_tokens;
     float mtp_margin;
+    float dspark_confidence_threshold;
     const char *directional_steering_file;
     const char *expert_profile_path;
     float directional_steering_attn;
@@ -266,6 +285,11 @@ int ds4_session_eval_speculative_argmax(ds4_session *s, int first_token,
                                         int max_tokens, int eos_token,
                                         int *accepted, int accepted_cap,
                                         char *err, size_t errlen);
+int ds4_session_copy_dspark_proposals(ds4_session *s, int *out, int cap);
+int ds4_session_copy_dspark_confidence(ds4_session *s, float *out, int cap);
+int ds4_session_get_spec_stats(const ds4_session *s, ds4_session_spec_stats *out);
+int ds4_session_get_kernel_stats(const ds4_session *s,
+                                 ds4_session_kernel_stats *out);
 void ds4_session_invalidate(ds4_session *s);
 void ds4_session_rewind(ds4_session *s, int pos);
 int ds4_session_pos(ds4_session *s);
@@ -274,8 +298,15 @@ int ds4_session_prefill_cap(ds4_session *s);
 int ds4_engine_routed_quant_bits(ds4_engine *e);
 bool ds4_engine_has_output_head(ds4_engine *e);
 bool ds4_engine_has_mtp(ds4_engine *e);
+bool ds4_engine_has_dspark(ds4_engine *e);
 int ds4_engine_mtp_draft_tokens(ds4_engine *e);
+int ds4_dspark_confident_prefix_length(const float *confidence_logits,
+                                       int block_size, float threshold);
 int ds4_dspark_support_validate(const char *path, int *stage_count);
+int ds4_dspark_probe_stages(const char *base_path, const char *support_path,
+                            const float *prefill_kv, unsigned prefill_count,
+                            const float *current_main_x, int input_token,
+                            float *stage_outputs, int *output_ids);
 const ds4_tokens *ds4_session_tokens(ds4_session *s);

 /* Low-level graph slice entry points used by distributed inference.  The
diff --git a/ds4_gpu.h b/ds4_gpu.h
index b58aca9..1d9f02c 100644
--- a/ds4_gpu.h
+++ b/ds4_gpu.h
@@ -19,6 +19,14 @@ extern "C" {
  */
 typedef struct ds4_gpu_tensor ds4_gpu_tensor;

+typedef struct {
+    uint64_t tiny_batch_pair_swiglu_calls;
+    uint64_t tiny_batch_activation_fallback_calls;
+    uint64_t tiny_batch_shared_swiglu_calls;
+} ds4_gpu_kernel_stats;
+
+void ds4_gpu_get_kernel_stats(ds4_gpu_kernel_stats *out);
+
 int ds4_gpu_init(void);
 void ds4_gpu_cleanup(void);

@@ -71,6 +79,7 @@ int ds4_gpu_preload_q4_expert_tables(const void *model_map, uint64_t model_size,
                                      uint32_t n_total_expert);
 int ds4_gpu_should_use_managed_kv_cache(uint64_t kv_cache_bytes, uint64_t context_bytes);
 void ds4_gpu_set_quality(bool quality);
+void ds4_gpu_set_tiny_decode_order(bool enabled);
 void ds4_gpu_set_ssd_streaming(bool enabled);
 void ds4_gpu_set_streaming_expert_cache_budget(uint32_t experts);
 void ds4_gpu_set_streaming_expert_cache_expert_bytes(uint64_t bytes);
@@ -274,6 +283,33 @@ int ds4_gpu_shared_gate_up_swiglu_q8_0_tensor(
         uint64_t                out_dim,
         const ds4_gpu_tensor *x,
         float                   clamp);
+int ds4_gpu_shared_gate_up_swiglu_q8_0_decode_order_batch_tensor(
+        ds4_gpu_tensor       *gate,
+        ds4_gpu_tensor       *up,
+        ds4_gpu_tensor       *mid,
+        const void             *model_map,
+        uint64_t                model_size,
+        uint64_t                gate_offset,
+        uint64_t                up_offset,
+        uint64_t                in_dim,
+        uint64_t                out_dim,
+        const ds4_gpu_tensor *x,
+        uint64_t                n_tok,
+        float                   clamp);
+
+int ds4_gpu_shared_gate_up_swiglu_q8_0_batch_tensor(
+        ds4_gpu_tensor       *gate,
+        ds4_gpu_tensor       *up,
+        ds4_gpu_tensor       *mid,
+        const void             *model_map,
+        uint64_t                model_size,
+        uint64_t                gate_offset,
+        uint64_t                up_offset,
+        uint64_t                in_dim,
+        uint64_t                out_dim,
+        const ds4_gpu_tensor *x,
+        uint64_t                n_tok,
+        float                   clamp);

 int ds4_gpu_matmul_f16_tensor(
         ds4_gpu_tensor       *out,
@@ -620,6 +656,7 @@ int ds4_gpu_attention_decode_raw_batch_heads_tensor(
         uint32_t                raw_cap,
         uint32_t                raw_start,
         uint32_t                window,
+        uint32_t                full_block,
         uint32_t                n_head,
         uint32_t                head_dim);

@@ -716,6 +753,20 @@ int ds4_gpu_attention_output_q8_batch_tensor(
         const ds4_gpu_tensor *heads,
         uint32_t                n_tokens);

+int ds4_gpu_attention_output_f16_batch_tensor(
+        ds4_gpu_tensor       *out,
+        ds4_gpu_tensor       *low,
+        const void             *model_map,
+        uint64_t                model_size,
+        uint64_t                out_a_offset,
+        uint64_t                out_b_offset,
+        uint64_t                group_dim,
+        uint64_t                rank,
+        uint32_t                n_groups,
+        uint64_t                out_dim,
+        const ds4_gpu_tensor *heads,
+        uint32_t                n_tokens);
+
 int ds4_gpu_attention_output_q8_batch_f16_tensor(
         ds4_gpu_tensor       *out_h,
         ds4_gpu_tensor       *low,
@@ -895,6 +946,15 @@ int ds4_gpu_hc_weighted_sum_tensor(
         uint32_t                n_embd,
         uint32_t                n_hc);

+int ds4_gpu_hc_mean_stage_tensor(
+        ds4_gpu_tensor       *out,
+        const ds4_gpu_tensor *residual_hc,
+        uint32_t                n_embd,
+        uint32_t                n_hc,
+        uint32_t                n_stages,
+        uint32_t                stage,
+        uint32_t                n_tokens);
+
 int ds4_gpu_hc_weighted_sum_split_tensor(
         ds4_gpu_tensor       *out,
         const ds4_gpu_tensor *residual_hc,
@@ -1002,6 +1062,21 @@ int ds4_gpu_shared_down_hc_expand_q8_0_tensor(
         const ds4_gpu_tensor *split,
         uint32_t                n_embd,
         uint32_t                n_hc);
+int ds4_gpu_shared_down_hc_expand_q8_0_decode_order_batch_tensor(
+        ds4_gpu_tensor       *out_hc,
+        ds4_gpu_tensor       *shared_out,
+        const void             *model_map,
+        uint64_t                model_size,
+        uint64_t                weight_offset,
+        uint64_t                in_dim,
+        uint64_t                out_dim,
+        const ds4_gpu_tensor *shared_mid,
+        const ds4_gpu_tensor *routed_out,
+        const ds4_gpu_tensor *residual_hc,
+        const ds4_gpu_tensor *split,
+        uint32_t                n_embd,
+        uint32_t                n_hc,
+        uint32_t                n_tokens);

 int ds4_gpu_matmul_q8_0_hc_expand_tensor(
         ds4_gpu_tensor       *out_hc,
diff --git a/ds4_metal.m b/ds4_metal.m
index 7e3f8bd..4de5300 100644
--- a/ds4_metal.m
+++ b/ds4_metal.m
@@ -37,6 +37,7 @@
     DS4_METAL_TENSOR_Q2_K    = 10,
     DS4_METAL_TENSOR_Q4_K    = 12,
     DS4_METAL_TENSOR_IQ2_XXS = 16,
+    DS4_METAL_TENSOR_MXFP4   = 39,
 };

 @class DS4MetalQ4ExpertTable;
@@ -86,6 +87,7 @@
 static id<MTLComputePipelineState> g_moe_mul_mv_id_q2_k_pipeline;
 static id<MTLComputePipelineState> g_moe_mul_mv_id_q2_k_sum6_pipeline;
 static id<MTLComputePipelineState> g_moe_mul_mv_id_q4_k_pipeline;
+static id<MTLComputePipelineState> g_moe_mul_mv_id_mxfp4_pipeline;
 static id<MTLComputePipelineState> g_moe_mul_mv_id_q4_k_pair_pipeline;
 static id<MTLComputePipelineState> g_moe_mul_mv_id_q4_k_pair_swiglu_pipeline;
 static id<MTLComputePipelineState> g_moe_mul_mv_id_q4_k_sum6_pipeline;
@@ -319,6 +321,7 @@ static int ds4_gpu_stream_expert_cache_entry_protected(
 static NSUInteger g_attn_out_group_ids_bytes;
 static int g_initialized;
 static int g_quality_mode;
+static int g_tiny_decode_order;
 static int g_mpp_invalid_env_reported;
 static int32_t g_routed_moe_selected_override[6];
 static uint32_t g_routed_moe_selected_override_n;
@@ -329,6 +332,18 @@ static int ds4_gpu_stream_expert_cache_entry_protected(
 static int32_t *g_moe_selected_trace_replay_ids;
 static uint64_t g_moe_selected_trace_replay_count;
 static uint64_t g_moe_selected_trace_replay_pos;
+static uint64_t g_tiny_batch_pair_swiglu_calls;
+static uint64_t g_tiny_batch_activation_fallback_calls;
+static uint64_t g_tiny_batch_shared_swiglu_calls;
+
+void ds4_gpu_get_kernel_stats(ds4_gpu_kernel_stats *out) {
+    if (!out) return;
+    out->tiny_batch_pair_swiglu_calls = g_tiny_batch_pair_swiglu_calls;
+    out->tiny_batch_activation_fallback_calls =
+        g_tiny_batch_activation_fallback_calls;
+    out->tiny_batch_shared_swiglu_calls =
+        g_tiny_batch_shared_swiglu_calls;
+}

 static double ds4_gpu_gib(uint64_t bytes);

@@ -2939,6 +2954,10 @@ void ds4_gpu_set_quality(bool quality) {
     g_quality_mode = quality ? 1 : 0;
 }

+void ds4_gpu_set_tiny_decode_order(bool enabled) {
+    g_tiny_decode_order = enabled ? 1 : 0;
+}
+
 void ds4_gpu_set_ssd_streaming(bool enabled) {
     g_ssd_streaming_mode = enabled ? 1 : 0;
     ds4_gpu_stream_expert_cache_clear_all(1);
@@ -3894,6 +3913,14 @@ static NSUInteger ds4_gpu_rms_norm_pipeline_threads(
     uint64_t nb1;
 } ds4_gpu_hc_weighted_sum_args;

+typedef struct {
+    uint32_t n_embd;
+    uint32_t n_hc;
+    uint32_t n_stages;
+    uint32_t stage;
+    uint32_t n_tokens;
+} ds4_gpu_hc_mean_stage_args;
+
 typedef struct {
     int64_t n_embd;
     int32_t n_hc;
@@ -5351,6 +5378,27 @@ int ds4_gpu_init(void) {
             return 0;
         }

+        error = nil;
+        fn = [library newFunctionWithName:@"kernel_mul_mv_id_mxfp4_f32"
+                           constantValues:moe_mv_id_constants
+                                    error:&error];
+        if (!fn) {
+            fprintf(stderr, "ds4: Metal kernel_mul_mv_id_mxfp4_f32 function not found: %s\n",
+                    [[error localizedDescription] UTF8String]);
+            g_queue = nil;
+            g_device = nil;
+            return 0;
+        }
+        g_moe_mul_mv_id_mxfp4_pipeline =
+            [g_device newComputePipelineStateWithFunction:fn error:&error];
+        if (!g_moe_mul_mv_id_mxfp4_pipeline) {
+            fprintf(stderr, "ds4: Metal kernel_mul_mv_id_mxfp4_f32 pipeline failed: %s\n",
+                    [[error localizedDescription] UTF8String]);
+            g_queue = nil;
+            g_device = nil;
+            return 0;
+        }
+
         error = nil;
         fn = [library newFunctionWithName:@"kernel_mul_mv_id_q4_K_pair_f32"
                            constantValues:moe_mv_id_constants
@@ -6738,6 +6786,7 @@ void ds4_gpu_cleanup(void) {
         g_moe_mul_mv_id_q2_k_pipeline = nil;
         g_moe_mul_mv_id_q2_k_sum6_pipeline = nil;
         g_moe_mul_mv_id_q4_k_pipeline = nil;
+        g_moe_mul_mv_id_mxfp4_pipeline = nil;
         g_moe_mul_mv_id_q4_k_pair_pipeline = nil;
         g_moe_mul_mv_id_q4_k_pair_swiglu_pipeline = nil;
         g_moe_mul_mv_id_q4_k_sum6_pipeline = nil;
@@ -12764,8 +12813,11 @@ static int ds4_gpu_matmul_q8_0_legacy_tensor(
         id<MTLCommandBuffer> cb = ds4_gpu_command_buffer(&owned);
         if (!cb) return 0;

-        if (n_tok == 1) {
+        const int tiny_decode_order = n_tok <= 8 && g_tiny_decode_order;
+        if (n_tok == 1 || tiny_decode_order) {
             ds4_gpu_q8_0_matvec_args mv_args = ds4_gpu_make_q8_0_mv_args(in_dim, out_dim);
+            mv_args.ne11 = (int32_t)n_tok;
+            mv_args.ne1 = (int32_t)n_tok;
             ds4_gpu_mv_dispatch mv_dispatch = ds4_gpu_make_q8_0_mv_dispatch();
             if (out_dim > 65536u) mv_dispatch.nsg = 8;
             mv_args.nr0 = mv_dispatch.nr0;
@@ -12781,7 +12833,7 @@ static int ds4_gpu_matmul_q8_0_legacy_tensor(
             [enc setBuffer:outbuf offset:ds4_gpu_tensor_offset(out) atIndex:3];
             [enc setThreadgroupMemoryLength:mv_dispatch.smem atIndex:0];
             [enc dispatchThreadgroups:MTLSizeMake(((NSUInteger)out_dim + (NSUInteger)mv_dispatch.nr0 - 1u) / (NSUInteger)mv_dispatch.nr0,
-                                                  1,
+                                                  (NSUInteger)n_tok,
                                                   1)
                  threadsPerThreadgroup:MTLSizeMake(32, (NSUInteger)mv_dispatch.nsg, 1)];
             ds4_gpu_end_compute_encoder(cb, enc);
@@ -13004,7 +13056,7 @@ int ds4_gpu_matmul_q8_0_f16_out_tensor(
     return 0;
 }

-int ds4_gpu_shared_gate_up_swiglu_q8_0_tensor(
+static int ds4_gpu_shared_gate_up_swiglu_q8_0_rows_tensor(
         ds4_gpu_tensor       *gate,
         ds4_gpu_tensor       *up,
         ds4_gpu_tensor       *mid,
@@ -13015,10 +13067,11 @@ int ds4_gpu_shared_gate_up_swiglu_q8_0_tensor(
         uint64_t                in_dim,
         uint64_t                out_dim,
         const ds4_gpu_tensor *x,
+        uint64_t                n_tok,
         float                   clamp) {
     if (!g_initialized && !ds4_gpu_init()) return 0;
     if (!gate || !up || !mid || !x || !model_map ||
-        (in_dim & 31u) != 0 ||
+        n_tok == 0 || n_tok > 8 || (in_dim & 31u) != 0 ||
         in_dim > UINT32_MAX || out_dim > UINT32_MAX ||
         !isfinite(clamp) || clamp < 0.0f) {
         return 0;
@@ -13029,8 +13082,8 @@ int ds4_gpu_shared_gate_up_swiglu_q8_0_tensor(
         id<MTLBuffer> gatebuf = ds4_gpu_tensor_buffer(gate);
         id<MTLBuffer> upbuf = ds4_gpu_tensor_buffer(up);
         id<MTLBuffer> midbuf = ds4_gpu_tensor_buffer(mid);
-        const uint64_t x_bytes = in_dim * sizeof(float);
-        const uint64_t out_bytes = out_dim * sizeof(float);
+        const uint64_t x_bytes = n_tok * in_dim * sizeof(float);
+        const uint64_t out_bytes = n_tok * out_dim * sizeof(float);
         if (!xbuf || !gatebuf || !upbuf || !midbuf ||
             ds4_gpu_tensor_bytes(x) < x_bytes ||
             ds4_gpu_tensor_bytes(gate) < out_bytes ||
@@ -13058,6 +13111,8 @@ int ds4_gpu_shared_gate_up_swiglu_q8_0_tensor(
         if (!gate_wbuf || !up_wbuf) return 0;

         ds4_gpu_q8_0_matvec_args args = ds4_gpu_make_q8_0_mv_args(in_dim, out_dim);
+        args.ne11 = (int32_t)n_tok;
+        args.ne1 = (int32_t)n_tok;
         ds4_gpu_mv_dispatch mv_dispatch = ds4_gpu_make_q8_0_mv_dispatch();
         args.nr0 = mv_dispatch.nr0;
         id<MTLComputePipelineState> pipeline =
@@ -13082,7 +13137,7 @@ int ds4_gpu_shared_gate_up_swiglu_q8_0_tensor(
         [enc setThreadgroupMemoryLength:2u * mv_dispatch.smem atIndex:0];
         [enc dispatchThreadgroups:MTLSizeMake(((NSUInteger)out_dim + (NSUInteger)mv_dispatch.nr0 - 1u) /
                                                   (NSUInteger)mv_dispatch.nr0,
-                                              1,
+                                              (NSUInteger)n_tok,
                                               1)
              threadsPerThreadgroup:MTLSizeMake(32, (NSUInteger)mv_dispatch.nsg, 1)];
         ds4_gpu_end_compute_encoder(cb, enc);
@@ -13095,6 +13150,137 @@ int ds4_gpu_shared_gate_up_swiglu_q8_0_tensor(
     return 1;
 }

+int ds4_gpu_shared_gate_up_swiglu_q8_0_tensor(
+        ds4_gpu_tensor       *gate,
+        ds4_gpu_tensor       *up,
+        ds4_gpu_tensor       *mid,
+        const void             *model_map,
+        uint64_t                model_size,
+        uint64_t                gate_offset,
+        uint64_t                up_offset,
+        uint64_t                in_dim,
+        uint64_t                out_dim,
+        const ds4_gpu_tensor *x,
+        float                   clamp) {
+    return ds4_gpu_shared_gate_up_swiglu_q8_0_rows_tensor(
+        gate, up, mid, model_map, model_size, gate_offset, up_offset,
+        in_dim, out_dim, x, 1, clamp);
+}
+
+int ds4_gpu_shared_gate_up_swiglu_q8_0_decode_order_batch_tensor(
+        ds4_gpu_tensor       *gate,
+        ds4_gpu_tensor       *up,
+        ds4_gpu_tensor       *mid,
+        const void             *model_map,
+        uint64_t                model_size,
+        uint64_t                gate_offset,
+        uint64_t                up_offset,
+        uint64_t                in_dim,
+        uint64_t                out_dim,
+        const ds4_gpu_tensor *x,
+        uint64_t                n_tok,
+        float                   clamp) {
+    if (n_tok < 2) return 0;
+    const int ok = ds4_gpu_shared_gate_up_swiglu_q8_0_rows_tensor(
+        gate, up, mid, model_map, model_size, gate_offset, up_offset,
+        in_dim, out_dim, x, n_tok, clamp);
+    if (ok) g_tiny_batch_shared_swiglu_calls++;
+    return ok;
+}
+
+static const char *ds4_gpu_shared_gate_up_swiglu_ext_name(int16_t r1ptg) {
+    switch (r1ptg) {
+    case 2: return "kernel_dsv4_shared_gate_up_swiglu_q8_0_r1_2";
+    case 3: return "kernel_dsv4_shared_gate_up_swiglu_q8_0_r1_3";
+    case 4: return "kernel_dsv4_shared_gate_up_swiglu_q8_0_r1_4";
+    case 5: return "kernel_dsv4_shared_gate_up_swiglu_q8_0_r1_5";
+    default: return NULL;
+    }
+}
+
+int ds4_gpu_shared_gate_up_swiglu_q8_0_batch_tensor(
+        ds4_gpu_tensor       *gate,
+        ds4_gpu_tensor       *up,
+        ds4_gpu_tensor       *mid,
+        const void             *model_map,
+        uint64_t                model_size,
+        uint64_t                gate_offset,
+        uint64_t                up_offset,
+        uint64_t                in_dim,
+        uint64_t                out_dim,
+        const ds4_gpu_tensor *x,
+        uint64_t                n_tok,
+        float                   clamp) {
+    if (!g_initialized && !ds4_gpu_init()) return 0;
+    if (!gate || !up || !mid || !x || !model_map ||
+        n_tok < 2 || n_tok > 5 || (in_dim % 128u) != 0 ||
+        in_dim > UINT32_MAX || out_dim > UINT32_MAX ||
+        !isfinite(clamp) || clamp < 0.0f) return 0;
+
+    @autoreleasepool {
+        id<MTLBuffer> xbuf = ds4_gpu_tensor_buffer(x);
+        id<MTLBuffer> gatebuf = ds4_gpu_tensor_buffer(gate);
+        id<MTLBuffer> upbuf = ds4_gpu_tensor_buffer(up);
+        id<MTLBuffer> midbuf = ds4_gpu_tensor_buffer(mid);
+        const uint64_t x_bytes = n_tok * in_dim * sizeof(float);
+        const uint64_t out_bytes = n_tok * out_dim * sizeof(float);
+        if (!xbuf || !gatebuf || !upbuf || !midbuf ||
+            ds4_gpu_tensor_bytes(x) < x_bytes ||
+            ds4_gpu_tensor_bytes(gate) < out_bytes ||
+            ds4_gpu_tensor_bytes(up) < out_bytes ||
+            ds4_gpu_tensor_bytes(mid) < out_bytes) return 0;
+
+        const uint64_t row_bytes = (in_dim / 32u) * 34u;
+        const uint64_t weight_bytes = out_dim * row_bytes;
+        if (gate_offset > model_size || weight_bytes > model_size - gate_offset ||
+            up_offset > model_size || weight_bytes > model_size - up_offset) return 0;
+
+        uint64_t gate_inner = 0;
+        uint64_t up_inner = 0;
+        id<MTLBuffer> gate_wbuf = ds4_gpu_wrap_model_range(
+            model_map, model_size, gate_offset, weight_bytes, &gate_inner);
+        id<MTLBuffer> up_wbuf = ds4_gpu_wrap_model_range(
+            model_map, model_size, up_offset, weight_bytes, &up_inner);
+        if (!gate_wbuf || !up_wbuf) return 0;
+
+        const int16_t nsg = 2;
+        const int16_t nxpsg = ds4_gpu_mv_ext_nxpsg(in_dim, n_tok);
+        const int16_t r1ptg = ds4_gpu_mv_ext_r1ptg(n_tok);
+        const char *name = ds4_gpu_shared_gate_up_swiglu_ext_name(r1ptg);
+        id<MTLComputePipelineState> pipeline = name
+            ? ds4_gpu_get_mul_mv_ext_pipeline(name, nsg, nxpsg) : nil;
+        if (!pipeline) return 0;
+
+        const int16_t nypsg = 32 / nxpsg;
+        const uint64_t r0ptg = (uint64_t)nypsg * (uint64_t)nsg;
+        ds4_gpu_mul_mv_ext_args args = ds4_gpu_make_mv_ext_args(
+            in_dim, out_dim, n_tok, 34, row_bytes);
+        int owned = 0;
+        id<MTLCommandBuffer> cb = ds4_gpu_command_buffer(&owned);
+        if (!cb) return 0;
+        id<MTLComputeCommandEncoder> enc = ds4_gpu_compute_encoder(cb);
+        [enc setComputePipelineState:pipeline];
+        [enc setBytes:&args length:sizeof(args) atIndex:0];
+        [enc setBuffer:gate_wbuf offset:(NSUInteger)gate_inner atIndex:1];
+        [enc setBuffer:up_wbuf offset:(NSUInteger)up_inner atIndex:2];
+        [enc setBuffer:xbuf offset:ds4_gpu_tensor_offset(x) atIndex:3];
+        [enc setBuffer:gatebuf offset:ds4_gpu_tensor_offset(gate) atIndex:4];
+        [enc setBuffer:upbuf offset:ds4_gpu_tensor_offset(up) atIndex:5];
+        [enc setBuffer:midbuf offset:ds4_gpu_tensor_offset(mid) atIndex:6];
+        [enc setBytes:&clamp length:sizeof(clamp) atIndex:7];
+        [enc dispatchThreadgroups:MTLSizeMake(
+             ((NSUInteger)out_dim + (NSUInteger)r0ptg - 1u) / (NSUInteger)r0ptg,
+             ((NSUInteger)n_tok + (NSUInteger)r1ptg - 1u) / (NSUInteger)r1ptg,
+             1)
+             threadsPerThreadgroup:MTLSizeMake(32, (NSUInteger)nsg, 1)];
+        ds4_gpu_end_compute_encoder(cb, enc);
+        if (!ds4_gpu_finish_command_buffer(cb, owned,
+                                            "shared expert fused batch gate/up")) return 0;
+        g_tiny_batch_shared_swiglu_calls++;
+    }
+    return 1;
+}
+
 int ds4_gpu_matmul_f16_tensor(
         ds4_gpu_tensor       *out,
         const void             *model_map,
@@ -13134,8 +13320,11 @@ int ds4_gpu_matmul_f16_tensor(
         id<MTLCommandBuffer> cb = ds4_gpu_command_buffer(&owned);
         if (!cb) return 0;

-        if (n_tok == 1) {
+        const int tiny_decode_order = n_tok <= 8 && g_tiny_decode_order;
+        if (n_tok == 1 || tiny_decode_order) {
             ds4_gpu_f16_matvec_args mv_args = ds4_gpu_make_f16_mv_args(in_dim, out_dim);
+            mv_args.ne11 = (int32_t)n_tok;
+            mv_args.ne1 = (int32_t)n_tok;
             ds4_gpu_mv_dispatch mv_dispatch =
                 ds4_gpu_make_plain_mv_dispatch(in_dim, 0);
             if (!g_quality_mode && (out_dim == 512u || out_dim == 1024u) && in_dim >= 4096u) {
@@ -13157,7 +13346,7 @@ int ds4_gpu_matmul_f16_tensor(
                 [enc setThreadgroupMemoryLength:mv_dispatch.smem atIndex:0];
             }
             [enc dispatchThreadgroups:MTLSizeMake(((NSUInteger)out_dim + (NSUInteger)mv_dispatch.nr0 - 1u) / (NSUInteger)mv_dispatch.nr0,
-                                                  1,
+                                                  (NSUInteger)n_tok,
                                                   1)
                  threadsPerThreadgroup:MTLSizeMake(32, (NSUInteger)mv_dispatch.nsg, 1)];
             ds4_gpu_end_compute_encoder(cb, enc);
@@ -13365,13 +13554,14 @@ int ds4_gpu_matmul_f32_tensor(
         const ds4_gpu_tensor *x,
         uint64_t                n_tok) {
     if (!g_initialized && !ds4_gpu_init()) return 0;
-    if (in_dim > UINT32_MAX || out_dim > UINT32_MAX || n_tok > UINT32_MAX || n_tok != 1) return 0;
+    if (in_dim > UINT32_MAX || out_dim > UINT32_MAX ||
+        n_tok == 0 || n_tok > UINT32_MAX) return 0;

     @autoreleasepool {
         id<MTLBuffer> xbuf = ds4_gpu_tensor_buffer(x);
         id<MTLBuffer> outbuf = ds4_gpu_tensor_buffer(out);
-        const uint64_t x_bytes = in_dim * sizeof(float);
-        const uint64_t out_bytes = out_dim * sizeof(float);
+        const uint64_t x_bytes = n_tok * in_dim * sizeof(float);
+        const uint64_t out_bytes = n_tok * out_dim * sizeof(float);
         if (!xbuf || !outbuf ||
             ds4_gpu_tensor_bytes(x) < x_bytes ||
             ds4_gpu_tensor_bytes(out) < out_bytes) {
@@ -13401,22 +13591,30 @@ int ds4_gpu_matmul_f32_tensor(
             ds4_gpu_get_mul_mv_pipeline(mv_dispatch.function_name, mv_dispatch.nsg);
         if (!pipeline) return 0;

-        id<MTLComputeCommandEncoder> enc = ds4_gpu_compute_encoder(cb);
-        [enc setComputePipelineState:pipeline];
-        [enc setBytes:&mv_args length:sizeof(mv_args) atIndex:0];
-        [enc setBuffer:wbuf offset:(NSUInteger)inner_offset atIndex:1];
-        [enc setBuffer:xbuf offset:ds4_gpu_tensor_offset(x) atIndex:2];
-        [enc setBuffer:outbuf offset:ds4_gpu_tensor_offset(out) atIndex:3];
-        if (mv_dispatch.smem) {
-            [enc setThreadgroupMemoryLength:mv_dispatch.smem atIndex:0];
+        for (uint64_t token = 0; token < n_tok; token++) {
+            id<MTLComputeCommandEncoder> enc = ds4_gpu_compute_encoder(cb);
+            [enc setComputePipelineState:pipeline];
+            [enc setBytes:&mv_args length:sizeof(mv_args) atIndex:0];
+            [enc setBuffer:wbuf offset:(NSUInteger)inner_offset atIndex:1];
+            [enc setBuffer:xbuf
+                    offset:ds4_gpu_tensor_offset(x) +
+                           (NSUInteger)(token * in_dim * sizeof(float))
+                   atIndex:2];
+            [enc setBuffer:outbuf
+                    offset:ds4_gpu_tensor_offset(out) +
+                           (NSUInteger)(token * out_dim * sizeof(float))
+                   atIndex:3];
+            if (mv_dispatch.smem) {
+                [enc setThreadgroupMemoryLength:mv_dispatch.smem atIndex:0];
+            }
+            [enc dispatchThreadgroups:MTLSizeMake(((NSUInteger)out_dim + (NSUInteger)mv_dispatch.nr0 - 1u) / (NSUInteger)mv_dispatch.nr0,
+                                                  1,
+                                                  1)
+                 threadsPerThreadgroup:MTLSizeMake(32, (NSUInteger)mv_dispatch.nsg, 1)];
+            ds4_gpu_end_compute_encoder(cb, enc);
         }
-        [enc dispatchThreadgroups:MTLSizeMake(((NSUInteger)out_dim + (NSUInteger)mv_dispatch.nr0 - 1u) / (NSUInteger)mv_dispatch.nr0,
-                                              1,
-                                              1)
-             threadsPerThreadgroup:MTLSizeMake(32, (NSUInteger)mv_dispatch.nsg, 1)];
-        ds4_gpu_end_compute_encoder(cb, enc);

-        if (!ds4_gpu_finish_command_buffer(cb, owned, "F32 tensor matvec")) return 0;
+        if (!ds4_gpu_finish_command_buffer(cb, owned, "F32 tensor matmul")) return 0;
     }

     return 1;
@@ -16389,6 +16587,96 @@ int ds4_gpu_attention_output_q8_batch_tensor(
     }
 }

+int ds4_gpu_attention_output_f16_batch_tensor(
+        ds4_gpu_tensor       *out,
+        ds4_gpu_tensor       *low,
+        const void             *model_map,
+        uint64_t                model_size,
+        uint64_t                out_a_offset,
+        uint64_t                out_b_offset,
+        uint64_t                group_dim,
+        uint64_t                rank,
+        uint32_t                n_groups,
+        uint64_t                out_dim,
+        const ds4_gpu_tensor *heads,
+        uint32_t                n_tokens) {
+    if (!g_initialized && !ds4_gpu_init()) return 0;
+    if (!out || !low || !heads || !model_map || group_dim == 0 || rank == 0 ||
+        n_groups == 0 || out_dim == 0 || n_tokens == 0 ||
+        group_dim > UINT32_MAX || rank > UINT32_MAX || out_dim > UINT32_MAX)
+        return 0;
+
+    @autoreleasepool {
+        const uint64_t low_dim = (uint64_t)n_groups * rank;
+        const uint64_t row_a_bytes = group_dim * sizeof(uint16_t);
+        const uint64_t out_a_bytes = (uint64_t)n_groups * rank * row_a_bytes;
+        if (out_a_offset > model_size || out_a_bytes > model_size - out_a_offset)
+            return 0;
+        if (ds4_gpu_tensor_bytes(heads) <
+                (uint64_t)n_tokens * n_groups * group_dim * sizeof(float) ||
+            ds4_gpu_tensor_bytes(low) <
+                (uint64_t)n_tokens * low_dim * sizeof(float) ||
+            ds4_gpu_tensor_bytes(out) <
+                (uint64_t)n_tokens * out_dim * sizeof(float))
+            return 0;
+
+        uint64_t out_a_inner = 0;
+        id<MTLBuffer> out_a_buf = ds4_gpu_wrap_model_range(
+                model_map, model_size, out_a_offset, out_a_bytes, &out_a_inner);
+        if (!out_a_buf) return 0;
+        const NSUInteger ids_bytes =
+            (NSUInteger)n_tokens * (NSUInteger)n_groups * sizeof(int32_t);
+        id<MTLBuffer> ids_buffer =
+            ds4_gpu_new_transient_buffer(ids_bytes, "attention F16 output group ids");
+        if (!ids_buffer) return 0;
+        int32_t *ids = (int32_t *)[ids_buffer contents];
+        for (uint32_t token = 0; token < n_tokens; token++)
+            for (uint32_t group = 0; group < n_groups; group++)
+                ids[(uint64_t)token * n_groups + group] = (int32_t)group;
+
+        const bool had_batch = g_batch_cb != nil;
+        if (!had_batch && ds4_gpu_begin_commands() == 0) return 0;
+        int owned = 0;
+        id<MTLCommandBuffer> cb = ds4_gpu_command_buffer(&owned);
+        bool ok = cb != nil && !owned;
+        ds4_gpu_mul_mv_id_args args = {
+            .nei0 = (int32_t)n_groups,
+            .nei1 = (int32_t)n_tokens,
+            .nbi1 = (uint64_t)n_groups * sizeof(int32_t),
+            .ne00 = (int32_t)group_dim,
+            .ne01 = (int32_t)rank,
+            .ne02 = (int32_t)n_groups,
+            .nb00 = sizeof(uint16_t),
+            .nb01 = row_a_bytes,
+            .nb02 = (uint64_t)rank * row_a_bytes,
+            .ne10 = (int32_t)group_dim,
+            .ne11 = (int32_t)n_groups,
+            .ne12 = (int32_t)n_tokens,
+            .ne13 = 1,
+            .nb10 = sizeof(float),
+            .nb11 = group_dim * sizeof(float),
+            .nb12 = (uint64_t)n_groups * group_dim * sizeof(float),
+            .ne0 = (int32_t)rank,
+            .ne1 = (int32_t)n_groups,
+            .nb1 = rank * sizeof(float),
+            .nr0 = 2,
+        };
+        id<MTLComputePipelineState> pipeline =
+            ds4_gpu_get_mul_mv_pipeline("kernel_mul_mv_id_f16_f32", 4);
+        if (ok) ok = pipeline != nil && ds4_gpu_encode_mul_mv_id(
+                cb, pipeline, &args,
+                out_a_buf, (NSUInteger)out_a_inner,
+                ds4_gpu_tensor_buffer(heads), ds4_gpu_tensor_offset(heads),
+                ds4_gpu_tensor_buffer(low), ds4_gpu_tensor_offset(low),
+                ids_buffer, 0, 32u * 2u * sizeof(float), 4, true) != 0;
+        if (ok) ok = ds4_gpu_matmul_f16_tensor(
+                out, model_map, model_size, out_b_offset,
+                low_dim, out_dim, low, n_tokens) != 0;
+        if (!had_batch) ok = ds4_gpu_end_commands() != 0 && ok;
+        return ok ? 1 : 0;
+    }
+}
+
 int ds4_gpu_attention_output_q8_batch_f16_tensor(
         ds4_gpu_tensor       *out_h,
         ds4_gpu_tensor       *low,
@@ -18432,6 +18720,7 @@ static int ds4_gpu_encode_flash_attention_decode_raw_batch_heads(
         uint32_t               raw_cap,
         uint32_t               raw_start,
         uint32_t               window,
+        uint32_t               full_block,
         uint32_t               n_head,
         uint32_t               head_dim) {
     if (head_dim != 512 || n_head == 0 || n_tokens == 0 ||
@@ -18528,11 +18817,15 @@ static int ds4_gpu_encode_flash_attention_decode_raw_batch_heads(
         return 0;
     }

-    ds4_gpu_fill_raw_decode_batch_mask((uint16_t *)[mask_buffer contents],
-                                         n_tokens,
-                                         n_raw,
-                                         pos0,
-                                         window);
+    if (full_block) {
+        memset([mask_buffer contents], 0, mask_bytes);
+    } else {
+        ds4_gpu_fill_raw_decode_batch_mask((uint16_t *)[mask_buffer contents],
+                                             n_tokens,
+                                             n_raw,
+                                             pos0,
+                                             window);
+    }

     id<MTLComputePipelineState> pad_pipeline = nil;
     if (has_kvpad) {
@@ -18693,6 +18986,7 @@ static int ds4_gpu_encode_flash_attention_decode_mixed_batch_heads(
                                                                        raw_cap,
                                                                        raw_start,
                                                                        window,
+                                                                       0,
                                                                        n_head,
                                                                        head_dim);
     }
@@ -19015,6 +19309,7 @@ int ds4_gpu_attention_decode_raw_batch_heads_tensor(
         uint32_t                raw_cap,
         uint32_t                raw_start,
         uint32_t                window,
+        uint32_t                full_block,
         uint32_t                n_head,
         uint32_t                head_dim) {
     if (!g_initialized && !ds4_gpu_init()) return 0;
@@ -19052,6 +19347,7 @@ int ds4_gpu_attention_decode_raw_batch_heads_tensor(
                                                                      raw_cap,
                                                                      raw_start,
                                                                      window,
+                                                                     full_block,
                                                                      n_head,
                                                                      head_dim)) {
             return 0;
@@ -19965,6 +20261,7 @@ static ds4_gpu_mul_mm_id_args ds4_gpu_make_mul_mm_id_args_src1_size(
 static uint32_t ds4_gpu_routed_mv_nr0(uint32_t type) {
     switch (type) {
     case DS4_METAL_TENSOR_Q4_K:    return 2;
+    case DS4_METAL_TENSOR_MXFP4:   return 2;
     case DS4_METAL_TENSOR_Q2_K:
     case DS4_METAL_TENSOR_IQ2_XXS: return 4;
     default:                       return 0;
@@ -19976,6 +20273,7 @@ static uint32_t ds4_gpu_routed_mv_nr0(uint32_t type) {
     case DS4_METAL_TENSOR_IQ2_XXS: return "iq2_xxs";
     case DS4_METAL_TENSOR_Q2_K:    return "q2_k";
     case DS4_METAL_TENSOR_Q4_K:    return "q4_k";
+    case DS4_METAL_TENSOR_MXFP4:   return "mxfp4";
     default:                       return "unknown";
     }
 }
@@ -19984,6 +20282,7 @@ static NSUInteger ds4_gpu_routed_mv_smem(uint32_t type) {
     if (type == DS4_METAL_TENSOR_IQ2_XXS) {
         return 256u * sizeof(uint64_t) + 128u * sizeof(uint8_t);
     }
+    if (type == DS4_METAL_TENSOR_MXFP4) return 32u * sizeof(float);
     return 0;
 }

@@ -19992,6 +20291,7 @@ static NSUInteger ds4_gpu_routed_mv_smem(uint32_t type) {
     case DS4_METAL_TENSOR_IQ2_XXS: return g_moe_mul_mv_id_iq2_xxs_pipeline;
     case DS4_METAL_TENSOR_Q2_K:    return g_moe_mul_mv_id_q2_k_pipeline;
     case DS4_METAL_TENSOR_Q4_K:    return g_moe_mul_mv_id_q4_k_pipeline;
+    case DS4_METAL_TENSOR_MXFP4:   return g_moe_mul_mv_id_mxfp4_pipeline;
     default:                       return nil;
     }
 }
@@ -22320,6 +22620,54 @@ int ds4_gpu_router_select_batch_tensor(
         if (!had_batch && ds4_gpu_begin_commands() == 0) return 0;
         int owned = 0;
         id<MTLCommandBuffer> cb = ds4_gpu_command_buffer(&owned);
+        if (g_tiny_decode_order && n_tokens <= 8) {
+            int ok = cb != nil;
+            for (uint32_t t = 0; ok && t < n_tokens; t++) {
+                ds4_gpu_tensor *selected_view = ds4_gpu_tensor_view(
+                        selected,
+                        (uint64_t)t * n_expert_used * sizeof(int32_t),
+                        (uint64_t)n_expert_used * sizeof(int32_t));
+                ds4_gpu_tensor *weights_view = ds4_gpu_tensor_view(
+                        weights,
+                        (uint64_t)t * n_expert_used * sizeof(float),
+                        (uint64_t)n_expert_used * sizeof(float));
+                ds4_gpu_tensor *probs_view = ds4_gpu_tensor_view(
+                        probs,
+                        (uint64_t)t * n_expert * sizeof(float),
+                        (uint64_t)n_expert * sizeof(float));
+                ok = selected_view && weights_view && probs_view &&
+                     ds4_gpu_encode_router_select(
+                            cb,
+                            selected_view,
+                            weights_view,
+                            probs_view,
+                            logitsbuf,
+                            ds4_gpu_tensor_offset(logits) +
+                                (NSUInteger)t * n_expert * sizeof(float),
+                            biasbuf,
+                            bias_set_offset,
+                            hashbuf,
+                            hash_set_offset,
+                            tokensbuf,
+                            ds4_gpu_tensor_offset(tokens) +
+                                (NSUInteger)t * sizeof(int32_t),
+                            NULL,
+                            hash_rows,
+                            1,
+                            n_expert,
+                            n_expert_used,
+                            expert_weight_scale,
+                            has_bias && !hash_mode,
+                            hash_mode);
+                ds4_gpu_tensor_free(probs_view);
+                ds4_gpu_tensor_free(weights_view);
+                ds4_gpu_tensor_free(selected_view);
+            }
+            if (!had_batch) {
+                ok = ds4_gpu_end_commands() != 0 && ok;
+            }
+            return ok;
+        }
         int ok = cb &&
                  ds4_gpu_encode_router_select(cb,
                                                       selected,
@@ -24920,6 +25268,13 @@ int ds4_gpu_routed_moe_batch_tensor(
             !use_mm_id &&
             ((gate_type == DS4_METAL_TENSOR_IQ2_XXS && g_moe_mul_mv_id_iq2_xxs_pair_pipeline) ||
              (gate_type == DS4_METAL_TENSOR_Q4_K && g_moe_mul_mv_id_q4_k_pair_pipeline));
+        const bool use_tiny_pair_swiglu =
+            use_tiny_pair_mv &&
+            gate_type == DS4_METAL_TENSOR_IQ2_XXS &&
+            g_moe_mul_mv_id_iq2_xxs_pair_swiglu_pipeline != nil &&
+            getenv("DS4_METAL_MOE_WRITE_CLAMPED_ACT") == NULL &&
+            getenv("DS4_METAL_DISABLE_ROUTED_PAIR_SWIGLU_FUSION") == NULL &&
+            getenv("DS4_METAL_DISABLE_TINY_BATCH_PAIR_SWIGLU") == NULL;
         ds4_gpu_mul_mm_id_map_args gate_map_args = { 0 };
         ds4_gpu_mul_mm_id_args gate_mm_args = { 0 };
         ds4_gpu_mul_mm_id_args down_mm_args = { 0 };
@@ -25350,6 +25705,42 @@ int ds4_gpu_routed_moe_batch_tensor(
                                                    ds4_gpu_tensor_offset(up));
                 DS4_METAL_PROFILE_MOE_STAGE("up");
             }
+        } else if (use_tiny_pair_swiglu) {
+            ds4_gpu_dsv4_moe_swiglu_weight_args act_args = {
+                .width = expert_mid_dim,
+                .rows = pair_rows,
+                .gate_row_stride = (uint64_t)expert_mid_dim * sizeof(float),
+                .up_row_stride = (uint64_t)expert_mid_dim * sizeof(float),
+                .mid_row_stride = (uint64_t)expert_mid_dim * sizeof(float),
+                .weight_stride = sizeof(float),
+                .write_clamped = 0,
+                .clamp_value = clamp,
+            };
+            ok = ds4_gpu_encode_mul_mv_id_pair_swiglu(
+                    cb,
+                    g_moe_mul_mv_id_iq2_xxs_pair_swiglu_pipeline,
+                    &gate_args,
+                    &act_args,
+                    gate_buf,
+                    (NSUInteger)gate_inner,
+                    up_buf,
+                    (NSUInteger)up_inner,
+                    xbuf,
+                    ds4_gpu_tensor_offset(x),
+                    gatebuf,
+                    ds4_gpu_tensor_offset(gate),
+                    upbuf,
+                    ds4_gpu_tensor_offset(up),
+                    midbuf,
+                    ds4_gpu_tensor_offset(mid),
+                    selectedbuf,
+                    ds4_gpu_tensor_offset(selected),
+                    weightsbuf,
+                    ds4_gpu_tensor_offset(weights),
+                    gate_smem,
+                    2,
+                    false);
+            if (ok) g_tiny_batch_pair_swiglu_calls++;
         } else if (use_tiny_pair_mv) {
             id<MTLComputePipelineState> pair_pipeline =
                 gate_type == DS4_METAL_TENSOR_IQ2_XXS ?
@@ -25416,7 +25807,10 @@ int ds4_gpu_routed_moe_batch_tensor(
             /* The table pair kernel already wrote weighted SwiGLU rows into mid. */
         } else if (ok && use_mm_id_pair_swiglu) {
             /* The fused batch mm_id pair kernel already wrote weighted f16 SwiGLU rows into mid. */
+        } else if (ok && use_tiny_pair_swiglu) {
+            /* The fused tiny-batch pair kernel already wrote weighted F32 SwiGLU rows into mid. */
         } else if (ok && use_fused_activation) {
+            if (use_tiny_pair_mv) g_tiny_batch_activation_fallback_calls++;
             ok = ds4_gpu_encode_moe_swiglu_weight(cb,
                                                     gatebuf,
                                                     ds4_gpu_tensor_offset(gate),
@@ -25822,6 +26216,56 @@ int ds4_gpu_hc_weighted_sum_tensor(
                                              "HC weighted sum");
 }

+int ds4_gpu_hc_mean_stage_tensor(
+        ds4_gpu_tensor       *out,
+        const ds4_gpu_tensor *residual_hc,
+        uint32_t                n_embd,
+        uint32_t                n_hc,
+        uint32_t                n_stages,
+        uint32_t                stage,
+        uint32_t                n_tokens) {
+    if (!g_initialized && !ds4_gpu_init()) return 0;
+    if (!out || !residual_hc || n_embd == 0 || n_hc == 0 ||
+        n_stages == 0 || stage >= n_stages || n_tokens == 0) return 0;
+
+    @autoreleasepool {
+        id<MTLBuffer> xbuf = ds4_gpu_tensor_buffer(residual_hc);
+        id<MTLBuffer> outbuf = ds4_gpu_tensor_buffer(out);
+        const uint64_t input_values = (uint64_t)n_tokens * n_hc * n_embd;
+        const uint64_t output_values = (uint64_t)n_tokens * n_stages * n_embd;
+        if (!xbuf || !outbuf ||
+            ds4_gpu_tensor_bytes(residual_hc) < input_values * sizeof(float) ||
+            ds4_gpu_tensor_bytes(out) < output_values * sizeof(float)) return 0;
+
+        id<MTLComputePipelineState> pipeline =
+            ds4_gpu_hot_pipeline(ds4_gpu_get_pipeline("kernel_dsv4_hc_mean_stage"),
+                                 "kernel_dsv4_hc_mean_stage");
+        if (!pipeline) return 0;
+        ds4_gpu_hc_mean_stage_args args = {
+            .n_embd = n_embd,
+            .n_hc = n_hc,
+            .n_stages = n_stages,
+            .stage = stage,
+            .n_tokens = n_tokens,
+        };
+        const NSUInteger n_elem = (NSUInteger)n_tokens * n_embd;
+        const NSUInteger nth = MIN((NSUInteger)256, n_elem);
+        int owned = 0;
+        id<MTLCommandBuffer> cb = ds4_gpu_command_buffer(&owned);
+        if (!cb) return 0;
+        id<MTLComputeCommandEncoder> enc = ds4_gpu_compute_encoder(cb);
+        [enc setComputePipelineState:pipeline];
+        [enc setBytes:&args length:sizeof(args) atIndex:0];
+        [enc setBuffer:xbuf offset:ds4_gpu_tensor_offset(residual_hc) atIndex:1];
+        [enc setBuffer:outbuf offset:ds4_gpu_tensor_offset(out) atIndex:2];
+        [enc dispatchThreads:MTLSizeMake(n_elem, 1, 1)
+              threadsPerThreadgroup:MTLSizeMake(nth, 1, 1)];
+        ds4_gpu_end_compute_encoder(cb, enc);
+        if (!ds4_gpu_finish_command_buffer(cb, owned, "HC mean stage")) return 0;
+    }
+    return 1;
+}
+
 int ds4_gpu_hc_weighted_sum_split_tensor(
         ds4_gpu_tensor       *out,
         const ds4_gpu_tensor *residual_hc,
@@ -26408,8 +26852,8 @@ int ds4_gpu_hc_expand_split_tensor(
             .nb_res2 = (uint64_t)n_hc * n_embd * sizeof(float),
             .nb_post0 = sizeof(float),
             .nb_post1 = mix_hc * sizeof(float),
-            .nb_comb0 = sizeof(float),
-            .nb_comb1 = (uint64_t)n_hc * sizeof(float),
+            .nb_comb0 = (uint64_t)n_hc * sizeof(float),
+            .nb_comb1 = sizeof(float),
             .nb_comb2 = mix_hc * sizeof(float),
             .nb0 = sizeof(float),
             .nb1 = (uint64_t)n_embd * sizeof(float),
@@ -26529,8 +26973,8 @@ int ds4_gpu_hc_expand_add_split_tensor(
             .nb_res2 = (uint64_t)n_hc * n_embd * sizeof(float),
             .nb_post0 = sizeof(float),
             .nb_post1 = mix_hc * sizeof(float),
-            .nb_comb0 = sizeof(float),
-            .nb_comb1 = (uint64_t)n_hc * sizeof(float),
+            .nb_comb0 = (uint64_t)n_hc * sizeof(float),
+            .nb_comb1 = sizeof(float),
             .nb_comb2 = mix_hc * sizeof(float),
             .nb0 = sizeof(float),
             .nb1 = (uint64_t)n_embd * sizeof(float),
@@ -26583,7 +27027,7 @@ int ds4_gpu_hc_expand_add_split_half_add_tensor(
     return 0;
 }

-int ds4_gpu_shared_down_hc_expand_q8_0_tensor(
+static int ds4_gpu_shared_down_hc_expand_q8_0_rows_tensor(
         ds4_gpu_tensor       *out_hc,
         ds4_gpu_tensor       *shared_out,
         const void             *model_map,
@@ -26596,11 +27040,13 @@ int ds4_gpu_shared_down_hc_expand_q8_0_tensor(
         const ds4_gpu_tensor *residual_hc,
         const ds4_gpu_tensor *split,
         uint32_t                n_embd,
-        uint32_t                n_hc) {
+        uint32_t                n_hc,
+        uint32_t                n_tokens) {
     if (!g_initialized && !ds4_gpu_init()) return 0;
     if (!out_hc || !shared_out || !model_map || !shared_mid || !routed_out ||
         !residual_hc || !split || n_embd == 0 || n_hc == 0 ||
-        n_hc != 4 || out_dim != n_embd || (in_dim & 31u) != 0 ||
+        n_hc != 4 || n_tokens == 0 || n_tokens > 8 ||
+        out_dim != n_embd || (in_dim & 31u) != 0 ||
         in_dim > UINT32_MAX || out_dim > UINT32_MAX) {
         return 0;
     }
@@ -26615,11 +27061,11 @@ int ds4_gpu_shared_down_hc_expand_q8_0_tensor(

         const uint64_t row_bytes = (in_dim / 32u) * 34u;
         const uint64_t weight_bytes = out_dim * row_bytes;
-        const uint64_t shared_mid_bytes = in_dim * sizeof(float);
-        const uint64_t embd_bytes = out_dim * sizeof(float);
-        const uint64_t hc_bytes = (uint64_t)n_hc * n_embd * sizeof(float);
+        const uint64_t shared_mid_bytes = (uint64_t)n_tokens * in_dim * sizeof(float);
+        const uint64_t embd_bytes = (uint64_t)n_tokens * out_dim * sizeof(float);
+        const uint64_t hc_bytes = (uint64_t)n_tokens * n_hc * n_embd * sizeof(float);
         const uint64_t mix_hc = 2ull * n_hc + (uint64_t)n_hc * n_hc;
-        const uint64_t split_bytes = mix_hc * sizeof(float);
+        const uint64_t split_bytes = (uint64_t)n_tokens * mix_hc * sizeof(float);

         if (weight_offset > model_size || weight_bytes > model_size - weight_offset) {
             fprintf(stderr, "ds4: Metal shared-down HC fusion weight range is outside the mapped model\n");
@@ -26643,13 +27089,15 @@ int ds4_gpu_shared_down_hc_expand_q8_0_tensor(
         if (!wbuf) return 0;

         ds4_gpu_q8_0_matvec_args mv_args = ds4_gpu_make_q8_0_mv_args(in_dim, out_dim);
+        mv_args.ne11 = (int32_t)n_tokens;
+        mv_args.ne1 = (int32_t)n_tokens;
         ds4_gpu_mv_dispatch mv_dispatch = ds4_gpu_make_q8_0_mv_dispatch();
         mv_args.nr0 = mv_dispatch.nr0;

         ds4_gpu_hc_expand_args hc_args = {
             .n_embd = n_embd,
             .n_hc = n_hc,
-            .n_tokens = 1,
+            .n_tokens = n_tokens,
             .nb_block0 = sizeof(float),
             .nb_block1 = (uint64_t)n_embd * sizeof(float),
             .nb_add0 = sizeof(float),
@@ -26659,8 +27107,8 @@ int ds4_gpu_shared_down_hc_expand_q8_0_tensor(
             .nb_res2 = (uint64_t)n_hc * n_embd * sizeof(float),
             .nb_post0 = sizeof(float),
             .nb_post1 = mix_hc * sizeof(float),
-            .nb_comb0 = sizeof(float),
-            .nb_comb1 = (uint64_t)n_hc * sizeof(float),
+            .nb_comb0 = (uint64_t)n_hc * sizeof(float),
+            .nb_comb1 = sizeof(float),
             .nb_comb2 = mix_hc * sizeof(float),
             .nb0 = sizeof(float),
             .nb1 = (uint64_t)n_embd * sizeof(float),
@@ -26692,7 +27140,7 @@ int ds4_gpu_shared_down_hc_expand_q8_0_tensor(
         [enc setThreadgroupMemoryLength:mv_dispatch.smem atIndex:0];
         [enc dispatchThreadgroups:MTLSizeMake(((NSUInteger)out_dim + (NSUInteger)mv_dispatch.nr0 - 1u) /
                                               (NSUInteger)mv_dispatch.nr0,
-                                              1,
+                                              (NSUInteger)n_tokens,
                                               1)
              threadsPerThreadgroup:MTLSizeMake(32, (NSUInteger)mv_dispatch.nsg, 1)];
         ds4_gpu_end_compute_encoder(cb, enc);
@@ -26703,6 +27151,48 @@ int ds4_gpu_shared_down_hc_expand_q8_0_tensor(
     return 1;
 }

+int ds4_gpu_shared_down_hc_expand_q8_0_tensor(
+        ds4_gpu_tensor       *out_hc,
+        ds4_gpu_tensor       *shared_out,
+        const void             *model_map,
+        uint64_t                model_size,
+        uint64_t                weight_offset,
+        uint64_t                in_dim,
+        uint64_t                out_dim,
+        const ds4_gpu_tensor *shared_mid,
+        const ds4_gpu_tensor *routed_out,
+        const ds4_gpu_tensor *residual_hc,
+        const ds4_gpu_tensor *split,
+        uint32_t                n_embd,
+        uint32_t                n_hc) {
+    return ds4_gpu_shared_down_hc_expand_q8_0_rows_tensor(
+        out_hc, shared_out, model_map, model_size, weight_offset,
+        in_dim, out_dim, shared_mid, routed_out, residual_hc, split,
+        n_embd, n_hc, 1);
+}
+
+int ds4_gpu_shared_down_hc_expand_q8_0_decode_order_batch_tensor(
+        ds4_gpu_tensor       *out_hc,
+        ds4_gpu_tensor       *shared_out,
+        const void             *model_map,
+        uint64_t                model_size,
+        uint64_t                weight_offset,
+        uint64_t                in_dim,
+        uint64_t                out_dim,
+        const ds4_gpu_tensor *shared_mid,
+        const ds4_gpu_tensor *routed_out,
+        const ds4_gpu_tensor *residual_hc,
+        const ds4_gpu_tensor *split,
+        uint32_t                n_embd,
+        uint32_t                n_hc,
+        uint32_t                n_tokens) {
+    if (n_tokens < 2) return 0;
+    return ds4_gpu_shared_down_hc_expand_q8_0_rows_tensor(
+        out_hc, shared_out, model_map, model_size, weight_offset,
+        in_dim, out_dim, shared_mid, routed_out, residual_hc, split,
+        n_embd, n_hc, n_tokens);
+}
+
 int ds4_gpu_matmul_q8_0_hc_expand_tensor(
         ds4_gpu_tensor       *out_hc,
         ds4_gpu_tensor       *block_out,
@@ -26775,8 +27265,8 @@ int ds4_gpu_matmul_q8_0_hc_expand_tensor(
             .nb_res2 = (uint64_t)n_hc * n_embd * sizeof(float),
             .nb_post0 = sizeof(float),
             .nb_post1 = mix_hc * sizeof(float),
-            .nb_comb0 = sizeof(float),
-            .nb_comb1 = (uint64_t)n_hc * sizeof(float),
+            .nb_comb0 = (uint64_t)n_hc * sizeof(float),
+            .nb_comb1 = sizeof(float),
             .nb_comb2 = mix_hc * sizeof(float),
             .nb0 = sizeof(float),
             .nb1 = (uint64_t)n_embd * sizeof(float),
diff --git a/metal/dense.metal b/metal/dense.metal
index ad1dd87..a3a82a5 100644
--- a/metal/dense.metal
+++ b/metal/dense.metal
@@ -914,6 +914,145 @@ template [[host_name("kernel_mul_mv_ext_q8_0_f32_r1_3")]] kernel mul_mv_ext_q4_f
 template [[host_name("kernel_mul_mv_ext_q8_0_f32_r1_4")]] kernel mul_mv_ext_q4_f32_t kernel_mul_mv_ext_q4_f32_disp<4, block_q8_0, 32, dequantize_q8_0_t4>;
 template [[host_name("kernel_mul_mv_ext_q8_0_f32_r1_5")]] kernel mul_mv_ext_q4_f32_t kernel_mul_mv_ext_q4_f32_disp<5, block_q8_0, 32, dequantize_q8_0_t4>;

+template<short r1ptg>
+void kernel_dsv4_shared_gate_up_swiglu_q8_0_ext_impl(
+        constant ds4_metal_args_mul_mv_ext & args,
+        device const char * src0_gate,
+        device const char * src0_up,
+        device const char * src1,
+        device       char * dst_gate,
+        device       char * dst_up,
+        device       char * dst_mid,
+        constant     float & clamp_value,
+        uint3   tgpig,
+        ushort  tiisg,
+        ushort  sgitg) {
+    const short NSG = FC_mul_mv_nsg;
+    const short nxpsg = FC_mul_mv_nxpsg;
+    const short chpt = 4;
+    const short chpb = 8;
+    const short nypsg = 32 / nxpsg;
+    const short tx = tiisg % nxpsg;
+    const short ty = tiisg / nxpsg;
+
+    const int i01 = tgpig.x * (nypsg * NSG) + nypsg * sgitg + ty;
+    const int i11 = tgpig.y * r1ptg;
+    const int i1m = tgpig.z;
+    const int i12 = i1m % args.ne12;
+    const int i13 = i1m / args.ne12;
+    const uint64_t offset0 = i01 * args.nb01 +
+                             (i12 / args.r2) * args.nb02 +
+                             (i13 / args.r3) * args.nb03;
+    const uint64_t offset1 = i11 * args.nb11 +
+                             i12 * args.nb12 + i13 * args.nb13;
+
+    device const block_q8_0 *xqg = i01 < args.ne01
+        ? (device const block_q8_0 *)(src0_gate + offset0) + tx / chpb
+        : (device const block_q8_0 *)src0_gate;
+    device const block_q8_0 *xqu = i01 < args.ne01
+        ? (device const block_q8_0 *)(src0_up + offset0) + tx / chpb
+        : (device const block_q8_0 *)src0_up;
+    device const float4 *y4[r1ptg];
+    for (short ir1 = 0; ir1 < r1ptg; ++ir1) {
+        y4[ir1] = i11 + ir1 < args.ne11
+            ? (device const float4 *)(src1 + offset1 + ir1 * args.nb11) + tx
+            : (device const float4 *)src1;
+    }
+
+    float sumg[r1ptg] = { [0 ... r1ptg - 1] = 0.0f };
+    float sumu[r1ptg] = { [0 ... r1ptg - 1] = 0.0f };
+    short cch = tx % chpb;
+    for (int ich = tx; 4 * ich < args.ne00; ich += chpt * nxpsg) {
+        float4 lg[chpt];
+        float4 lu[chpt];
+#pragma unroll(chpt)
+        for (short ch = 0; ch < chpt; ++ch) {
+            dequantize_q8_0_t4(xqg, cch, lg[ch]);
+            dequantize_q8_0_t4(xqu, cch, lu[ch]);
+            cch += nxpsg;
+            if (cch >= chpb) {
+                xqg += cch / chpb;
+                xqu += cch / chpb;
+                cch %= chpb;
+            }
+        }
+#pragma unroll(chpt)
+        for (short ch = 0; ch < chpt; ++ch) {
+#pragma unroll(r1ptg)
+            for (short ir1 = 0; ir1 < r1ptg; ++ir1) {
+                const float4 y = y4[ir1][ch * nxpsg];
+                sumg[ir1] += dot(lg[ch], y);
+                sumu[ir1] += dot(lu[ch], y);
+            }
+        }
+#pragma unroll(r1ptg)
+        for (short ir1 = 0; ir1 < r1ptg; ++ir1)
+            y4[ir1] += chpt * nxpsg;
+    }
+
+    for (short ir1 = 0; ir1 < r1ptg; ++ir1) {
+        if (nxpsg >= 16) {
+            sumg[ir1] += simd_shuffle_down(sumg[ir1], 8);
+            sumu[ir1] += simd_shuffle_down(sumu[ir1], 8);
+        }
+        if (nxpsg >= 8) {
+            sumg[ir1] += simd_shuffle_down(sumg[ir1], 4);
+            sumu[ir1] += simd_shuffle_down(sumu[ir1], 4);
+        }
+        if (nxpsg >= 4) {
+            sumg[ir1] += simd_shuffle_down(sumg[ir1], 2);
+            sumu[ir1] += simd_shuffle_down(sumu[ir1], 2);
+        }
+        if (nxpsg >= 2) {
+            sumg[ir1] += simd_shuffle_down(sumg[ir1], 1);
+            sumu[ir1] += simd_shuffle_down(sumu[ir1], 1);
+        }
+    }
+
+    if (tx == 0 && i01 < args.ne01) {
+        for (short ir1 = 0; ir1 < r1ptg && i11 + ir1 < args.ne11; ++ir1) {
+            const uint64_t row = (uint64_t)(i11 + ir1) * args.ne0;
+            device float *gate = (device float *)dst_gate + row;
+            device float *up = (device float *)dst_up + row;
+            device float *mid = (device float *)dst_mid + row;
+            gate[i01] = sumg[ir1];
+            up[i01] = sumu[ir1];
+            float g = sumg[ir1];
+            float u = sumu[ir1];
+            if (clamp_value > 1.0e-6f) {
+                g = min(g, clamp_value);
+                u = clamp(u, -clamp_value, clamp_value);
+            }
+            mid[i01] = (g / (1.0f + exp(-g))) * u;
+        }
+    }
+}
+
+template<short r1ptg>
+kernel void kernel_dsv4_shared_gate_up_swiglu_q8_0_ext_disp(
+        constant ds4_metal_args_mul_mv_ext & args,
+        device const char * src0_gate,
+        device const char * src0_up,
+        device const char * src1,
+        device       char * dst_gate,
+        device       char * dst_up,
+        device       char * dst_mid,
+        constant     float & clamp_value,
+        uint3   tgpig [[threadgroup_position_in_grid]],
+        ushort  tiisg [[thread_index_in_simdgroup]],
+        ushort  sgitg [[simdgroup_index_in_threadgroup]]) {
+    kernel_dsv4_shared_gate_up_swiglu_q8_0_ext_impl<r1ptg>(
+        args, src0_gate, src0_up, src1, dst_gate, dst_up, dst_mid,
+        clamp_value, tgpig, tiisg, sgitg);
+}
+
+typedef decltype(kernel_dsv4_shared_gate_up_swiglu_q8_0_ext_disp<2>)
+    dsv4_shared_gate_up_swiglu_q8_0_ext_t;
+template [[host_name("kernel_dsv4_shared_gate_up_swiglu_q8_0_r1_2")]] kernel dsv4_shared_gate_up_swiglu_q8_0_ext_t kernel_dsv4_shared_gate_up_swiglu_q8_0_ext_disp<2>;
+template [[host_name("kernel_dsv4_shared_gate_up_swiglu_q8_0_r1_3")]] kernel dsv4_shared_gate_up_swiglu_q8_0_ext_t kernel_dsv4_shared_gate_up_swiglu_q8_0_ext_disp<3>;
+template [[host_name("kernel_dsv4_shared_gate_up_swiglu_q8_0_r1_4")]] kernel dsv4_shared_gate_up_swiglu_q8_0_ext_t kernel_dsv4_shared_gate_up_swiglu_q8_0_ext_disp<4>;
+template [[host_name("kernel_dsv4_shared_gate_up_swiglu_q8_0_r1_5")]] kernel dsv4_shared_gate_up_swiglu_q8_0_ext_t kernel_dsv4_shared_gate_up_swiglu_q8_0_ext_disp<5>;
+
 constant bool FC_mul_mm_bc_inp [[function_constant(FC_MUL_MM + 0)]];
 constant bool FC_mul_mm_bc_out [[function_constant(FC_MUL_MM + 1)]];

diff --git a/metal/dsv4_hc.metal b/metal/dsv4_hc.metal
index fb67011..f170aa6 100644
--- a/metal/dsv4_hc.metal
+++ b/metal/dsv4_hc.metal
@@ -21,6 +21,14 @@ struct ds4_metal_args_dsv4_hc_weighted_sum {
     uint64_t nb1;
 };

+struct ds4_metal_args_dsv4_hc_mean_stage {
+    uint32_t n_embd;
+    uint32_t n_hc;
+    uint32_t n_stages;
+    uint32_t stage;
+    uint32_t n_tokens;
+};
+
 struct ds4_metal_args_dsv4_hc_split_weighted_sum {
     int64_t  n_embd;
     int32_t  n_hc;
@@ -643,7 +651,8 @@ kernel void kernel_dsv4_shared_down_hc_expand4_q8_0(
         uint3  tgpig[[threadgroup_position_in_grid]],
         ushort tiisg[[thread_index_in_simdgroup]],
         ushort sgitg[[simdgroup_index_in_threadgroup]]) {
-    if (hc.n_hc != 4 || hc.n_tokens != 1) {
+    const uint token = tgpig.y;
+    if (hc.n_hc != 4 || token >= hc.n_tokens) {
         return;
     }

@@ -659,7 +668,7 @@ kernel void kernel_dsv4_shared_down_hc_expand4_q8_0(
     const short il = tiisg % (NW / NQ);
     const int ib0 = sgitg * NQ + ix;

-    device const float *y = (device const float *)(shared_mid);
+    device const float *y = (device const float *)(shared_mid + token * mv.nb11);
     device const float *yb = y + ib0 * QK8_0 + il * NQ;

     device const block_q8_0 *ax[NR0];
@@ -717,25 +726,27 @@ kernel void kernel_dsv4_shared_down_hc_expand4_q8_0(

         const float shared_v = simd_sum(shmem_f32[row][tiisg]);
         if (tiisg == 0 && sgitg == 0) {
-            *((device float *)(shared_out + (uint64_t)d * sizeof(float))) = shared_v;
+            *((device float *)(shared_out +
+                ((uint64_t)token * mv.ne0 + d) * sizeof(float))) = shared_v;

-            float block_v = *((device const float *)(routed_out + (uint64_t)d * hc.nb_block0));
+            float block_v = *((device const float *)(routed_out +
+                (uint64_t)d * hc.nb_block0 + token * hc.nb_block1));
             block_v += shared_v;

-            const float r0 = *((device const float *)(residual + (uint64_t)d * hc.nb_res0 + 0 * hc.nb_res1));
-            const float r1 = *((device const float *)(residual + (uint64_t)d * hc.nb_res0 + 1 * hc.nb_res1));
-            const float r2 = *((device const float *)(residual + (uint64_t)d * hc.nb_res0 + 2 * hc.nb_res1));
-            const float r3 = *((device const float *)(residual + (uint64_t)d * hc.nb_res0 + 3 * hc.nb_res1));
+            const float r0 = *((device const float *)(residual + (uint64_t)d * hc.nb_res0 + 0 * hc.nb_res1 + token * hc.nb_res2));
+            const float r1 = *((device const float *)(residual + (uint64_t)d * hc.nb_res0 + 1 * hc.nb_res1 + token * hc.nb_res2));
+            const float r2 = *((device const float *)(residual + (uint64_t)d * hc.nb_res0 + 2 * hc.nb_res1 + token * hc.nb_res2));
+            const float r3 = *((device const float *)(residual + (uint64_t)d * hc.nb_res0 + 3 * hc.nb_res1 + token * hc.nb_res2));

             for (int64_t dst_hc = 0; dst_hc < 4; ++dst_hc) {
-                float acc = block_v * *((device const float *)(post + dst_hc * hc.nb_post0));
+                float acc = block_v * *((device const float *)(post + dst_hc * hc.nb_post0 + token * hc.nb_post1));

-                acc += *((device const float *)(comb + dst_hc * hc.nb_comb0 + 0 * hc.nb_comb1)) * r0;
-                acc += *((device const float *)(comb + dst_hc * hc.nb_comb0 + 1 * hc.nb_comb1)) * r1;
-                acc += *((device const float *)(comb + dst_hc * hc.nb_comb0 + 2 * hc.nb_comb1)) * r2;
-                acc += *((device const float *)(comb + dst_hc * hc.nb_comb0 + 3 * hc.nb_comb1)) * r3;
+                acc += *((device const float *)(comb + dst_hc * hc.nb_comb0 + 0 * hc.nb_comb1 + token * hc.nb_comb2)) * r0;
+                acc += *((device const float *)(comb + dst_hc * hc.nb_comb0 + 1 * hc.nb_comb1 + token * hc.nb_comb2)) * r1;
+                acc += *((device const float *)(comb + dst_hc * hc.nb_comb0 + 2 * hc.nb_comb1 + token * hc.nb_comb2)) * r2;
+                acc += *((device const float *)(comb + dst_hc * hc.nb_comb0 + 3 * hc.nb_comb1 + token * hc.nb_comb2)) * r3;

-                *((device float *)(dst + (uint64_t)d * hc.nb0 + dst_hc * hc.nb1)) = acc;
+                *((device float *)(dst + (uint64_t)d * hc.nb0 + dst_hc * hc.nb1 + token * hc.nb2)) = acc;
             }
         }
     }
@@ -883,3 +894,24 @@ kernel void kernel_dsv4_hc_weighted_sum(

     *((device float *) (dst + d*args.nb0 + t*args.nb1)) = acc;
 }
+
+kernel void kernel_dsv4_hc_mean_stage(
+        constant ds4_metal_args_dsv4_hc_mean_stage & args,
+        device const float * x,
+        device       float * dst,
+        uint gid [[thread_position_in_grid]]) {
+    const uint64_t n_elem = (uint64_t)args.n_tokens * args.n_embd;
+    if ((uint64_t)gid >= n_elem || args.n_hc == 0 ||
+        args.stage >= args.n_stages) return;
+
+    const uint32_t token = gid / args.n_embd;
+    const uint32_t dim = gid - token * args.n_embd;
+    const uint64_t input_row = (uint64_t)token * args.n_hc * args.n_embd;
+    float sum = 0.0f;
+    for (uint32_t stream = 0; stream < args.n_hc; stream++) {
+        sum += x[input_row + (uint64_t)stream * args.n_embd + dim];
+    }
+    const uint64_t output_row = (uint64_t)token * args.n_stages * args.n_embd;
+    dst[output_row + (uint64_t)args.stage * args.n_embd + dim] =
+        sum / (float)args.n_hc;
+}
diff --git a/metal/moe.metal b/metal/moe.metal
index d47fab2..f3fafb7 100644
--- a/metal/moe.metal
+++ b/metal/moe.metal
@@ -1,9 +1,16 @@
 // DS4 Metal routed-MoE matvec kernels.

 #define QK_K 256
+#define QK_MXFP4 32
 #define N_R0_Q2_K 4
 #define N_R0_Q4_K 2
 #define N_R0_IQ2_XXS 4
+#define N_R0_MXFP4 2
+
+static constant float ds4_metal_mxfp4_values[16] = {
+    0.0f, 0.5f, 1.0f, 1.5f, 2.0f, 3.0f, 4.0f, 6.0f,
+    0.0f, -0.5f, -1.0f, -1.5f, -2.0f, -3.0f, -4.0f, -6.0f,
+};

 static constant uchar ds4_metal_kmask_iq2xs[8] = {
     1, 2, 4, 8, 16, 32, 64, 128
@@ -110,6 +117,11 @@ struct block_iq2_xxs {
     ushort qs[QK_K/8];
 };

+struct block_mxfp4 {
+    uchar e;
+    uchar qs[QK_MXFP4/2];
+};
+
 struct ds4_metal_dsv4_moe_swiglu_weight_args {
     uint32_t width;
     uint32_t rows;
@@ -790,6 +802,82 @@ void kernel_mul_mv_iq2_xxs_pair_f32_impl(
     }
 }

+template<int nr0, typename args_t>
+void kernel_mul_mv_mxfp4_f32_impl(
+        args_t args,
+        device const char * src0,
+        device const char * src1,
+        device       char * dst,
+        threadgroup  char * shmem,
+        uint3  tgpig,
+        ushort tiisg,
+        ushort sgitg) {
+    const short NSG = FC_mul_mv_nsg;
+    threadgroup float * values = (threadgroup float *)shmem;
+    const int r0 = tgpig.x;
+    const int r1 = tgpig.y;
+    const int im = tgpig.z;
+    const int first_row = (r0 * NSG + sgitg) * nr0;
+    const uint i12 = im % args.ne12;
+    const uint i13 = im / args.ne12;
+    const uint64_t offset0 = first_row * args.nb01
+                           + (i12 / args.r2) * args.nb02
+                           + (i13 / args.r3) * args.nb03;
+    const uint64_t offset1 = r1 * args.nb11 + i12 * args.nb12 + i13 * args.nb13;
+    device const block_mxfp4 * x = (device const block_mxfp4 *)(src0 + offset0);
+    device const float * y = (device const float *)(src1 + offset1);
+    const int blocks = args.ne00 / QK_MXFP4;
+    const short ix = tiisg / 2;
+    const short it = tiisg % 2;
+    values[tiisg] = ds4_metal_mxfp4_values[tiisg % 16];
+    threadgroup_barrier(mem_flags::mem_threadgroup);
+    float sum[nr0] = {0.0f};
+    device const float * yb = y + ix * QK_MXFP4 + it * 8;
+    for (int ib = ix; ib < blocks; ib += 16) {
+        device const float4 * y4 = (device const float4 *)yb;
+        const float4 y0 = y4[0];
+        const float4 y1 = y4[4];
+        const float4 y2 = y4[1];
+        const float4 y3 = y4[5];
+        for (short row = 0; row < nr0; row++) {
+            device const block_mxfp4 & block = x[row * blocks + ib];
+            device const uchar * q = block.qs + 8 * it;
+            float4 acc = y0 * float4(values[q[0] & 15], values[q[1] & 15],
+                                     values[q[2] & 15], values[q[3] & 15]);
+            acc += y1 * float4(values[q[0] >> 4], values[q[1] >> 4],
+                               values[q[2] >> 4], values[q[3] >> 4]);
+            acc += y2 * float4(values[q[4] & 15], values[q[5] & 15],
+                               values[q[6] & 15], values[q[7] & 15]);
+            acc += y3 * float4(values[q[4] >> 4], values[q[5] >> 4],
+                               values[q[6] >> 4], values[q[7] >> 4]);
+            const float scale = as_type<float>((uint)block.e << 23);
+            sum[row] += scale * ((acc[0] + acc[1]) + (acc[2] + acc[3]));
+        }
+        yb += 16 * QK_MXFP4;
+    }
+    device float * out = (device float *)dst
+                       + (uint64_t)im * args.ne0 * args.ne1
+                       + (uint64_t)r1 * args.ne0;
+    for (int row = 0; row < nr0 && first_row + row < args.ne0; row++) {
+        const float total = simd_sum(sum[row]);
+        if (tiisg == 0) out[first_row + row] = total;
+    }
+}
+
+template<typename args_t>
+void kernel_mul_mv_f16_f32_id_impl(
+        args_t args,
+        device const char * src0,
+        device const char * src1,
+        device       char * dst,
+        threadgroup  char * shmem,
+        uint3  tgpig,
+        ushort tiisg,
+        ushort sgitg) {
+    kernel_mul_mv_t_t_disp<half, float, args_t>(
+            args, src0, src1, dst, shmem, tgpig, tiisg, sgitg);
+}
+
 typedef void (kernel_mul_mv2_disp_t)(
         ds4_metal_args_mul_mv args,
         device const char * src0,
@@ -893,6 +981,8 @@ template [[host_name("kernel_mul_mv_id_q8_0_f32")]]    kernel kernel_mul_mv_id_q
 template [[host_name("kernel_mul_mv_id_q2_K_f32")]]    kernel kernel_mul_mv_id_q_t kernel_mul_mv_id<mmv_fn<kernel_mul_mv_q2_K_f32_impl<N_R0_Q2_K>>>;
 template [[host_name("kernel_mul_mv_id_q4_K_f32")]]    kernel kernel_mul_mv_id_q_t kernel_mul_mv_id<mmv_fn<kernel_mul_mv_q4_K_f32_impl<N_R0_Q4_K>>>;
 template [[host_name("kernel_mul_mv_id_iq2_xxs_f32")]] kernel kernel_mul_mv_id_q_t kernel_mul_mv_id<mmv_fn<kernel_mul_mv_iq2_xxs_f32_impl<N_R0_IQ2_XXS>>>;
+template [[host_name("kernel_mul_mv_id_mxfp4_f32")]]   kernel kernel_mul_mv_id_q_t kernel_mul_mv_id<mmv_fn<kernel_mul_mv_mxfp4_f32_impl<N_R0_MXFP4>>>;
+template [[host_name("kernel_mul_mv_id_f16_f32")]]      kernel kernel_mul_mv_id_q_t kernel_mul_mv_id<mmv_fn<kernel_mul_mv_f16_f32_id_impl>>;

 // DS4 attention output low projection, specialized for the fixed block
 // diagonal mapping used by the model:
