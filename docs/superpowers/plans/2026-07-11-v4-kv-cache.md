# DeepSeek V4 KV Cache Test Plan

1. Add `attn.0.keys` to the real Transformers oracle and assert its tiny shape. Run the oracle test RED, implement with native projection/norm/RoPE helpers, then run GREEN and commit.
2. Add a C fixture and Make target for ring append, absolute positions, attention prefill/decode parity, and append-only indexer input. Confirm RED because the cache API and cached attention entry point do not exist.
3. Implement `v4_kv_cache.h`, then the smallest cached sliding-attention function in `v4_attention.h`. Run the KV, attention, indexer, and compressor fixtures and commit.
4. Run `make test-v4-oracle`, clean build, all C/V4 fixtures, Moonlight teacher-forcing and greedy smoke, `git diff --check`, and prohibited-artifact/status audits.
